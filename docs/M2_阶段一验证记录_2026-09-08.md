# M2 阶段一验证记录

对应 [M2 Task](测试版隔离与DSH研究交互验证_M2_2026-09-08.md)。这是执行证据，不改变 Task 范围和验收标准。

## 当前结论

已构建并实际打开独立测试 App，完成基本并存与退出检查。M2 **尚未完成**：首页/抽屉尚未接入 DSH，没有真实模型工具调用或点击引用验收。本记录中的文档读取是直接调用已有运行时的预检。

## 改动与产物

- `packaging/macos/build.mjs --test`：独立 App 名、Bundle ID、端口和数据目录，生成 `.app` 后停止，不生成 DMG。
- `WindowPolicy.swift`、`Launcher.swift`：从随包 Info.plist 读取身份和端口，窗口、菜单、启动地址、就绪检测和导航限制一致。
- `orchestrator/src/desktop_runtime_paths.ts`、`packaged.ts`：Codex、DSH 和临时目录置于指定数据根，目录权限 0700，拒绝子目录符号链接。
- `orchestrator/test/desktop_runtime_paths.test.ts`：隔离与符号链接边界测试。
- 未编辑 M1 业务页面、Stock-Research、正式 DSH 宿主或原安装版。未提交 Git。

产物：`.local/mac-builds/20260908T054458/VibeDeepResearchTest.app`。
构建命令：`node packaging/macos/build.mjs --test`。
随包 `Contents/Resources/build-manifest.json` SHA256：
`207088373f07e5ccb904dfe245ae1b205dc0e2b5cef4adc205d77cd3672c002f`。
版本继承上游 1.1.0 / build 40，manifest 的 milestone 仍继承 M40；**不代表通过上游正式发布验收或本轮 M2 验收**。临时签名，无公证、无发布。

## 实际隔离检查

| 项目 | 原安装版 | 本轮测试版 |
| --- | --- | --- |
| Bundle ID | `ai.phoenixtree.viberesearch` | `app.vibedeepresearch.test` |
| App 路径 | `/Applications/VibeResearch.app` | 上述 `.local/mac-builds/` 产物 |
| UI 端口 | 5938 | 5939 |
| 数据根 | `~/.vibe-research-desktop` | `~/.vibe-deep-research-test` |
| WebKit 目录，已实查存在 | `~/Library/WebKit/ai.phoenixtree.viberesearch` | `~/Library/WebKit/app.vibedeepresearch.test` |
| Codex / DSH / tmp | 未修改 | 数据根下 `codex-home` / `dsh` / `tmp`，均 0700 |

- 测试版首次显示「请接入 AI」，选择「暂不接入，先浏览」；未复制原版配置或登录。
- AppKit 标题和菜单显示「Vibe Deep Research · 测试版」。实际首页保留 M1 的 chat 和免责声明，无功能网格。
- 实际大盘行情页显示指数、全球市场、连板股和成交额数据。观察时市场情绪仍加载中，不能据此声称全部行情供应商通过。
- 两个进程同时分别监听 5938 / 5939。对测试端口重复启动返回 `EADDRINUSE`、退出码 1，没有连接占用者。
- 通过测试 App 的 Cmd+Q 退出后，5939 不再监听，测试服务 PID 65528 已退出；原版 PID 47346 未变，5938 首页仍 HTTP 200。
- 未测试 Claude / WorkBuddy 的外部 CLI 账户隔离；现有 UI 仍显示其本机订阅选项，不能将 Codex 独立数据根推论为全部来源账户隔离。

## 确定性检查

- `node --test orchestrator/test/desktop_runtime_paths.test.ts`：2 项通过。
- Swift WindowPolicy 测试：21 项通过。首次受默认编译缓存沙箱限制失败，改用 `/private/tmp/vdr-m2-swift-cache` 后通过。
- Home、workspace_visual、daily_review_context、lazy_routes、agent_toggle_cancel 定向测试：12 项通过，包含源码断言，不能替代 UI 验收。
- `npm run typecheck --prefix orchestrator`：通过（先通过锁文件安装开发依赖）。
- 打包内执行桌面 TypeScript 检查与 Vite 构建：通过，仍有上游大 chunk 提示。
- `codesign --verify --deep --strict`：通过，仅证明本地产物完整性。
- `git diff --check`：通过。

## DSH 与真实文档预检

- 在独立 `/private/tmp/vdr-m2-dsh-probe` 下启动钉住的 DSH rc.1 Web，监听 5941；验证后已终止探针。未复制正式 DSH 认证。
- SDK JSON-RPC 当前无逐会话 cancel；Web session controller 有 `cancel()`。原生 Web 客户端与 fork 分别使用 React 18 / 19。
- 已向用户提出原生会话嵌入与保留组件适配事件的选择，尚未收到决定，不预先改两入口。
- 真实 Backend `127.0.0.1:8700/api/v1` 可读；通过 Stock-Research 现有 DSH 文档运行时执行目录、导航、scan 和精确 Block 读取。
- 华友钴业 2025 年年报：document `c5d9c6cc82a6483b9cba0883c8c8fb9c`；revision `r-0d4e50b463b9424a`；hash `5dc582f6d2b6552050260b8d34f9571b8ef9633d792914b62c7e040f3f60811c`。
- 主营业务 Block `r-0d4e50b463b9424a:p9:b3` 两次回读结果完全一致，325 字，非截断。错误 hash 返回 409，无版本回退。
- 上述不含模型调用、DSH session 工具事件或 UI 点击；未写入事实、Wiki、草案或导入资料。

## 继续所需

确认 DSH 界面接法后，继续同一个 M2 Task：运行时随包、认证、两入口会话及取消、引用绑定与受限回读、真实模型和 App 内验收。此阶段一包仅用于查看隔离和 M1 界面，不能用于验收 DSH。
