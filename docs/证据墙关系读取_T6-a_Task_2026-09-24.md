# 证据墙关系读取与用户假设（T6-a）

状态：2026-09-24 自动实施与检查已回填 §9；真实界面由 T6-b（Claude）实施后用户验收。不提交、不 push。实施仓库：Stock-Research `backend/`（主），Vibe `desktop/dsh/finance-ui/research.mjs`（代理放行一处）。

权威：[产品对象模型与交互 v1](产品对象模型与交互_v1_2026-09-23.md) §4.5；[Human Checklist](../human-checklist.md)「证据墙“用户假设”作为研究关联的一种状态（2026-09-24）」及 2026-09-23 审阅裁决（硬关系在证据墙只读展示）；Stock `docs/standard.md` §2–§4（硬关系定义、端点与双时间）。

## 1. 目标

一个只读接口给出某议题的“证据墙”：墙上有哪些对象、对象之间有哪些连线（硬关系 / 研究关联 / 用户假设），每条线能回到依据。并让用户能在议题内直接写“假设”连线，之后经提案确认升格为研究关联。

## 2. 现状（2026-09-24 Claude 核实）

- 议题（`app/services/research_topics.py`，文件在 `workspace/research/topics/`）：`subjects`（如 `company:600900.SH`、`industry:sw2:801161.SI`，也混有“境外收入占比”这类非身份文字）、`observation.fact_refs / source_refs / relation_refs(claim:)`、`judgment.basis_refs`（`claim:` / `evidence:` / `source:` / `lookup:` / `page:<slug>@<hash>`）。
- 研究关联（`app/services/research_links.py`，`workspace/research/links/index.json`）：来源仅 `note:` / `result:`（及议题发布的 `wiki_material`），目标仅 `topic:` 或 Wiki 页；提案 → 确认两步；无状态字段。现有 8 条。
- 硬关系：PostgreSQL `claims`（`claim_kind=relation`），`WorldModelStore.local_graph`（`/entities/{id}/graph`）按单个根实体遍历。实体身份 `company:600900.SH`；NBS 行业 `industry:nbs:<官方名>`，对应 Wiki `industries/nbs-<简称>`（`wiki/constants.py`）；公司 Wiki `companies/600900-sh`。当前数据很少（长江电力：持股湖北能源、运营白鹤滩水电站；后者端点为 Asset，不属硬关系端点）。

## 3. 冻结决定

### 3.1 墙上的对象（节点）

从议题收集，去重后每个节点给 `ref`（产品对象引用：`companies/…`、`industries/…`、`themes/…`、`comparisons/…`、`document:<32位>`、`note:…`、`result:<32位>`）、`kind` 与 `origins`（为什么在墙上，可多个：`subject` / `basis` / `source` / `relation` / `link` / `hypothesis`）：
- `subjects` 中可解析的公司 / NBS 行业身份 → 对应 Wiki slug；申万二级与非身份文字忽略（2026-09-24 审阅更正）。
- `basis_refs` 的 `page:<slug>@…` → slug；`source:` / `evidence:` → `document:<文档ID>`；`claim:` → 该 Claim 的两端（若为公司 / NBS 行业）。
- `observation.source_refs` 同上；`observation.relation_refs` 的 Claim 两端。
- 以该议题为目标的已确认研究关联的来源（记录 / 成果 / 发布材料）。
- 本议题研究关联与假设的两端。
- 标签不在此接口解析（前端对象登记层负责）；节点 ≤ 60，超出按来源优先级（subject > basis > relation > link/hypothesis > source）截断并给 `truncated: true`。

### 3.2 连线

每条线：`edge_id`、`kind`（`hard` / `link` / `hypothesis`）、`from`、`to`（节点 `ref`）、`label`（中文）、`basis`（依据引用列表），总数 ≤ 200。
- **硬关系**（只读）：墙上公司 / NBS 行业节点**两两之间**的当前有效硬关系（`valid_at = known_at = 现在`）。谓词只取 standard §2：`subsidiary_of` 隶属、`owns_stake_in` 持股、`supplies_to` 供应、`customer_of` 客户、`produces`（对端为行业时）所属行业、`upstream_of` / `downstream_of` 上游 / 下游；Asset 等其他端点不出现。`basis` 为支持该边的 `claim:` 列表（前端点开走现有 `/claims/{id}`）。在 `WorldModelStore` 新增按实体集合一次查询边的方法，不逐节点遍历。**不向墙外扩展邻居。**
- **研究关联**：两端都在墙上的已确认关联（含本 Task 新增的对象 ↔ 对象关联）。以议题为目标的“记录属于议题”不画线（已体现为节点 `origins: link`）。
- **用户假设**：本议题的假设连线，`label` 为用户那句说明，另给 `unverified: true`。
- 硬关系读取失败（PostgreSQL 不可用等）时，其余照常返回，`sections.hard = "unavailable"`；正常为 `"ok"`。不静默当作“无关系”。

### 3.3 研究关联扩展

- **端点放宽**：来源与目标均可为公司 / 行业（`companies/` / `industries/`）、资料（`document:<32位>`）、记录 / 成果；目标仍可为议题。存在性校验沿现有 `_target_resolvable` / `_material_exists` 思路补齐资料与 Wiki 页。
- **状态**：新增 `status`：`hypothesis` / `confirmed`。现有记录无此字段即视为 `confirmed`（读取规则，不迁移数据文件）。
- **创建假设**：`POST /wiki/research-links/hypotheses`：`topic_id`（必填，须存在）、`from`、`to`、`note`（1–200 字）。**由用户直接创建，不经提案**；同议题同一对端点（无序）只允许一条假设；不经 Agent 工具开放。
- **撤回假设**：`POST /wiki/research-links/hypotheses/withdraw`：`hypothesis_id` + `topic_id`；只删除 `hypothesis` 状态记录。
- **升格**：沿现有 `propose` → `confirm`，`propose` 增加可选 `hypothesis_id` 与必填（升格时）`basis`（≥1 条 `claim:` / `evidence:` / `source:` / `page:` 引用，格式校验）；确认后该记录 `status` 改为 `confirmed`，保留原 `note`、`created_at` 与 `promoted_at`、`basis`、`proposal_id`；拒绝则假设原样保留。
- 研究关联仍**不是**硬关系：不写 `claims`，不进图遍历。

### 3.4 接口

- `GET /wiki/research-topics/{topic_id}/wall` → `{ topic_id, nodes, edges, sections: { hard }, truncated }`；只读，不触发任何外部取数、模型或 Jev 调用。
- Vibe `desktop/dsh/finance-ui/research.mjs` 代理放行：GET `…/research-topics/{id}/wall`；POST `/wiki/research-links/hypotheses`、`/wiki/research-links/hypotheses/withdraw`。

## 4. 范围

- Backend：`app/services/research_links.py`（端点放宽、状态、假设创建 / 撤回 / 升格）、`app/services/topic_wall.py`（新：节点收集、身份映射、连线组装）、`app/services/world_model_store.py`（集合内边查询）、`app/api/v1/wiki.py`（三个接口、`propose` 扩字段）、测试；Stock `docs/human-checklist.md` 一条、Wiki / 研究关联契约文档同步。
- Vibe：`dsh/finance-ui/research.mjs` 放行与对应测试。

## 5. 验收

- **执行方（自动）**：`cd backend && uv run pytest -q`（已知环境类失败同 T5-a §9）；`cd dsh && pnpm run test`；Vibe `cd desktop && env -u NODE_ENV node --test test/*.test.ts`；两仓 `git diff --check`；重建 Backend 容器与 finance-ui，重启工作台。新增测试：
  - 节点：subjects 身份映射（公司、NBS 行业、申万行业）、非身份文字忽略、`source:` / `evidence:` → 文档、`claim:` 两端、上限截断；
  - 硬关系：只出墙内节点间、只出 §3.2 谓词、Asset 端点排除、PostgreSQL 不可用时 `sections.hard = "unavailable"` 且其余照常；
  - 假设：创建（议题必须存在、说明长度、同对端点唯一）、撤回只删假设、升格须带依据且确认后状态改变并保留原说明与时间、拒绝后假设保留；
  - 现有无状态关联读为 `confirmed`，原有研究关联测试全部通过；
  - 墙接口不调用外部取数 / 模型（假客户端断言零调用）；代理放行三条路径。
- **执行方回填**：用本机真实议题（如 `topic:24ff5def6bf2` 长江电力相关）调一次墙接口，贴节点数、连线数、`sections` 与一两条示例（不含正文）。
- **用户验收**：随 T6-b 证据墙界面进行。

## 6. Out of Scope

- 证据墙界面、布局持久化、议题报告（T6-b / T6-c）；判断便签与“有变化”提示的展示（前端组合现有议题与 `basis-changes`）；墙外邻居扩展；Agent 读取墙或创建假设的工具；硬关系写入与准入改动；Jev。

## 7. Stop Conditions

- 公司 / 行业实体身份与 Wiki slug 无法确定性互转（需要猜测或模糊匹配）：停止，回填样例。
- 集合内边查询需要改硬关系表结构或准入：停止回填。
- 研究关联端点放宽会改变现有提案 / 确认语义或现有数据含义：停止回填差异。
- 与其他工具同时修改同一文件（T3-0 同期改 Vibe `desktop/`）：先看 git 状态，冲突时停下告知用户。

## 8. 回滚

还原代码；新增的假设记录在 `links/index.json` 中以 `status: hypothesis` 标识，可按此过滤删除。

## 9. 执行回填

2026-09-24 Codex 实施，未提交、未 push，未调用真实模型：

- Stock：`topic_wall.py` 按议题 subject、判断依据、观察引用及所属研究关联收集节点，并用现有确定性身份规则映射公司、41 个 NBS 行业与申万二级；最多 60 节点、200 连线，截断显式标记。`WorldModelStore.edges_within` 一次查询墙内当前双时间有效、已验证的标准谓词 Claim，支持依据回读；硬关系失败时墙的其他部分仍返回，`sections.hard = unavailable`。
- 研究关联继续使用 `links/index.json`：旧记录缺 `status` 时读为 `confirmed`；假设在指定议题直接创建、同一无序端点对唯一、只能撤回假设；附格式有效依据的 `propose → confirm` 原地升格并保留说明与创建时间，拒绝提案不改假设。新增对象端点与存在性校验、议题范围关联 ID；不写 Claim 或 GBrain Page Link。`wiki.py` 增加墙读、假设创建/撤回接口。Stock Human Checklist 与 Wiki 契约已同步。
- Vibe 主目录只修改 `desktop/dsh/finance-ui/research.mjs` 和 `desktop/test/my_research.test.ts`，放行墙 GET 与两条假设 POST，拒绝错方法。同期其他线在 Vibe 主目录出现的文档改动未触碰，也未访问其 worktree 或分支。
- 自动检查：Stock `env -u NODE_ENV UV_CACHE_DIR=/tmp/t6a-uv-cache uv run pytest -q` 为 **811 passed, 7 skipped, 6 failed**；6 项与 [T5-a §9](研究页版本差异_T5-a_Task_2026-09-23.md) 相同：5 项本机真实年报原件/解析产物缺失，1 项旧 `test_wiki_research_link_http_confirm_and_nbs_catalog` 缺 `STOCK_RESEARCH_GBRAIN_ROOT`。新增墙、假设、升格及原关联测试均通过。Stock DSH `env -u NODE_ENV pnpm run test` 为 **170 passed**；Vibe desktop `env -u NODE_ENV node --test test/*.test.ts` 为 **255 passed**。两仓 `git diff --check` 通过。受限沙盒首跑曾阻止本机端口、PostgreSQL 与 Stock `dsh/dist` 写入，随后按原命令在本机权限下复跑；以上为复跑结果。
- 按 §5 已重建 Backend 容器（`healthy`）、重建 finance-ui 并重启 Vibe 工作台（`http://127.0.0.1:5930` 已启动）。最终镜像对真实现存议题 `topic:24ff5def6bf2` 的只读 HTTP 墙请求返回 **10 节点、1 连线、`sections.hard = ok`、`truncated = false`**；示例节点 `companies/600900-sh`（`subject`、`relation`），示例持股线 `companies/600900-sh → companies/000883-sz`，依据 `claim:ee883cf1f797aee766c1b839`。未做真实界面或模型验收。

### 9.x Claude 审阅与补修（2026-09-24）

- 通过：墙节点收集、硬关系集合内一次查询（与 `local_graph` 同一有效性口径：validated + 双时间）、谓词与端点过滤、硬关系失败分节降级、假设创建 / 撤回 / 升格（原地保留说明与时间、拒绝不改假设）、旧关联读为 confirmed、代理放行与测试。
- 补修 1：研究关联列表 `list_links` 默认不返回假设。原实现让 `read_research_links`、`topic_list_links` 与维护核对读到未核验假设，可能被当作已确认关联；现只有证据墙（`all_links`）读取假设。
- 补修 2：申万二级身份（`industry:sw2:…`）不再成为墙节点。它属于「产业研究」口径，不是行业研究或硬关系端点（与 D 项一致）；此前是本 Task §3.1 写法有误，已按此更正。
- 补修 3（已撤回）：曾去掉对 `observation.fact_refs` 的关系端点回读，但真实议题把关系 Claim 也记在 `fact_refs`，导致湖北能源与持股线消失；已恢复原逻辑（非关系 Claim 仍会被忽略）。合并后真实议题墙：9 个对象（去掉申万“电力”）、1 条持股线、`sections.hard = ok`。
- 检查：Backend 相关 162 通过，1 失败为已知环境项（缺 `STOCK_RESEARCH_GBRAIN_ROOT`）；`git diff --check` 通过。需重建 Backend 容器生效。
- 记录（不阻塞）：同端点多条载荷不同的 Claim 会成为多条硬线；早于本 Task 的未绑定议题的对象间关联不画线（墙只画本议题范围的关联与假设）。
