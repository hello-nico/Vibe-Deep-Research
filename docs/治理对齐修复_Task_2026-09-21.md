# 治理对齐修复 · 审计发现的必须修复项

状态：2026-09-21 用户授权完善实施文档，交其他 Agent 整体实施，原对话 review；本文更新不代表代码或运行验收完成。权威：[Human Checklist](../human-checklist.md)「治理对齐审计处置」「研究内核推进顺序」。范围为 A–D、D2、F、G；E 仅记录后续包边界。M9.9 主任务已闭合，开工时核对其 review 结论及两仓当前改动，本包排在 M9.7 之前。真实启动与 DSH 重启属于本 Task 的运行验证；不得覆盖其他 Agent 在途改动。不提交、不 push，提交须另获授权。

## 0. DSH 参考基线与实施边界

- 2026-09-21 核实：产品运行基线为带补丁的 `0.1.2-rc.1`；参考仓库 `/Users/apple/ts/src/deepseek-harness` 本地为 `0.1.6-alpha.2`、HEAD `ddefc45fbc`，未核验远端最新版本。开工记录两仓 HEAD 与实际 runtime 版本，引用路径以当前源码为准。本 Task 不升级 DSH。
- DSH 是 Harness 设计的首要参考：模型配置、凭据、工具注册、执行、会话、轨迹、中止和作用域释放由原生运行时拥有；产品拥有金融对象入口、上下文绑定和展示；Stock Backend 拥有资料、证据、计算和知识持久化。不得新增平行 Provider、会话存储、执行器或前端证据映射。
- 上游阅读入口（均相对参考仓库）：`docs/architecture.md`、`packages/llm/llm/README.md`、`packages/core/system-prompt/README.md`。沿已有服务与插件接缝收敛职责；金融界面的布局、密度、引用层和视觉语言保持现有产品基线。
- `packages/util/launch-environment/README.md` 明确：环境来源快照与优先级不隔离子进程，各层仍写入 `process.env`。C 必须落实产品启动白名单，不能以采用上游环境分层替代。`packages/client/resources/README.md` 的资源地址也不保证不透明；A 的展示要求由产品契约验证。
- 上游 `packages/client/ui-subagent/src/client/sidebar-chat/index.tsx` 已使用 `sessions.retain(address, { source, signal })`。它是下次升级时评估 session patch 替代的证据，不是当前 rc.1 可删 patch 的依据。版本同步、API 适配和 patch 迁移归 [todo 第 7 包](../todo.md)，本轮仅登记。

## 1. 修复项与依据

### A. 技术身份不出现在用户界面

依据：Checklist「版本、hash 等技术身份不出现在用户界面，包括折叠详情」。

| 位置 | 现状 | 处理 |
|---|---|---|
| `desktop/src/verticals/finance/components/TaskTranscript.tsx` `BoundSourceList`（约 56–70 行） | 依据行渲染 `version.slice(0, 12)` | 去掉版本片段；保留标签与读取时点。版本只留在数据属性或依据面板的内部引用中 |
| `desktop/src/verticals/finance/components/EvidenceCard.tsx` → `pages/ReportReader.tsx` | 阅读链接把 revision/hash/block 放进 URL | 先核实已有不透明证据身份的覆盖范围；可用时由 Backend 只读解析回完整固定版本元组，`readPinnedBlock` 校验不变。无现成身份的路径按 Stop Conditions 报告缺口 |
| 问助手 chip 提示（原 `components/ui/FinanceAiDock.tsx`，以 M9.9 迁移后路径为准） | 旧实现 `title={item.hint \|\| item.id}` 可露出 `slug@input_hash` | 核对 M9.9 最终实现，已修则只回归；残留由本 Task 修复，提示只用业务 label/hint，不回退到 id |
| 页面上下文和 M9.9 固定快照中的版本身份 | 模型内部输入需要固定版本，用户展示不需要技术身份 | 保留内部绑定；仅核查并修复用户可见的正文、提示与依据展示，不更改 Ask/Agent 语义 |

`/my-research/topics/:topicHex` 的路径身份不在本 Task（见 Out of Scope）。

已核实 Stock `backend/app/services/ref_resolver.py`：`source:` 格式直接包含 document/revision/hash/block，不是不透明引用；`evidence:` 有既有记录解析分支，但未证明每个原始资料块都有对应记录。逐类核对 `lib/evidence.ts`、EvidenceCard 与 ReportReader 的实际输入，不能将参数改名、编码元组或省略版本视为完成。读取不得创建证据记录，不新增持久化或映射表；只读解析可复用现有存储身份。未知引用、版本失效、hash 不匹配必须明确失败，不能回退到最新解析版本。

### B. 对外来源名"同花顺"在源头修正，删除前端正则补丁

依据：AGENTS.md「对外名称统一同花顺」；用户明确：不允许前端给 Backend 输出打补丁，不允许用正则静默实现命名纪律。

- Stock：`backend/wiki/markdown_renderer.py:625` 的 `{"hithink": "扶摇", ...}` 改为 `"同花顺"`。全量排查 Backend 与 datasources 中面向用户的 `title` / `source_label` / `display_name` 字段是否还会产出"扶摇"或"同花顺扶摇"（含 `source_snapshot.title`、`datasources/registry.json` 的展示名）；模块 docstring 与注释中的"扶摇"不属于对外文本，不改。
- Vibe：`desktop/src/verticals/finance/lib/financialDisplay.ts:16-18` `sourceName()` 删除 `replaceAll('同花顺扶摇', ...)` 与 `/^扶摇$/` 两个正则分支，只保留 provider id → 显示名映射（`hithink` → `同花顺`）；`desktop/test/financial_display.test.ts:22` 对应用例改为断言不再做字符串替换。`citationMarks.ts`、`ResearchResult.tsx`、`wikiFacts.ts`、`resultSource.ts` 的调用点不需要改。
- 验收：用真实 Backend 渲染一份含 provider 事实的公司 Wiki，正文来源显示"同花顺"；前端 grep 无"扶摇"。

### C. DSH 子进程环境变量白名单

依据：AGENTS.md「取数进程仅获得必要环境变量」；用户确认 DSH 路径须对齐。

- `desktop/dsh-dev.ts` 移除启动 env 的全量继承；复用 `orchestrator/src/config.ts` 的白名单机制（当前 `pickEnv` / `BASE_ENV_KEYS` 未导出，按最小共享范围调整），保持一个筛选实现，按用途配置精确键名，不复制第二份逻辑。
- `desktop/dsh/setup.ts` 同时处理 `run(..., env = process.env)` 默认值和显式 spread，覆盖 Stock 的安装、构建与 DSH 插件注册子进程；安装/构建和产品运行分别核实必需键，不混用无限扩大的白名单。Stock 的 pnpm 使用保持不变。
- 开工枚举 runtime、产品插件和安装工具的实际读取点，为每个允许键登记消费者与用途：基础 OS、代理/证书、DSH 路径、产品路径及 `researchRuntimeEnv(paths)` 的确切输出。禁止按 `VRA_*`、`STOCK_RESEARCH_*` 或其他前缀无限放行。保持当前 DSH 配置/凭据 Owner，不读取、打印或复制用户认证文件，不创建产品 Provider 密钥配置链。
- 对照上游 `packages/credentials/credentials-local/README.md` 及已安装 rc.1 实现核实凭据来源优先级；不能因过滤改变已配置模型的有效接入。确有原生 Provider 环境变量依赖时，仅登记必要键名与来源类别；不记录值。验证原生已保存配置，并核对当前实际使用的环境变量接入路径；未覆盖路径明确列为未验收。
- 真实启动完成模型选择、一次回答和一次 Backend 读取；记录产品传给 DSH 的键集合，并区分 DSH 启动后自行加载的环境层，本 Task 不宣称隔离所有后代工具进程。保留现有 stderr 行为，仅修正不准确注释。

### D. 本地缓存与包管理器

- `.gitignore` 增加 `.pnpm-store/`（仓库根已有 553 MB untracked）。
- 项目声明的包管理器是 npm（`scripts/setup:51-55`、`scripts/setup-windows.ps1` 均为 `npm ci`；`desktop/package-lock.json` 已跟踪）。`desktop/package.json` 增加 `packageManager` 字段声明 npm 版本；`desktop/pnpm-lock.yaml`、`desktop/pnpm-workspace.yaml` 为未跟踪的杂散文件，确认非在途工作所需后删除，并在 `.gitignore` 增加 `pnpm-lock.yaml` 防再生。`desktop/dsh/setup.ts:21` 对 Stock-Research 仓库调用 `pnpm install` 属于对方仓库的包管理器，不在本 Task 改。
- 验收：`git status` 不再出现 `.pnpm-store/` 与 pnpm 锁文件；`npm ci` 在 `desktop/` 可重复安装。

### D2. DSH runtime patch 台账（文档项）

依据：Checklist 审计处置"patch 留着，不做迁移；补类型与移除条件"。

`desktop/dsh/runtime/README.md` 现有 patch 表增加"类型"与"移除条件"两列，按实际 diff 逐项区分 bug fix、产品功能、产品配置和文案；混合 patch 分条说明。session-controller 的 retain 记录上游 `sessions.retain` 候选，待升级后验证只读面板、关闭/释放与后台执行互不干扰；JSON 安全与 cwd 检查单列。settings-models、model-selection、conversation/chat/trajectory、llm-deepseek、client-modules、cordis-plugin-loader、deliverables 逐项列出现有依赖和可验证退出条件。不得把未核实的 `locale.extend` 等扩展点写成已可用能力，也不预设上游已修复 bug。不改任何 patch 内容。

### E. 问助手「存入沉淀」入口（移交 M9.9 后续包）

依据：Checklist「普通页面问助手……存入沉淀保留为记录入口」；用户确认接在 M9.9 已调整的问助手位置。

本 Task 不实施，记录边界供 M9.9 后续包引用：入口放在问助手回答条目上；保存调用现有 `POST /finance-research/notes`（`lib/notes.ts` 的 `addNote` → Backend），内容含用户问题、回答、本轮引用身份与页面来源；不启动 DSH 研究、不自动关联 Topic、不写 Client SQLite；对应记录在「我的研究」记录 Tab 可回读。

### F. 顶层遗留目录清理（2026-09-21 用户要求一并处理）

依据：AGENTS.md「原六阶段状态机、旧资料库……已退役」「不恢复旧 localStorage 模型配置与平行 Provider 控制链」「旧 macOS 独立客户端退出范围，不恢复原生外壳」。以下按 2026-09-21 引用盘点结果逐目录处置；每项先复核引用再动手，不凭目录名判断。

| 目录 | 盘点事实 | 处置 |
|---|---|---|
| `providers/`（8 个 Codex `model_provider` 模板 JSON） | `orchestrator/src/providers.ts` 仍在启动路径校验这些模板；`productConfig.ts` 的 `provider` / `wire_api` / `codexPath` 字段与之绑定；`service.ts`、`init.ts` import。这是被禁止的平行 Provider 控制链 | 删除目录与 `providers.ts`；`productConfig.ts` 去掉 provider / Codex 字段，只保留 `paths`（constitution / skills / data_root 等）与仍被消费的项；`init.ts` 去掉 `PROVIDER_ID_RE` 与 provider 引导；删 `providers.test.ts`，改 `productConfig.test.ts`。模型接入统一由 DSH 配置入口拥有 |
| `packaging/macos/`（Launcher.swift、build.mjs、签名脚本等 11 文件） | 仅被 `orchestrator/test/macos_packaging.test.ts`、`release_version.test.ts` 读取 | 删除目录与 `macos_packaging.test.ts`；`release_version.test.ts` 去掉对 `build.mjs` 的读取，保留对其余版本一致性的断言 |
| `backtest/`（33 文件，A/美/港股回测模块） | 无运行时引用；与 `calc/` 互相引用 `stdio_utf8.py`。产品纪律不给投资动作建议，回测不在产品范围 | 删除目录；`calc/tests/test_stdio_utf8.py` 去掉对 `backtest/` 的引用 |
| `knowledge/`（模板 + manifest，2 文件） | 旧资料库遗留；仅 `retirement.test.ts` 断言 `/knowledge/...` 路由已不存在 | 删除目录；测试不改（断言的是路由缺失） |
| `artifacts/`（M9 原型与工具失败探针） | README 当前仍链接 `artifacts/m9-research-workbench.html`，不能标为无引用；回测自身 run 目录与此无关 | 原型删除时同步清理 README/文档链接；仍被审计引用的探针迁入 `docs/` 并更新链接，无消费者的再删除；`.gitignore` 增加根目录 `/artifacts/` |
| `calc/`（确定性估值计算库） | 未发现所查 orchestrator 路径 spawn，但 `productConfig.calc_cli`、data-access/valuation Skill 及 Skill README 仍依赖计算契约 | 两仓核对直接/间接调用、脚本、Skill 与文档消费者，列出所需运算及 Backend 协议覆盖。真实调用仍有效则保留并登记；无有效调用且现用 Skill 所需能力均有可执行替代时，才删除目录/配置并更新全部消费者。仅改工具名或仅无 spawn 不足以证明可删；缺口按 Stop Conditions 处理 |
| `datasources/`（registry.json 等 9 文件） | `registry.json` 由 `orchestrator/src/registry.ts` / `mcp.ts` 运行时读取，活的 | 保留。`health.py` / `gen_catalog.py` 无引用：确认是否仍是开发脚本，是则移入 `scripts/` 并在 README 登记，否则删 |

附带核对：`desktop_runtime_paths.ts` 的 `codexHome` 与 `packaged.ts` 的 `VRA_CODEX_HOME` 属 Codex 时代路径；查实无读取方后随 providers 一起清理，有读取方则记录用途。README 第 38 行与相关模块映射随结果更新。

providers 清理必须覆盖 `service.ts`、`init.ts`、`productConfig.ts`、`packaged.ts` 的启动/初始化链，保留仍使用的数据根路径、Python 与取数配置。不得删除现用 Client、DSH 或 Backend 数据目录。按 Checklist 已确认范围，将前端 `lib/backend.ts`（orchestrator 客户端）按实际职责改名并更新 import，与 Stock 客户端 `research.ts` 区分；不改请求契约或借机重排整个 lib。旧 `core/ai/AiDock` / `AiMessages` / `useAiChat` 已裁决随 M9.9 清理，开工核查其结果，未落实则记为前置缺口，不称为未裁决。

验证：`orchestrator` 全量测试通过；`npm run doctor`（或等价启动自检）在无 `providers/` 时正常；真实启动产品一次，模型选择与 Backend 读取可用。

### G. 深度对话纪律文本归一（2026-09-21 用户决定）

依据：`dsh/src/prompt.ts` 的 `DOCUMENT_DISCIPLINE` 常量源于 2026-09-07 `a7fb480`，当时 `research-discipline.md` 与 Pi 共享、DSH 专属工具路由只能放代码。现 DSH 已有专属 `dsh/resources/research-discipline.md`（含五个 DSH 章节，`loadResearchDiscipline` 返回整文件），Pi 副本内容已分叉；Pi 一律不管、不维护、不同步。

处置：把 `DOCUMENT_DISCIPLINE` 全文并入 `dsh/resources/research-discipline.md` 作 `## Research tool routing` 章节（用 2026-09-21 review 合并后的版本，不再改句），删除代码常量，`loadResearchDiscipline` 只读文件；保留 `shared-research-principles` 标记校验，并在文件标记旁注一句"历史上与 Pi 共享，现仅 DSH 使用"。`prompt.test.mjs`、Vibe `assistant_context.test.ts` 的断言改为对文件内容断言。不动 `pi/` 目录任何文件。

保持现有 `ctx.systemPrompt.section` 注册、顺序、作用域和角色边界，不合并问助手各角色或后台纪律。本项只移动深度对话文本。注记若进入模型文本，须计入下述差异白名单，不以“文档注释”忽略实际输入变化。

验证：`rg -n "DOCUMENT_DISCIPLINE" dsh/src` 为空；以同一固定输入比较改前后的最终组装 prompt（仅纪律文本，不含用户会话或密钥），正文、工具路由、标记与角色贡献不变；仅允许明确列出的章节标题及上述历史注记差异，记录实际 diff。文件存在或单测通过不能替代组装结果对比。

## 2. 写范围

Vibe：A 的组件、读取解析与迁移后的问助手展示文件；`financialDisplay.ts` 及测试、`dsh-dev.ts`、`desktop/dsh/setup.ts`、`orchestrator/src/config.ts`、`.gitignore`、`desktop/package.json`、runtime patch README；F 的目录及 `orchestrator/src/{providers,productConfig,init,service,desktop_runtime_paths,packaged}.ts`、配置与测试、前端 orchestrator 客户端及 import、相关 README 与 `.agents/skills/{data-access,valuation}/SKILL.md`、Skill 索引。仅按依赖修改，不覆盖 M9.9 新模块或其他 Agent 在途代码。
Stock：`backend/wiki/markdown_renderer.py` 及排查到的对外展示名字段；必要时为不透明证据引用补最小只读解析接口，不新增持久化；G 项 `dsh/src/prompt.ts`、`dsh/resources/research-discipline.md`、`dsh/test/prompt.test.mjs`。

顺序：先记录两仓基线、确认 M9.9 review 与并行改动、核查 A 引用覆盖及 F 删除依赖 → D → G → F → B → A → C → 合并执行一次 F/C 的真实启动验证。单项触发停止条件时保留安全的独立进展，明确未闭合项，不将整包标为完成。

时序：本包不与 M9.9 修复并行；若开工发现相关代码仍在变动，先明确文件交接。D 删除未跟踪 pnpm 文件前确认无人依赖。重启仅用于本包已确认的真实验证，不替代 M9.9 自身验收；提交授权与实施授权分开。

## 3. 验收

1. 真实浏览器检查问助手依据、chip 和证据卡：用户展示无技术版本片段；按 A 的各输入类别验证链接，地址栏不含明文或编码后的版本元组，仍读取同一固定版本 block。覆盖新鲜页面/刷新回读及未知引用、版本/hash 失配的失败语义；未覆盖原始块身份则 A URL 项未闭合。
2. 真实 Backend 渲染公司 Wiki 的 provider 事实行显示"同花顺"；`rg 扶摇 desktop/src` 为空。
3. 按 C 分别验证安装/构建/注册与 DSH 运行入口；实际模型选择、回答、Backend 读取成功。记录允许键名、消费者、来源类别和传入键集合，证明未继承无关变量；只记录键名，不输出凭据值。DSH 自行加载环境的事实与产品传入白名单分别报告。
4. `git status --short` 无 `.pnpm-store/`、无 pnpm 锁文件；`desktop/package.json` 含 `packageManager`。
5. 相关单测通过；不以测试代替第 1–3 条真实验证。
6. 仓库根不再有 `providers/`、`packaging/`、`backtest/`、`knowledge/`、`artifacts/`；`orchestrator/src` 无 `providers.ts`，`productConfig` 无 provider / Codex 字段；`rg -n "Codex|codex" orchestrator/src` 只剩已登记用途的命中；`calc/` 与 `datasources/` 脚本的去留在本文记录核实结果。
7. G 的最终 prompt 对比、D2 patch 台账、F 的所有 Skill/README 链接及客户端改名引用均核查；定向测试、类型/构建与 `git diff --check` 按实际改动执行，orchestrator 全量测试及启动自检按 F 执行。保留现有金融 UI，真实交互验证与用户视觉验收分开记录。
8. Execute / Review / Smoke 结果追加在本文，保持总长不超过 200 行；列出两仓基线、修改文件、命令结果、真实运行证据、fixture 使用、review 结论和未闭合项。不要另建状态文档；未执行项不得写通过。

## 4. Out of Scope

- `awaiting_authorization` 状态归属与草案过期语义（待用户讨论）。
- `/my-research/topics/:topicHex` 路径身份是否更换（路由身份，需另行决定）。
- 四个产品性 DSH runtime patch 的迁移（另行分析）。
- `$DSH_HOME/research/*.json` 跨仓契约、插件角色注册表、`client.tsx` 拆分等架构项。
- M9.9 已负责的旧 AI 组件清理、Ask/Agent 语义及其模块重构；本包只核查前置结果和 A 的展示残留。
- Stock-Research 的包管理器切换；DSH 版本升级及上游源码修改；新增证据身份持久化、用户数据迁移或删除。

## 5. Stop Conditions

- Backend 没有可由前端持有的不透明证据引用能解析回 `(document_id, parse_revision_id, parsed_content_sha256, block_id)`：停止 A 的 URL 项，回报缺口，不在前端建本地映射。
- 排查发现"扶摇"来自第三方返回的原始 `title`（非本方代码写死）：在 Backend 适配层归一化为"同花顺"，不得在前端处理；若适配层无统一出口，回报。
- 白名单启动后模型接入或 Backend 连接失败且无法定位必要变量，或必须改变凭据归属/优先级才能运行：停止 C，记录缺失消费者与键名并回报。不能把全量 env、前缀放行或第二条 Provider 链作为修复交付；需恢复既有服务时只恢复本轮改动前基线并注明 C 未完成。
- 删除 pnpm 文件或修改文件会影响其他 Agent 在途工作：先明确交接，不删、不覆盖。
- F 项：拆掉 providers 链后启动自检或真实启动失败，且失败源于 `productConfig` 之外的隐性依赖：停止，回报依赖点，不为通过启动而保留半套 provider 校验。`calc/` 查到有真实调用方但调用方本身已退役：一并回报，由用户决定，不单删其一。
- `calc/` 仍有有效消费者或 Backend 无法覆盖现用 Skill 所需计算/证据绑定：保留相关能力并记录具体缺口，不强删、不扩大为计算协议重构；待责任归属明确后再处理该项。
- 同一行为两次修补仍不闭合：重新核对 Owner，不叠补丁。

## 6. Execute 记录（2026-09-21，本 Agent 实施结果）

基线：Vibe HEAD `b0a86edd`（main；工作树含 M9.6/M9.9 在途改动：cninfo.py、CompanyPeek、startup.ts、scripts/start.ps1、scripts/README.md、todo、human-checklist、docs，本包未覆盖）。Stock HEAD `8e4847c`（工作树在途：industries.py、wiki.py、wiki_publish_lock、gbrain_adapter、workspace 等，未触碰）。参考仓库 `deepseek-harness` HEAD `ddefc45fbc`（0.1.6-alpha.2）；产品运行基线带补丁 `0.1.2-rc.1`，未升级 DSH。M9.9 状态行：用户验收通过。开工前置核查：`core/ai/AiDock`、`AiMessages`、`useAiChat` 死代码仍在 —— 记为前置缺口（M9.9 负责清理，本包不代做）。

修改（Vibe）：`.gitignore`、`desktop/package.json`（+packageManager npm@10.9.8）、删 `desktop/pnpm-{lock,workspace}.yaml`；`orchestrator/src/{config,productConfig,init,service,doctor,packaged,desktop_runtime_paths}.ts`、删 `orchestrator/src/providers.ts`、删 `providers/`、`packaging/`、`backtest/`、`knowledge/`、`orchestrator/test/{providers,macos_packaging}.test.ts`、改 `productConfig/init_doctor/desktop_runtime_paths/release_version/page_query/ledger/retirement/service_api_mcp/source_hygiene/ui_adapter` 测试、`calc/tests/test_stdio_utf8.py`、`vibe-research.config.json`、`scripts/{setup,init,README.md,gen_catalog.py,health.py}`（后两者自 datasources 迁入）、`datasources/README.md`、`artifacts/` 清空（原型删，探针迁 `docs/probes/`，审计文档链接同步）、`README.md`（M9 边界图链接）、`docs/README.md`（provider 模板行）、`desktop/dsh/runtime/README.md`（D2 台账）、`desktop/dsh-dev.ts`、`desktop/dsh/setup.ts`、`desktop/src/verticals/finance/lib/{financialDisplay,localService(原 backend),wikiFacts 未改}.ts`、`lib/{api,prefs,watchlist,researchRoster}.ts`、`pages/{DailyReview,ReportReader}.tsx`、`components/{TaskTranscript,EvidenceCard}.tsx`、`components/ui/FinanceAiDock.tsx`、`test/{financial_display,daily_review_context,null_semantics,quote_snapshot,global_indices}.test.ts`。

修改（Stock）：`backend/wiki/markdown_renderer.py:625`（同花顺）；`dsh/src/prompt.ts`（删 DOCUMENT_DISCIPLINE，loadResearchDiscipline 只读文件）；`dsh/resources/research-discipline.md`（并入 `## Research tool routing` + 英文历史注记）；`dsh/test/prompt.test.mjs`（文件内容断言）。

命令结果：Stock DSH build 后 `node --test test/*.test.mjs` 120/120；backend pytest renderer 26 过、wiki 156 过 7 skip；Vibe desktop `npm test` 177/177、`tsc --noEmit` 过、`npm ci` 成功（7s）；orchestrator 带 `VRA_PYTHON=.venv` 全量 127 中 126 过 0 失败（1 个 Windows 用例 macOS 跳过；不带该变量时 2 条签名棘轮因找不到 `../.venv/bin/python` 失败，与本包无关）；`git diff --check` 干净；G 项固定输入对比：改前 `文件+\n\n+常量` vs 改后文件仅差历史注记行与末尾一个空行，工具路由/正文/标记不变。

真实运行证据：
- F：无 `providers/` 下 `node orchestrator/src/doctor.ts` 5 项全 OK（117 端点、凭据扫描 0 命中）；`.pnpm-store` 加入 doctor 扫描跳过目录（pnpm 缓存内第三方内容曾触发 1 命中）。
- C/F：真实重启验证（旧实例是本包改码前启动的，先精确结束本仓库 api/vite/build-ui/dsh 4 个进程再启动）。启动日志记录传给 DSH 的环境键集合：最终 17 键 = 基础 OS 9（HOME/LOGNAME/PATH/SHELL/TERM/USER/TMPDIR/LANG + DSH_HOME）+ 研究运行时 6（STOCK_RESEARCH_BACKEND_URL/HOOK_TOKEN/WORKSPACE/GBRAIN_ROOT/GBRAIN_DATABASE_URL/ACCUMULATE）+ VRA_FINANCE_DATA_ROOT + DEEPSEEK_API_KEY；本机 shell 未设代理证书键，故无传入。
- C：问助手（个股研究·紫金矿业）真实回答成功：Ask 零工具，答 pe_ttm=12.380501 倍（hithink，观测时间 2026-09-18 14:47:17）并说明口径与时点。模型接入核验：`DSH_HOME/.credentials.yaml` 无 DEEPSEEK 键、当前实际接入路径是启动环境 `DEEPSEEK_API_KEY` → 首轮 16 键白名单回答失败（no API key for provider route），按 C 项“确有原生 Provider 环境变量依赖”仅补登记该键名（值不进产品）后重启复验通过；接入优先级仍由 DSH 原生分层拥有，未改凭据归属。
- B：用户执行 `docker compose up -d --build backend` 重建 Backend 容器后，`/wiki/pages/read?slug=companies/601899-sh` 渲染紫金矿业公司 Wiki：正文“同花顺” 15 处、“扶摇” 0；`rg 扶摇 desktop/src` 空；financial_display 测试改为断言不做字符串替换。
- A：ReportReader 以 `?ref=evidence:…` 打开 `/my-reports/read/{doc}`：地址栏无明文/编码版本元组，正文经只读 `/wiki/refs/resolve` 解析回固定元组后由 `readPinnedBlock` 校验定位到第 206 页表格块，内容正确；未知 evidence 引用明确报“原引用暂时无法核对”，不回退最新正文。chip：mention 候选与已选 chip 均无 `slug@input_hash`（title 只用业务 label/hint）；BoundSourceList 不再渲染 `version.slice(0,12)`（版本保留在 `data-version` 内部属性）。
- 验证浏览器为专用实例，验证后已关闭；产品保持运行（与开工前状态一致）。未提交、未 push。

未闭合项 / 缺口：① A 的 URL 项对没有 evidence 身份的引用（如直接引用的 `source:` 块）没有不透明身份可用：这类引用保留旧参数路径（明文元组仍在地址栏），按 Stop Conditions 记录缺口、不在前端建映射；`claim:` 汇总经 related 展示具体 evidence 时链接已跟随实际展示的 evidence 身份（见下方 Review 记录）。② C 未验收路径：经网页设置页把密钥写入凭据存储的接入未实测（当前实际路径是环境变量）。③ 前置缺口：`core/ai` 旧三件套未随 M9.9 删除。④ Backend 服务是 Docker 镜像（代码需重建镜像才生效），后续重启用 `docker compose up -d --build backend`。⑤ Stock 仓工作树在途改动（industries.py 等）不属本包，未核销。

## 7. Review 记录（2026-09-21，第一轮）

Review 结论：2 个 P2，方向无新阻塞；实施方修复后复验，review 方独立复跑结果（类型检查、11/11、8/8、diff check）不重申为已验收。

1. 已修（P2）：ReportReader 引用切换残留。解析状态绑定当前引用（`resolvedRef` 存 ref 名，`refTuple` 派生），`?ref=` 变化瞬间旧元组失效；正文 effect 在 `refParam && !refTuple` 时清空旧正文、选区与 selectionChanged 标记，旧读取由 cleanup abort；失败只置 refError，不残留上一条引用内容。
2. 已修（P2）：EvidenceCard 链接身份改为跟随实际展示 block 的 evidence 引用：新增 `displayedRef`，claim:/汇总经 `related[0]` 展示具体 evidence 时用它生成 `?ref=` 链接，不再回退到明文元组参数；`source:` 等无 evidence 身份的路径仍为缺口（见未闭合项 ①）。

修复后：desktop `tsc --noEmit` 过、`npm test` 177/177、`git diff --check` 干净；本修复未重做浏览器真实验证（待用户或下一轮验收）。

## 8. Review 记录（2026-09-21，第二轮）

Review 结论：claim 链接修复确认；另 1 个 P2 —— 解析状态只按引用名比对，返回曾成功的引用且本次失败时旧元组复活并掩盖错误（review 方以真实 React 组件加模拟响应复现）。

已修（P2）：解析改为按"当前解析请求"的状态机控制。新增 `lib/refResolution.ts`：`RefResolution`（pending/resolved/failed，携带发起请求的引用名）+ 纯 reducer —— `start` 即进入 pending（旧元组失效），只有当前 pending 同一引用的响应才写入，迟到/过期/终态后响应一律拒绝；`activeRefTuple` 仅在 resolved 且引用名匹配当前 `?ref=` 时给出元组。ReportReader 改用 `useReducer` 接入：任何引用切换（含返回旧引用）先落 pending，失败只置 failed 态，不再缓存旧成功结果；错误提示由 failed 态派生，不再被旧元组隐藏。

回归测试 `test/ref_resolution.test.ts`（3 条，覆盖 review 指定的三条路径：返回旧引用且本次失败不复活元组、切换后旧元组立即失效、迟到/过期/终态后响应被拒；另含 location 嵌套元组提取）。修复后：desktop `tsc --noEmit` 过、`npm test` 180/180、`git diff --check` 干净；组件级行为已由状态机单测覆盖，真实验收仍待用户。
