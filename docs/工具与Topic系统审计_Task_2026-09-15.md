# 工具与 Topic 系统审计

## 范围与权威

- 用户授权：首轮审计已交付；随后授权基于多轮实际 sessions 作出初步判断，实施工具收敛及协议补齐。
- 产品边界：`../human-checklist.md`「工具与 Topic 生命周期系统审计」。
- 研究协议：Stock-Research 现有 Backend、DSH 工具及 `docs/human-checklist.md`。
- 审计结果沿现有 `Tools与Skills审计_M7前置_2026-09-10.md` 更新，旧章节保留为历史。

## 工作包

2026-09-16 已确认的后续有界闭环：合并 Wiki 正文、关系导航、方法/示例读取；关系词汇退出普通深研，统一 DSH 注册名及产品消费者。校验原角色绑定、中止/错误归集、历史图表展示与核心页阅读状态。文档工具、动态能力加载、Backend/Pi 不在本轮改动范围。合并后的精确数量以注册结果为准，旧结果段为前一轮证据。

1. 从真实 DSH request/header 提取全部工具、schema 和工具调用统计；与当前角色注册对照。
2. 全工具覆盖职责、输入、输出、状态前置、副作用、错误恢复；区分真实失败和静态风险。
3. 按真实业务场景提出保留、合并、限制可见、内部化和退役建议；不预设工具数量指标。
4. 对照 Topic/Wiki 现有约定与执行链，提出触发、创建、更新、追踪、失败和中止边界。
5. 交付证据清单、缺口优先级、分包实施建议及真实验收矩阵。
6. 依据历史轨迹确定本轮最小改动，补齐公开协议、减少无关暴露、同步示例；保护 Backend 校验。
7. 运行真实 DSH schema 注册与工具调用测试、历史失败参数重放、构建；能运行新模型时验证实际加载与自然问题。

## 验证与完成标准

- 真实请求全部工具均有清单，不用源码计数冒充实际加载。
- 实际角色外工具单列；每个协议发现有源码或轨迹依据。
- 生成材料不含系统全文、模型思考、凭据或个人配置。
- 机械检查文档链接、工具覆盖和 diff；不把只读审计称作模型对照实验。
- 当前轮完成以审计文档、可复核的生成清单和明确待决项为准。
- 修改后机械重放与新模型效果分开；不为验证制造生产 Topic 或发布数据。

## 本轮执行结果

- 已扫描 63 份真实会话文件，提取 469 次调用与 87 次错误；历史版本、验收与自然问题混合，不作为当前错误率。
- 已实施普通研究 Topic 只读、绑定 Topic 写入门禁、显式 UI 发起、计算 schema 收敛、文档/行情/Topic/PageSpec 协议补齐及 Wiki 导航输入精简。
- 构建后 DSH 注册：普通研究 30 tools / 33,900 字符；此前真实请求 32 / 85,039。定义精简不等于模型表现验收。
- DSH 构建和 69/69 测试通过；前端构建及议题相关 14/14 测试通过，后者包含源码契约断言及 mock HTTP。
- 真实 Backend 既有行情成果读取、双区间计算、再读一致通过；未新建成果图或 Topic。
- 浏览器已确认发起表单布局；未执行生产创建。协议清单、历史摘要和真实 Backend 回执在 `../artifacts/tool-audit*` 与 `../artifacts/session-tool-audit-2026-09-15.json`。
- 完整说明见审计文档 F 节。新模型采样、服务加载及同条件质量比较仍待运行；未重启服务、提交或 push。

## Out of Scope

- 数据迁移、Topic 清理、提交和 push；Backend 公开 API/持久化变更。
- 恢复六阶段、引入新框架、重建事实准入或发布权限。

## 同类读取收敛结果（2026-09-16）

- 已合并核心/组合 Wiki 正文为 `wiki_read`、入出边/遍历为 `wiki_relations`、方法/示例为 `read_research_method`；关系词汇仅“我的研究”可见。其余工具统一去旧前缀，原始行情仍是通用计算的合法算子，因此用 `calculate_metrics` 而非财务专名。
- 导航工具改为 Agent 作用域注册；中止与错误归集引用同一角色工具集合。Topic 原绑定权限保留，无旧名注册别名；产品保留历史生成/搜索事件展示，同步新 Topic 提示。
- 使用本机安装的 GBrain operation schema，在真实 DSH ToolRuntime 注册得到普通深研 25 个、“我的研究”34 个。清单及验证日志在 `../.local/tool-convergence-2026-09-16/`；这不是现用服务的新模型请求。
- `pnpm --dir dsh test`：72/72；产品 `npm test --prefix desktop`：88/88；`npm run build --prefix desktop` 与两仓 `git diff --check` 通过。新增回归覆盖核心页不可旁路、改名后中止/错误归集、角色目录、资源枚举、旧会话投影；HTTP/MCP/模型使用受控 fixture，实际 DSH 生命周期接线运行。
- 真实 Backend 只读验证：`wiki_read` 返回 `companies/600674-sh` 已接受页面并保留 readPages；`read_research_result` 连读已有 243 行行情快照一致，未生成新成果。首次辅助查询误传证券代码而非对象 slug 返回 422；核对现有 API 后使用返回的对象 slug，不改 Backend 校验。
- 独立差异审查发现搜索结果过期错误仍提示旧名；仅将 Backend 两处恢复文案改为运行时中立表达，API/缓存/Pi 行为不变。`pytest -q tests/test_external_search.py`：16 passed、2 项依赖弃用警告。其余无新增阻断问题。
- 未重启现用 DSH/Backend、未进行新模型对照或真实 GBrain 图数据查询；没有创建 Topic、发布页面、提交或 push。本轮实现与机械回归完成，模型选择改善仍待同条件实际问题验证。

## 2026-09-18 全工具失败协议复审（分析交付）

- 用户授权：全面复审并先看结论；本轮不实施产品修复。
- 当前四角色 allowlist 去重 35 项，逐项审计记录在原审计报告 G 节；不是运行中模型请求的工具数。
- 确认八组缺口 F01–F08：行情/Radar诊断丢失、公共HTTP适配、报告Wiki输出、Topic冲突恢复、草案部分成功、成果重试身份及公开限额。
- 新增只读故障探针和JSON结果：`../artifacts/tool-failure-probes-2026-09-18.{mjs,json}`；HTTP为合成数据，执行使用真实DSH ToolRuntime，不使用真实模型或生产写入。
- Stock DSH构建与全量95/95通过；故障探针仍复现多个错误，证明旧测试缺乏跨层失败保真和角色组合覆盖。
- F06仅源码证据；F07已验证操作身份变化，但未制造真实保存后丢回执。真实模型/浏览器/生产故障注入未跑。无产品代码改动、服务重启或提交。
- 后续修复按G节顺序，必须验证最终模型可见错误与部分成功回执；不靠用户逐一手测工具发现确定性缺陷。

### 2026-09-18 修复授权

- 用户明确授权主 Agent 亲自修复 F01–F08；本包允许针对失败诊断、部分成功和幂等恢复调整现有 Backend 接口，不扩大工具权限或发布范围。
- 验证使用最终模型可见输出、四类报告读取、冲突后重读、部分写入恢复及保存后回执丢失；保护既有未提交改动。不提交、不重启服务。

### 修复交付

- 主 Agent 直接完成 F01–F08，具体行为与验证边界见原审计 G 节「主 Agent 修复结果」。未新增业务工具、执行器或权限；仅给原草案/成果工具补显式恢复参数。
- DSH 构建＋103/103；Backend 定向 52 passed；两仓 diff 检查通过。新增故障测试覆盖实际 DSH 输出，HTTP 为受控响应；Backend 恢复测试使用临时仓与 SQLite，不写生产数据。
- 旧故障探针是修复前基线，保留不改；修复后的验证入口为 Stock `dsh/test/failure-protocol.test.mjs` 及 G 节列出的 Backend 测试。
- 未重启服务、未提交。真实模型与浏览器验收尚未执行，代码级通过不意味着运行中旧进程已加载修复。

## 本次复审 Stop Conditions

- 工具合并改变公开 API、持久化或权限时，先提交具体方案供用户判断。
- 缺少实际请求或 Provider 原因时保留未知，不用猜测补全证据。
- 用户尚未确认的新 Topic 语义不得写成已生效规则。
