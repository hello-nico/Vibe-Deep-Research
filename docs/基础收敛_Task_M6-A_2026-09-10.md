# M6-A · 对话入口与外壳基础收敛

日期：2026-09-10。状态：用户已授权实施；代码收敛已完成，随 `79773cc` 提交，真实运行与最终体验待验收。此 Task 的 Execute、Review、Smoke 共用本文，不自行扩大或改写范围。

## 目标与上位依据

先完成一轮有界基础收敛，再实现证据锚点与一种交互成果；不以实现整张目标架构图为本轮完成标准。
已确认 Owner 与执行路径见 [M4](研究闭环与DSH交互_M4_2026-09-08.md)，目标图与接口限制见 [M6](架构边界与UI组合方案_M6_2026-09-09.md)，人类决定见 [Human Checklist](../human-checklist.md)。M5 继续拥有整体视觉体验，M4 业务缺口不由本 Task 关闭。

本轮交付：外壳控制展示位置；可复用对话模块控制对话交互；业务继续提供页面上下文。沿已有引用入口确定下一轮接入位置，不实现新证据功能、不预建空接口。

## 当前基线与具体问题

- 当前 HEAD 为 `4c16b1c`，存在用户未提交修复。实施开始重新核对 HEAD、status 与相关 diff，以最新工作树为准。
- `dsh/client.tsx` 同时处理 root、主题、工作区/会话启动、历史与研究触发；只提取本轮涉及的 root 组合，不顺便重排所有启动逻辑。
- `Layout.tsx` 直接放置 `FinanceAiDock`；`core/ai/AiDock.tsx` 同时包含入口按钮、对话状态及 body Portal 弹层，承载与交互责任混合。
- 已有 `AiPageProvider/useAiPage` 与 `useAiChat`；继续使用这两个 Owner，不新造上下文总线或会话 store。
- `AiDock.close()` 当前调用 `abort()`；关闭/Esc 取消请求是本轮保留的基线，不改成后台继续生成。
- `NativeDshHost` 通过 DOM ID + Portal 承载原生会话/设置/状态。先保留已工作的原生会话路径，不以删除 Portal 为验收目标。
- `ResearchKnowledge.ReferenceButtons` 已调用 `/wiki/refs/resolve`，目前是页面内引用展示；不能声称原生回答已有证据锚点。

## 本轮范围与交付

| 工作 | 具体交付 | 到此停止 |
| --- | --- | --- |
| 基线核对 | 记录本轮相关用户 diff、现有关闭/切页/停止行为、DSH 实际版本和使用到的 slot 类型 | 相关预存失败独立说明，不顺手修复无关问题 |
| 外壳组合 | 将 root 展示组合从 `client.tsx` 收拢到产品布局模块；保持 Provider 顺序、slot 声明、会话身份与工作区初始化语义 | 不拆所有注册函数，不改启动/缓存失败策略 |
| 页面助手分责 | 从已有 AiDock 分离可复用对话控制/内容与布局承载；Finance 适配拥有文案和 `sendPageModel`，外壳负责入口位置、面板几何与层级 | 保留 `useAiChat`、AiPage、原存储和取消逻辑；不合并原生 Agent 对话 |
| 实际装配 | 以页面助手作为真实消费者，将助手视图通过现有 `shell.overlay` 装配；顶栏调用同一个助手 Owner 的打开动作 | 优先现有 slot；不新增 finance.page、导航注册服务或空证据 slot |
| 后续接缝核对 | 在本 Task 交接段记录真实引用解析入口、原生消息扩展候选、资源/会话定位与不支持项 | 只形成 M6-B 的证据，不替换原生消息 renderer、不新增引用协议 |

同一助手控制实例管理开关、草稿及对话绑定，不能在顶栏和浮层各创建一份。外壳提供承载配置/容器，对话内容不读取外壳 DOM 来寻找位置。若当前 slot scope 无法在不搬迁会话状态的前提下完成装配，触发停止条件，不增加全局变量或第二份 store。

原生对话继续由 DSH 服务拥有；页面助手继续由原页面模型与存储路径拥有。复用能力不要求本轮把这两种对话改造成一个通用运行框架。

## 可写范围与只读边界

路径前缀为仓库根目录；新增文件限于相邻目录、承接上述具体职责，不预建新包。

- `desktop/src/verticals/finance/dsh/client.tsx`、`NativeDsh.tsx`：仅本轮 root/助手装配与必要类型适配。
- `desktop/src/verticals/finance/components/layout/Layout.tsx`、`ConversationWorkspace.tsx`：仅承载接线；保留用户拖拽、展开和稳定挂载修复。
- `desktop/src/verticals/finance/components/ui/FinanceAiDock.tsx` 与 `desktop/src/core/ai/AiDock.tsx`：分责及现有消费者兼容；不清理同文件中的旧业务入口。
- `desktop/src/core/ai/pageContext.tsx`、`useAiChat.ts`：优先只读复用；只有本轮抽取所必需的类型/导出调整可写，禁止更换存储键、消息结构或请求归属规则。
- 同目录相邻的有实际消费者的组件/控制 hook；`desktop/test/` 中针对本轮生命周期的定点测试。
- CSS 仅允许因承载位置改变而必需的局部修正；禁止全局主题、字号和视觉重设计。
- 本 Task 交接状态与必要索引更新；不改写 M4/M6 已确认边界。

只读：`router.tsx`、业务页面、`ResearchKnowledge.tsx`、`lib/research.ts`、产品 Server、安装 runtime、Stock-Research/dsh 与 Backend。当前上传/代码归一化修复不属于本轮编辑范围。用户验收后追加授权的例外：`pages/Intel.tsx` 的新闻/公告上下文接入及相邻 `lib/feedPageContext.ts`、对应测试；仅传递当前列表和状态，不扩展正文抓取或模型权限。

## 实施顺序

1. 核对最新基线及所有受影响消费者，先记录现有行为和定点检查结果；无法区分预存问题时暂停该项。
2. 抽取 root 的展示组合，保持实际 slot 声明/消费和 Provider 作用域，验证原生会话没有重建。
3. 抽取页面助手的可复用控制/内容；先保留原承载验证行为，再通过现有 shell.overlay 接入外壳。保证只有一个控制实例。
4. 验证关闭、切页、旧请求晚返回和重新打开；只对本轮回归做修复。核对原生详情、设置与审批弹层仍可操作。
5. 记录引用接缝的源码/类型证据、运行检查与未覆盖项，交用户验收；不自动开始 M6-B。

## 验证与验收

执行环境使用项目要求的 Node（desktop/package.json 当前要求 >=22.18），不为运行检查升级 runtime 或安装新依赖。以下命令从仓库根执行：

```sh
npm --prefix desktop run build
cd desktop && node --test test/page-model.test.ts
git diff --check
```

新增生命周期测试随实际文件运行，优先检验以下行为，不写只断言文件名/源码字符串的测试。已有 page-model 测试使用模拟 Provider，只证明模型接缝契约，不证明真实模型或 UI。

| 验收场景 | 必须保持的结果 |
| --- | --- |
| 原生对话进行中切换页面再返回 | 会话 ID 和消息保持；不重发请求、不重新建立研究任务；拖拽/展开仍可用 |
| 页面助手发送后关闭或 Esc | 按当前行为取消；重新打开无重复发送或幽灵 loading |
| 页面 A 请求后切到 B，A 晚返回 | 不把 A 回答、错误或 loading 写进 B；请求包含发送时的 A 上下文 |
| 切回 A、重新打开助手 | 沿现有存储恢复，不创建第二份会话；页面助手不使用原生 Agent Session |
| 助手、设置、工具详情与审批入口 | 焦点、关闭、层级与点击正常；不被新增遮罩挡住 |
| 宽/窄屏及浅/深色 | 入口、面板、输入框不裁切；主页面与对话滚动保持原有体验 |
| 原模型/研究边界 | 页面助手仍走 `/finance-model`，无研究工具；DSH Agent 原路径保持 |

用户负责手动启动与最终体验验收，沿 M4 使用 `./scripts/setup` / `./scripts/start`；Agent 不自行驻留整套服务。没有真实运行证据的项目标为待验收，不用构建通过代替。至少取得原生对话切页恢复、页面助手关闭/切页隔离的真实行为记录后，才可判定此轮完成。

## 交接记录（实施后填写）

- 基线：`4c16b1c` + 用户工作树，Node v22.22.3，实际 ui-renderer 为 0.1.2-rc.1。用户的 ConversationWorkspace、上传、代码归一化、CSS 改动均保留；未修改这些业务逻辑。
- 变更：新增布局 `FinanceRoot.tsx` 抽取 root 展示；新增 `FinanceAssistantSurface.tsx`，在 `shell.overlay` 注册唯一助手挂载点。`Layout.tsx` 提供 renderPanel，FinanceAiDock 传递给 Core AiDock；Core 保留单一对话实例和关闭取消，移出几何/Portal。`client.tsx` 仅改展示委托和该 slot 注册，启动与原生 Session 逻辑不变。
- 挂载机制：root 的 Context 只保存显式 DOM ref，不保存消息；助手以 Portal 进入该挂载点，保留 Router/AiPage 的 React 上下文。无新增 DOM 查询、全局 store、slot 名称或服务端 API；原生 Portal 路径保留。
- 验证：修改前后 `npm --prefix desktop run build` 均通过；新增 `finance_root.test.ts` 与 `page-model.test.ts`、`chat_storage.test.ts` 共 3 项通过；`git diff --check` 通过。构建有原有 React Router 的 use-client 提示。
- 测试限制：新测试通过 SSR + 模拟 slot 分发验证 Provider 归属、唯一助手 seat、既有浮层和详情分支；不证明浏览器 Portal、实际 Cordis 生命周期或请求竞态。页面模型测试使用模拟 Provider，无真实模型调用。未新增运行依赖；新测试禁用产品 Vite 配置，使用不监听端口的 HTTP 对象，结束关闭 Vite。
- 扩展检查：`chat_controls.test.ts` 通过，但旧测试触发 Vite HMR 端口 EPERM；`ai_surface.test.ts` 1 通过、1 失败，旧断言要求 `data-ai-identity className="mt-2 space-y-1"`，HEAD 与本轮前 Layout 均已无该类名，属于预存断言不一致，未改测试或侧栏掩盖。此组不能报告全绿。
- 差异自审：核对本轮 6 个源码文件及新增测试，未发现静态阻断项；原生上下文顺序、会话/Provider/存储、close 的 abort 保留。无独立审阅结论，浏览器作用域与焦点/层级仍需实测。
- 运行限制：现有 Chrome 产品页 `127.0.0.1:5930/my-reports` 无可读内容，HTTP 探测未连通（状态 000）；未自行启动驻留服务。原生对话恢复、关闭/切页隔离、设置/审批、宽窄屏与主题全部标为待用户运行验收，不宣称本 Task 完成。
- 证据接缝：`ResearchKnowledge.tsx:53` 的 ReferenceButtons 保留真实 refs，并在 :67 调用 `/wiki/refs/resolve`；仍为页内读取。实际 rc.1 `ui-chat/lib/types/client/contract/slots.d.ts` 的 chat.node 为 keyed、turnTail 为 chain、assistant-actions 为 list；正文锚点及修订/Block 恢复链路尚未验证，不因有 slot 就承诺支持。
- 剩余风险：运行服务恢复后优先验收挂载点就绪/卸载、同一助手实例、Router 链接、弹层遮罩及切页竞态；M6-B 尚未开始，M4 业务闭环仍未验收。
- 用户后续验收：切页恢复已由用户确认通过；截图证明助手浮层可打开并完成模型回答。关闭取消、其他弹层/主题等未因此自动判定通过。
- 追加修复：新闻/公告列表现在由数据所在的 WatchlistFeed 单独登记 AiPage，替代父页的栏目空描述；按 kind 设置 React key，防止新闻切公告时短暂复用旧数据。上下文包含本页完整展示列表及加载/刷新/失败状态，明确只有标题与链接，未读正文；其他两个资讯栏目暂保留原描述，未宣称修复。
- 追加验证：build、diff 检查通过；feed_page_context 的 2 项快照/状态测试及 page-model 测试通过（共 3 项）。这些验证序列化与模型接缝，不代替浏览器切栏目或真实回答；待用户刷新后重新提问确认。

## 后续任务边界

- **M6-B 证据锚点**：本轮验收后另行准备 Task，完成真实回答引用 → 对应修订/证据定位 → 历史恢复；不重做整个阅读器。
- **M6-C 一种交互成果**：证据链路后选择 K 线或产业链图，验证生成、保存、对话展示、展开和恢复。类型选择与具体协议届时确定。
- 本轮不编写后续实施契约，也不预先批准跨仓变更；M6 的目标图继续作为方向。

## Out of Scope

- 全面重构、所有页面插件化、Page/Navigation 注册框架、通用 Feature Registry、新 npm 包或插件 Loader。
- 主题状态重写、全站 CSS 收敛、缓存故障区域降级、改变面板关闭后继续生成的语义。
- 新证据锚点/证据面板、HTML 执行、K 线、产业链图、产业信号改版及 Topic/Theme 业务补齐。
- 改 Server/tools/Backend 公开契约、研究权限、Provider 凭据、会话存储及旧六阶段执行路径。
- 升级 DSH、删除原生消息/权限/审批功能、迁移用户数据、提交、打包或发布。

## Stop Conditions

- 必须改动只读 Owner、公开协议、持久化或用户可见行为才能继续时，先列出具体差异，交用户决定。
- 当前 rc.1 slot/Provider scope 无法承载单一助手实例，或需替换原生消息 renderer、改安装 bundle 时，停止该方案。
- 用户并行修复触及同一段逻辑且含义不清，先核对最新 diff，不覆盖、不回退。
- 相同行为两轮修复仍未闭合，保存证据，重审 Owner 和作用域，不继续扩大补丁。
- 无法获得必要运行验证时，停在“实现完成、验收待办”，不得标记 Task 完成。
