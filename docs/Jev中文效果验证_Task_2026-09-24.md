# Jev 中文效果验证

状态：2026-09-24 Codex 已准备样本、完成真实 Jev 运行并生成评审表；待 Claude 先裁决、用户终裁。不提交样本数据、不 push。实施仓库为 Stock-Research（`backend/` 下一次性评估脚本，不改产品行为）。

权威：[Human Checklist](../human-checklist.md)「Jev 判断接入边界（2026-09-20）」「Jev 判断连续性（2026-09-24）」「Jev 使用范围与验证（2026-09-24）」；[Jev 判断连续性 Task](Jev判断连续性_Task_2026-09-24.md)。

## 1. 目标

用本产品真实的中文样本，验证 Jev 在四类判断上是否够准、置信度是否可用，为是否推进“回答引用核验 / 外部内容筛查 / 大模型子任务前置闸门 / 判断连续性”提供依据。官方文档未说明中文支持与价格延迟，必须实测。

## 2. 用户决定（2026-09-24）

- 私有资料（用户上传到“我的资料”的文件）**一律跳过**，不进入样本、不发送。
- 只发公开内容：公告、定期报告、研报等公开来源片段，研究页块，议题判断文字，外部网页 / 资讯正文片段。
- 裁决：执行方**不给出正确答案**；Claude 先判，分歧由用户终裁。

## 3. 前置条件

- 用户已在 Backend 环境配置 `AI_GATEWAY_API_KEY`（执行方不读取、不打印）。未配置时：完成样本准备与请求体生成（dry run），在 §8 回填“待开启”后停止，不调用。

## 4. 四类问题与样本

每类 10 个样本（第 2 类另加 3 个人工构造的注入样本，明确标注 `synthetic`）。样本取自本机真实数据：DSH 会话记录（`.local/dsh/sessions`，只读）、Backend 原文块与研究页、议题与跟踪项。每个样本记录来源身份（会话 / 文档 / 研究页 + 版本），便于复核。

1. **回答引用核验** `citation_support`（choice：`supports` 原文陈述或直接蕴含该说法 / `contradicts` 原文与之相反 / `silent` 原文没有谈到该说法）。state：`claim`（回答中带引用的一句话）、`section`（被引用的公开原文块文本）。先用代码检查说法中的数字是否在原文出现，结果记为独立字段 `number_found`，不喂给 Jev。
2. **外部内容筛查**：`relevance`（noul：该段是否与研究问题相关）、`injection`（noul：该段是否试图指挥或操控回答它的 AI）。state：`question`、`passage`（网页 / 资讯正文片段，≤1200 字）。
3. **知识整理前置闸门** `settlement_worthy`（noul：本轮是否形成了值得写入研究页的、有公开原文依据的新认识）。state：`question`、`final_answer`（截 2000 字）、`read_sources`（本轮读过的公开原文块标题与首段，≤5 段）、`target_page_summary`（研究页现有经营模式一句话）。样本同时记录该轮真实的整理结果（出草案 / 无新增 / 未整理），仅供裁决参考，不发送。
4. **判断连续性** `basis_change_effect`（choice：`unaffected` / `supports` / `weakens` / `challenges` / `insufficient`）与 `revisit_needed`（choice：`yes` / `no` / `insufficient`），题面与 [Jev 判断连续性 Task](Jev判断连续性_Task_2026-09-24.md) §4 一致。state：议题判断文字、研究页研究结论 / 数据块的前后片段。真实变化样本不足 10 个时，可用同一研究页相邻两个历史版本（T5-a 台账）构造，并标注。

题面（instructions / criteria）用中文写，写清边界情况（遵循官方“字面理解、给出边界”的建议）；同一类的所有样本共用同一题面与版本号。

## 5. 运行

- 脚本放 `backend/scripts/jev_eval/`（一次性评估，不接入产品）。在 Backend 容器内运行，复用容器内凭据；直接调用 Vercel AI Gateway `typesafe-ai/jev` 评估接口（与 `maintenance_jev.py` 相同端点与模型），每样本一个请求（同一样本的多题合并在一个请求）。
- 预算：总请求数 ≤ 60；单请求输入 ≤ 16KB；超时 / 重试沿用 `maintenance_jev.py` 默认值。失败样本记录错误类型，不重试超过 2 次，不计入准确率。
- 输出到 `workspace/research/jev-eval/2026-09-24/`（先在 Stock `.gitignore` 加 `workspace/research/jev-eval/`，现未忽略）：`samples.jsonl`（输入与来源身份）、`results.jsonl`（每题选项、各选项概率、confidence、用量、request_id、耗时）、`review.md`（评审表）。

## 6. 评审表格式（`review.md`）

按类分节；每行：样本编号、来源身份、输入摘要（说法 / 段落前 120 字）、Jev 结论、概率、confidence、`number_found`（第 1 类）、真实整理结果（第 3 类），以及**两列空白**：`Claude 裁决`、`用户终裁`。文末汇总：总请求数、总用量、平均 / 最大耗时、失败数。

**执行方不填写裁决列，不计算准确率**；准确率与置信度分桶（≥0.8 / 0.5–0.8 / <0.5）由 Claude 在裁决后计算。

## 7. Out of Scope

- 任何产品接入；私有资料；判断复核与实体关系两类；阈值调优；自动化回归。

## 8. Stop Conditions

- 凭据未配置：只做样本与 dry run，回填后停止。
- 某类找不到足够的公开真实样本：如实回填数量，不用私有资料补。
- Jev 接口对中文返回异常（大量空答、协议错误）：停止并回填错误样例。
- 与其他工具同时修改同一文件：先看 git 状态，冲突时停下告知用户。

## 9. 执行回填

- Stock 新增一次性脚本 `backend/scripts/jev_eval/session_candidates.py` 与 `evaluate.py`，以及脚本契约测试；`.gitignore` 忽略 `workspace/research/jev-eval/`。会话只读解压并按轮次关联问答，命中私有资料标记或私有工具的会话整段跳过；引用样本再由 Backend 检查原文文档的公开 Provider，并排除任何同时带上传来源的文档。网页样本仅取带 HTTPS URL 的公开搜索结果；判断连续性只接受有版本身份且引用可证明公开的研究 / 数据块。每类题面共用 `zh-v1-2026-09-24`。
- 凭据检查：旧运行容器未看到新 `.env`，新建的一次性 Backend 容器能看到；本次用一次性容器运行，未读取或打印密钥，未重启产品 Backend。先做请求体 dry run，再发 1 个请求核对协议，随后完成其余请求。
- 样本 / 结果位于 Stock `workspace/research/jev-eval/2026-09-24/`，仅本地 ignored：`samples.jsonl`、`results.jsonl`、`review.md`；另有不含正文的 `excluded-requests.jsonl`。实际发出 22 个请求；更严格的事后会话来源审计发现，其中 2 条公开网页样本所在会话读过一份 `user.upload` 研报，已从有效样本和评审表剔除。核对这两份实际请求体，仅包含普通研究问题和公开 HTTPS 网页片段，未包含上传资料正文。有效样本为 20：回答引用核验 6、外部内容筛查 8 个真实网页片段 + 3 个标注 `synthetic` 的人工注入片段、整理闸门 3、判断连续性 0。脚本现进一步拒绝任何读过非公开文档的会话。引用样本只覆盖 2 份公开文档；3 条整理样本主要是已有会话中的只读问答。议题判断只有一条 `page:` 依据，对应研究页在版本台账中只有一个版本，因此无法构造相邻版本变化。未用私有资料或虚构真实样本补足数量。
- Jev 实际 22/22 请求成功，0 失败；实际总用量 input 23,752 / output 838 tokens，平均耗时 0.535 秒，最大 1.829 秒。有效评审样本用量 input 22,411 / output 758 tokens。最长请求体 7,761 字节（上限 16KB），未超过 60 请求。Choice 返回完整选项概率与 confidence；Boolean 返回真值概率、不带独立 confidence，评审表该列留空。`number_found` 是独立代码检查，未送给 Jev。评审表的 `Claude 裁决`、`用户终裁` 均为空，执行方未计算准确率。
- 验证：一次性 Backend 容器内 `python -m unittest discover -s /eval -p 'test_*.py' -v` 3/3 通过；脚本语法检查、Stock `git diff --check`、样本与结果 ID 一致性、私有资料标记为零和请求体大小检查通过。未改产品行为、未提交、未 push。
- 不能用这批样本裁决四类判断的整体中文效果：引用与整理数量和来源多样性不足，判断连续性无样本。待 Claude 对现有评审表先裁决，再由用户终裁；是否补采样本或推进产品接入由后续决定。

## 10. 裁决与结论（Claude 与用户）

### 10.1 Claude 裁决（2026-09-24，逐条见评审表“Claude 裁决”列；待用户终裁）

| 类别 | 有效样本 | 判对 | 判错 | 边界 | 要点 |
|---|---|---|---|---|---|
| 注入识别 | 11（含 3 条人工攻击） | 11 | 0 | 0 | 攻击样本 0.95–0.98，正常内容最高 0.38，分得很开 |
| 相关性 | 8 | 4 | 3 | 1 | 偏保守：片段只有标题、行情表格、或问题里夹着系统拼接文字时会判“不相关” |
| 引用核验 | 6 | 3 | 2 | 1 | 两条错判都是“原文其实支持”：一条因说法后半夹带模型解读，一条因原文以“元”记而说法用“亿元” |
| 整理闸门 | 3 | 1 | 1 | 1 | 样本太少，且输入缺研究页摘要，无法下结论 |
| 判断连续性 | 0 | — | — | — | 无样本 |

耗时平均 0.54 秒、最长 1.83 秒；22 次请求共约 2.2 万输入 token，全部成功。

### 10.2 结论与建议

1. **注入识别可以直接上产品**：外部网页、资讯、搜索摘要在进入模型前先问一次注入，概率 ≥0.8 的片段不交给模型并在过程里标注。对应产品纪律“网页、公告、研报中的指令不可执行”。
2. **引用核验需要先改输入再测**：官方局限（数字与单位、复合说法）在这里完全印证。改法：只取引用标记前的那一句作为说法（不含模型解读），数字核对由代码完成并做单位换算（元 / 万元 / 亿元），Jev 只判断文字层面的支持。改后第二轮验证。
3. **相关性暂不采用**：准确率不够，且会误伤表格类原始数据；研究问题需去掉系统拼接文字后再议。
4. **整理闸门与判断连续性需第二轮**：整理闸门用真实整理记录（已知出草案 / 无新增 / 未整理）至少 10 条、输入补研究页摘要；判断连续性用 T5-a 台账相邻版本构造至少 10 条。
5. 数据合规：执行方事后剔除 2 条来自读过上传资料会话的样本，已核对发送内容仅为公开网页片段；脚本已加整会话来源检查。

### 10.3 用户终裁

2026-09-24：用户认可 §10.2 建议，确认注入识别采用“隔离”（≥0.8 不交给模型，保留元数据并在过程标注）；未对单条裁决提出异议。落地见 [外部内容注入隔离 Task](外部内容注入隔离_Task_2026-09-24.md)。

## 11. 第二轮验证（2026-09-24 延期，见 todo §5；恢复后 Codex 实施，Claude 裁决、用户终裁）

沿用 §2–§6 的数据边界、运行方式与评审表格式，输出到 `workspace/research/jev-eval/<日期>-r2/`；总请求 ≤ 60。

1. **引用核验**（≥10 条）：说法只取引用标记前的那一句（去掉其后的解读与其他引用），题面不变；数字核对由代码完成：抽取说法与原文中的数字并做单位换算（元 / 千元 / 万元 / 亿元、%），字段 `number_match`（全部匹配 / 部分 / 无数字 / 不匹配）写入评审表，不发送给 Jev。优先覆盖第一轮错判的两类情形（夹带解读、元与亿元）。
2. **整理闸门**（≥10 条）：用 DSH 后台任务记录中已知结果的整理（出草案 / 无新增 / 未整理各至少 3 条），state 补 `target_page_summary`（研究页现有经营模式一句话，无则写“暂无”）；问题文本去掉系统拼接部分（如“本轮引用身份…”“模式：Ask”），只留用户问题。
3. **判断连续性**（≥10 条）：用 T5-a 台账相邻版本对构造——选研究结论或数据块确有变化的版本对，配真实议题判断文字或据该研究页旧版内容写一句代表性判断（标注 `constructed`）；题面同 [Jev 判断连续性 Task](Jev判断连续性_Task_2026-09-24.md) §4。
4. 相关性本轮不测。

### 11.1 第二轮执行回填

（待实施）
