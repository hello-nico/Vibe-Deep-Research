# 图文报告生成后质检（渲染检查 + 退回重做）

状态：2026-09-26 Claude 写定，同日用户决定**不再等演示、立即派发**。§2 第 1 条（引入无头浏览器）已由用户 2026-09-26 确认。待 Codex 实施并回填 §8。执行方只跑自动检查，不重建、不重启、不提交。实施仓库：Stock-Research（`backend/`、`dsh/`）；Vibe `desktop/`（仅“排版待核”提示）。

权威：用户 2026-09-26 要求“生成后再单独检查是否存在不合理的地方”；AGENTS.md「研究纪律」；已授权的 Lieflat 设计语言（`dsh/resources/lieflat/SKILL.md`）。

## 1. 问题

- 资料时间线等图由报告 Agent 按图库样式自己写 SVG，标签坐标也是它算的。时间点靠近时文字互相压住（用户截图：2026-03 到 2026-09 之间 3 个点的标题和日期重叠）。
- `SKILL.md` §八“交付前自检清单”第 3 条已要求检查“标签会不会重叠”。但 Agent 看不到渲染结果，这条自检等于没做。
- Backend 保存报告（`backend/app/services/wiki_reports.py` `validate_report_html`）只查非空、大小、引用格式，不看渲染效果和内容是否合理。
- 同日农业银行报告在研究页数据被清空后照常生成，得到“一个读数，八项空白”。数据源问题已另修，但报告层面也没有任何拦截。

## 2. 冻结决定

1. **渲染检查（2026-09-26 用户确认）**：
   - 在 Stock Backend 引入 Playwright + Chromium（约 150MB，仅本机依赖），保存报告前用无头浏览器按 1200px 与 800px 两个宽度渲染 HTML。
   - 不等待入场动画：渲染时模拟“减少动态效果”（`prefers-reduced-motion: reduce`），并注入样式把 `animation`、`transition` 时长设为 0，元素直接落在最终位置；布局稳定后，执行下列检查。关闭动画**只作用于检查用的临时渲染页**：保存的报告 HTML 原样保留，用户查看时的入场动画与特效不受影响（2026-09-26 用户明确要求保留动画）。测试须断言：检查前后报告 HTML 的 sha256 不变。
   - 渲染在隔离页面里进行：禁止网络，只加载报告自身内容。
2. **检查项**（均为确定性规则，不调用模型）：
   - **文字重叠**：可见文字元素（SVG `text`、带文字的 HTML 元素）两两的边界框，交叠面积超过较小者的 30% 即违规。
   - **溢出与截断**：文字超出所在 SVG 的 `viewBox` 或父容器的可见区域。
   - **空图**：图表容器（`.fig` 或 `svg`）里没有画出任何数据元素。
   - **脚本错误**：渲染期间控制台出现报错。
   - **内容空洞**：带引用的数据元素少于 3 个，且正文“待补充”超过 5 处。
3. **退回重做**：
   - 首次保存有违规时，返回 422，`code = report_quality_failed`。明细逐条写出：检查项、元素文字前 40 字、宽度、坐标。
   - `dsh/src/report-tools.mjs` 把明细交回报告 Agent，提示“只修这些位置，不改其余内容”，最多重试 1 次。
   - 第二次仍违规：允许保存，报告记录 `quality_warnings`；前端在报告顶部显示灰色提示“这份报告有 N 处排版待核，可重新生成”。
   - “内容空洞”不走重试，直接拒绝生成，提示“研究页数据不足，先刷新资料或发起公司研究”。
4. **技能说明补规则**（`dsh/resources/lieflat/SKILL.md` §二或 §八）：
   - 时间轴、散点等带标签的图，相邻标签水平间距小于 90px 时，上下交替排布；仍放不下时，把 45 天内的点合并成一个点，标签写“N 份”。
   - 这条是减少退回次数的预防措施，不替代第 1、2 条的检查。
5. 旧报告不回溯检查。深度对话里的图表不在本 Task 内。

> 2026-09-26 用户确认采用第 1 条。依赖安装：在 `backend/` 执行 `uv add playwright`，再执行 `uv run playwright install chromium`（Chromium 下载到本机缓存 `~/Library/Caches/ms-playwright`，不进仓库）。备选的“仅前端展示时检查”不采用。

6. **校准后再接入（2026-09-26 修订，窗口 1 首轮回放触发 §5 后 Claude 裁决）**：
   - 首轮回放 38 份报告全部检出违规：脚本错误 76、文字截断 261、文字重叠 69、空图 46、内容空洞 16。其中脚本错误 76 条全部是 CSP 元标签的浏览器诊断（每份报告 × 2 个宽度），其余几类的量级也明显偏大，规则需要先校准。
   - **“脚本错误”的定义改为只统计未捕获的页面异常（`pageerror`）**，控制台信息（含 CSP 诊断）不计。这是纠正定义，不是放宽阈值。
   - **校准轮**：接入发布流程前，对文字截断、文字重叠、空图三类检出，各随机抽 10 条（跨报告），导出标注截图（在 1200px 渲染图上用红框标出违规元素，若是重叠则同时框出另一方，裁切到违规区域附近），存到 `workspace/tmp/report-quality-calibration/<检查项>/`，并附一份索引（报告 ID、检查项、文字、坐标、截图路径）。
   - Claude 逐张判定真假并回填准确率。执行方按判定调整规则（例如：只取元素自身文字节点、跳过 `opacity` 为 0 或不可见的元素、跳过 `title`/tooltip 文字、按真实裁切容器判断截断），再重跑抽样。**某一类抽样准确率达到 90% 才可拦截**；达不到的只记入 `quality_warnings` 作为提示，不退回、不拦截。
   - 内容空洞首轮已正确检出农业银行“一个读数，八项空白”，可直接进入拦截。
   - 校准完成、Claude 确认后，再做第 3 条接入发布流程、DSH 重试、前端提示与第 4 条技能规则。

## 3. 范围

- Stock backend：`app/services/wiki_reports.py`（调用检查）、新增 `app/services/report_quality.py`（渲染与规则）、`requirements.txt`（依赖唯一入口）、`Dockerfile`（安装 Chromium）、对应 pytest（用固定 HTML 夹具覆盖每个检查项）。
- Stock dsh：`src/report-tools.mjs`（收到 `report_quality_failed` 时退回重试）、`resources/lieflat/SKILL.md`（第 4 条）、测试。
- Vibe：`components/WikiReportPane.tsx`（显示 `quality_warnings`）与测试。

## 4. 验收

- **自动**：
  - Backend pytest；Stock DSH `pnpm test`；Vibe `npm test`、typecheck、`node dsh/build-ui.mjs`；`git diff --check`。
  - 夹具测试：
    - 重叠标签被拦下；
    - 间距足够的同一张图能通过；
    - 空 SVG 被拦下；
    - 脚本报错被拦下；
    - 第二次违规时保存并带 `quality_warnings`。
- **回放**：对 `workspace/wiki-reports/` 下现有报告（只读）逐份跑检查，列出每份的违规数。用户截图里那份时间线报告必须检出重叠；抽 3 份用户认为正常的报告，不得误报。结果写 §8。
- **耗时**：单份检查不超过 3 秒（两个宽度合计，含浏览器启动），写入 §8。报告生成总耗时对比改动前后各测 2 份，写入 §8。
- **用户验收**：重新生成一份此前时间线重叠的报告，时间线标签不再重叠；如出现“排版待核”，提示可见。

## 5. Stop Conditions

- 回放中误报超过 1 份：停止，回填误报样例与规则阈值，不自行放宽。（2026-09-26 修订：首轮已按第 6 条进入校准轮；校准轮内以“抽样准确率”为准，不再按份数停止。）
- Chromium 在本机无法无网络启动，或单份检查超过 10 秒：停止，回填。

## 6. Out of Scope

- 用模型做语义审稿（另见 [报告引用语义核对 Task](报告引用语义核对_Jev_Task_2026-09-26.md)）；
- 引用数字核对（另见 [报告引用数字核对 Task](报告引用数字核对_Task_2026-09-26.md)，两者可共用“退回重做”通道，先落地者定接口）；
- 深度对话图表；旧报告回溯修复。

## 7. 回滚

还原代码与依赖。`quality_warnings` 为新增字段，旧报告无影响。

## 8. 执行回填

**2026-09-26 当前状态：第二轮经 Claude 确认，发布接入及自动验收已完成。历史停止均已裁决；真实 Agent 生成总耗时按用户确认留到部署后，本轮以固定 HTML 保存回放代替。**

- 本轮改动：Stock `backend/app/services/report_quality.py`、`backend/tests/test_report_quality.py`；新增 `backend/scripts/jev_eval/calibrate_report_quality.py`。Vibe 仅更新本 §8；未改保存接口、DSH、前端质量提示、Lieflat 或已有报告。
- 自查修正：脚本错误仅记录 `pageerror`，移除 console.error 监听；SVG/HTML 均按元素自身文本节点的 Range 取框，避免父元素合并子元素文字；跳过不可见、opacity=0 的祖先及 title/desc/tooltip；裁切仅比较实际具有裁切行为的 HTML/SVG 容器，HTML 取 padding 边界，不把 SVG text 当成 CSS 裁切盒。30% 重叠阈值、空图判定未放宽；其余误报留给校准判断。
- 自动检查：`cd backend && uv run pytest tests/test_report_quality.py` **10 通过/0 失败，6.17 秒**；包含控制台诊断不计 pageerror、隐藏/tooltip 排除、网络阻断、动画及 sha256 不变。两个仓库 `git diff --check` 通过。未重复全套自动验收，本轮只是校准交付，不称发布验收通过。
- 新规则只读回放 38 份：脚本错误 **0**、文字截断 **261**、文字重叠 **69**、空图 **46**、内容空洞 **16**（均为两个宽度的原始明细数，尚非真实缺陷数）。总检查 **21.901406 秒**，单份 **0.519541–0.643605 秒**（1200/800 两宽合计含浏览器启动）。全部源文件 sha256 在回放、截图后复核不变，Chromium 无网络运行成功，未触发 10 秒停止条件。
- 随机抽样：固定种子 **20260926**，仅抽 1200px 检出；随机报告顺序，每轮每报告抽一条，再从各报告随机候选取样。三类各 **10 条**，文字截断覆盖 **7 份**报告，文字重叠/空图各覆盖 **10 份**报告。导出 **30 张**局部 PNG，红框标注检出元素，重叠同时标出另一方；索引保留原页面坐标、另一方文字/坐标、裁切区域与绝对截图路径，判定字段为空。抽样不按视觉结果替换。
- 索引：`Stock-Research/workspace/tmp/report-quality-calibration/index.md`，机器可读 `index.json`；逐份完整回放 `replay.jsonl`；截图分别位于 `text_clipped/`、`text_overlap/`、`empty_chart/`。首次导出因截图区域在首屏之外报错，改为 full_page 配合 clip 后按同一随机种子成功导出；没有改变检测规则。抽查三类图像确认红框与局部截图已生成，不代替 Claude 真伪判定。
- 当前边界：遵循修订 §2 第 6 条，准确率待 Claude 回填，未自行赋真假标签或决定拦截级别。截图交付后停止；不重启、不提交、不 push。下列为首轮历史证据，其停止已由本次修订裁决，不再作为校准轮停止依据。

2026-09-26 窗口 1：用户确认“允许以 Task 1 相关检查通过作为前置”后开始。**本 Task 未完成：回放误报超过 1 份，触发 §5，立即停止后续实施。** 未放宽阈值或增加过滤特例，未接入现用报告发布流程。

- 改动文件（Stock）：新增 `backend/app/services/report_quality.py`、`backend/tests/test_report_quality.py`、`backend/pyproject.toml`、`backend/uv.lock`；Vibe 仅回填本 §8。报告保存、DSH、Lieflat 技能、前端质量提示均未接入；保留 Task 1 已定义的 `details` / `retry_token` / 单次重试接口，没有另建重试通道。
- 依赖：按要求在 backend 执行 `uv add playwright`、`uv run playwright install chromium`，成功安装 Playwright 1.63.0 / Chromium 1243（本机缓存，不进仓库）。原 backend 无 pyproject，按现有 requirements 建立项目依赖并生成锁文件；uv 解析时更新了部分现有环境包，停止后未重新执行全量验收，兼容性尚未闭合。
- 临时渲染：两个宽度 1200/800，禁止网络，减少动态效果并仅向检查页注入零动画时长样式。固定 HTML 夹具覆盖重叠、足够间距、空图、脚本异常、SVG/HTML 裁切、内容空洞、网络隔离及动画；检查前后 sha256 断言不变。首次夹具测试 7 通过/1 失败，修正自身容器裁切检查后 **8 通过/0 失败，5.01 秒**。日志 `/tmp/window1-task2-quality-final.log`。这只证明夹具行为，不能证明真实报告规则准确。
- 误报证据：华能国际报告 `3c17290cf396fcce153e96e2e9554fa9`、`46a232d925b8d246c1c8b9293268e7f3`（还有 `6e4b7929cbeb38cb2e92b75e8615b74c`）各只有两个宽度的 `script_error`。明细文字为 CSP 元标签浏览器诊断（`The Content Security Policy directive 'f…` / `The Content Security Policy 'default-src…`），被归类为报告脚本错误；并非报告脚本异常。至少两份确定误报，触发停止。当前规则仍是重叠面积大于较小者 30%、任何 console.error/pageerror 计脚本错误；未调整阈值。
- 回放只读 38 份现有报告，合计 **23.005182 秒**；单份两个宽度含浏览器启动 **0.536484–0.747521 秒**，全部 HTML 文件检查前后 sha256 不变。Chromium 离线启动成功、临时浏览器已关闭，未触发启动/10 秒停止条件。批量回放完成后核对明细发现误报，停止后未再跑质检或修改旧报告。日志 `/tmp/window1-task2-replay.jsonl`、`/tmp/window1-task2-replay.log`。
- 下表违规数是两个宽度的原始明细总数，含已证实的 CSP 误报，不等于真实缺陷数。农业银行“一个读数，八项空白”检出 `content_empty` 两条；截图报告身份和 3 份用户指定正常报告尚未确认，因此不声称截图验收/正常报告对照通过。

下表保留首轮历史代表样例；完整 38 份逐项数据见 `/tmp/window1-task2-replay.jsonl`，当前结果以 `report-quality-final/replay.jsonl` 为准。

| 报告 ID（省略 report:） | 标题 | 违规数 | 秒 |
|---|---|---:|---:|
| 77fe835dc0d4bb527320dff5a381e3d3 | 云铝股份·报告期证据与缺口 | 30 | 0.664 |
| 3c17290cf396fcce153e96e2e9554fa9 | 华能国际·快照读数、口径与缺口一览 | 2 | 0.579 |
| 46a232d925b8d246c1c8b9293268e7f3 | 华能国际·快照读数、口径与缺口一览 | 2 | 0.594 |
| 6e4b7929cbeb38cb2e92b75e8615b74c | 华能国际·快照读数、口径与缺口一览 | 2 | 0.594 |
| 18d46e2acca659ada09799d1a819b7be | 中国巨石·玻璃玻纤一页快照 | 13 | 0.612 |
| 65844fb147f80f0f9ec054ec3a641f7a | 万华化学·化学制品行业读数一页纸 | 6 | 0.626 |
| b272fc5495876eddcb5d56ffa35cf447 | 万华化学·公司快照报告 | 30 | 0.730 |
| e4a66efc5025c9988c14d27a9a4e60d7 | 海螺水泥·报告期证据一页 | 6 | 0.562 |
| 2607b370879c07263080ebed39a0fd2e | 国电电力：半年期快照、证据与缺口 | 6 | 0.560 |
| 19ebd44efae97e73e4d635a2ea8e0ab7 | 华能蒙电·当期快照与证据缺口 | 13 | 0.592 |
| 62a2c3b53559bed0a1782a87428809a3 | 华能蒙电·电力·一页纸研究报告 | 2 | 0.682 |
| 076ae4ca3a1bfcc2a2aceeb63e3109a4 | 长江电力：有依据的读数、报告期口径与缺口 | 26 | 0.556 |
| 7d8c78243c10685e4781bf4834dff622 | 长江电力：两个报告期的读数、依据与缺口 | 8 | 0.584 |
| 03450c8022e811ac7995d3b83aafc5b8 | 中国移动·快照关键读数与待补缺口 | 2 | 0.599 |
| 273f6c972ac133fed9e8de8a3f27740e | 农业银行·公司研究快照 | 12 | 0.613 |
| 6cd008425e3e4b3f93abf54cae0e52f0 | 农业银行·规模读数、报告期口径与缺口 | 12 | 0.579 |
| ad67a69b80fa93be0c07338ab6bd7a0c | 农业银行：一个读数，八项空白 | 8 | 0.557 |
| ed3008c835604603a47b26f769fbdbed | 紫金矿业·工业金属：快照读数、证据表与缺口 | 4 | 0.608 |
| f8904dea2519911ba37df86579ba4143 | 紫金矿业·工业金属：快照读数、证据表与缺口 | 4 | 0.590 |
| fade391d0a749318f4f7cb1c7e6d1a05 | 紫金矿业·有据快照与缺口 | 47 | 0.608 |
| b5ea21ccbb70addecc171f887ebef785 | 华友钴业·关键读数与证据 | 10 | 0.586 |
| d6796ce5f1a0e82f2ce5222e5e4f24e0 | 有色金属矿采选业·行业身份一页 | 4 | 0.563 |
| 30d2a0d05f8796a5224fc7e61c360d9f | 煤炭开采和洗选业·行业底稿报告 | 2 | 0.536 |
| 437aef92962d5c7a6d9046593f1312b6 | 煤炭开采和洗选业·行业研究一页 | 6 | 0.559 |
| e6529ffb35f32f313bdf8eac2d22d795 | 煤炭开采和洗选业·行业研究页 | 6 | 0.568 |
| 849185989942f39f5134803d28fab4ef | 金属制品、机械和设备修理业·交互报告 | 6 | 0.571 |
| f7f641caaa00cf1a3a95f05edfd29428 | 非金属矿采选业·分类身份与经营机制底稿 | 26 | 0.563 |
| c33f52d7d7d2c84baa754e30d55510ec | 黑色金属矿采选业·身份、机制与资料缺口 | 6 | 0.651 |
| 1d118270c767c7c0a8169abb16a21a05 | 电力行业公司结构：水电主体的控股与配售电平台结构 | 6 | 0.748 |
| de5a6cb41fe6da7812dbf2952bde4c6b | 电力行业公司结构：水电主体的控股与配售电平台结构 | 2 | 0.622 |
| dba6ff9920a3b3dafc2e0e1cb5759d57 | 黄金和美债·研究状态与缺口 | 4 | 0.615 |
| 6a66783e7543451edff8c698d4354b96 | 电力行业资本开支与分红的相容性 | 6 | 0.617 |

- 停止后未执行：第二次违规保存与 `quality_warnings`、DSH/前端接入、Lieflat 规则、报告生成前后各 2 份总耗时及用户验收；不提供这些项目的通过结论。未继续全套测试；两个仓库 `git diff --check` 通过。保持不重启、不提交、不 push、不读打印密钥、不修改已有报告。恢复实施前需处理已触发的停止条件，不能按当前规则直接上线。

**2026-09-26 Claude 校准判定（第 1 轮，30 张，逐张判定已写入 `workspace/tmp/report-quality-calibration/index.md`）**

| 检查项 | 真问题 / 抽样 | 准确率 | 误报原因 |
|---|---:|---:|---|
| 文字重叠 | 6 / 10 | 60% | 固定悬浮条（fixed/sticky）、交互弹层（依据引用卡片）、极小标记点、用了被裁掉部分的边界框 |
| 文字截断 | 8 / 10 | 80% | 只超出一两个像素，肉眼完整可见 |
| 空图 | 0 / 10 | 0% | **检查器缺陷**：抽到的报告全部用 `IntersectionObserver` 在滚动到可见时才绘图，检查器没有滚动，图一直没画出来 |

规则调整（执行方按此修改后重跑）：
1. **先让图画出来再检查（最重要）**：加载后把视口高度设为整页高度，或分段滚动全页，让所有 `IntersectionObserver` 触发，等绘制稳定（最长 1 秒）后再测量。空图、重叠、截断三类都要在这之后测，否则没画出来的图里的问题也会漏检。
2. **重叠**：跳过 `position: fixed/sticky` 的元素及其子孙；跳过交互弹层（`role` 为 dialog/tooltip，或带 popover/tooltip 类、初始应隐藏的浮层）；跳过宽或高小于 12px 的元素；两方都改用“被祖先裁切后的可见矩形”求交。
3. **截断**：被裁掉的部分至少 4px，或至少 0.4 个字宽，才算截断。
4. 改完后换一个随机种子，三类各重新抽 10 条导出截图，再由 Claude 判定。某类准确率达到 90% 才可拦截；未达到的只记入 `quality_warnings`。

**2026-09-26 窗口 1：第二轮修改与超时停止回填**

- 修改 Stock `backend/app/services/report_quality.py`：加载后将检查页视口高度扩为整页，触发 IntersectionObserver，按 DOM 变动等待稳定（80ms 静默，最长 1 秒）后测量；重叠排除 fixed/sticky 及子孙、dialog/tooltip/popover、可见宽高不足 12px 的文字；交叠用经过祖先真实裁切的可见矩形；截断仅在裁掉至少 4px 或 0.4 字宽时检出。SVG overflow:visible 不再按 viewBox 虚构裁切。保留 30% 重叠阈值。
- 修改 `backend/tests/test_report_quality.py`：新增懒绘图、浮层/小标记排除、裁切后求交、轻微/明显裁切测试。`cd backend && uv run pytest tests/test_report_quality.py` **15 通过/0 失败，11.15 秒**。更新 `backend/scripts/jev_eval/calibrate_report_quality.py`，支持独立输出目录；候选不足 10 条时如实记录，不重复或造样本。种子改为 **2026092602**，目录 `workspace/tmp/report-quality-calibration-r2/`，首轮索引判定未覆盖。
- 本轮只完成前 **21/38** 份回放：文字截断 **180**、文字重叠 **58**，空图/内容空洞/脚本错误各 **0**（两宽原始明细数，不等于真实问题）。单份 **0.714–1.155 秒**，均低于 3 秒；这是终端输出三位小数，不能提供完整 38 份总数或总耗时。
- 第 22 份 `report:ad67a69b80fa93be0c07338ab6bd7a0c`（农业银行“一个读数，八项空白”）触发懒绘图后卡死，单份超过 10 秒，**触发 §5，停止**。原 HTML `draws.wabTimeline` 第 186–191 行循环要求 `y===2026 && m===12` 才退出，但 `m` 到 12 就重置为 0，因此无法终止；浏览器/驱动约 1 分 6 秒持续高 CPU。已仅终止本轮临时进程（回放退出码 143），未修改原报告、未跳过它继续。原检查器超时判定在返回后，不能打断此类死循环；硬超时隔离及该报告处理须先裁决，不自行绕过。
- 第二轮截图及 `index.md` **未导出**，准确率未评估。停止证据：`Stock-Research/workspace/tmp/report-quality-calibration-r2/STOP.md`。停止后只回填并执行两个仓库 `git diff --check`，不继续质检；没有接入发布，不重启、不提交、不 push、不修改已有报告。

**2026-09-26 Claude 裁决（第 2 轮停止）**：窗口 1 这次停下是因为撞到了**报告本身的真缺陷**：农业银行报告 `ad67a69b80fa93be0c07338ab6bd7a0c` 的时间线脚本有死循环，用户在产品里滚到那里也会卡死页面。因此：
1. **新增检查项 `script_hang`（直接拦截，不需校准）**：每个宽度的渲染和测量设硬超时 5 秒，超时就立即关闭该页面、终止脚本，记为 `script_hang`，明细写“报告脚本在 N 秒内没有结束，可能有死循环”，然后继续检查下一份。卡死的报告不许保存，退回 Agent 修改，按第 3 条重试一次。
2. 单份检查耗时上限仍为 3 秒；超时属于异常分支，不计入这个上限。
3. 回放遇到 `script_hang` 时照常记录并继续，不再作为停止条件；只有检查器自身崩溃，或连续 3 份超时，才停下。
4. 继续第 2 轮校准：跑完 38 份，文字截断、文字重叠各抽 10 条导出到 `report-quality-calibration-r2/`（空图第 2 轮已降为 0，不再抽样），然后停下等 Claude 判定。

**2026-09-26 窗口 1：第二轮恢复与交付**

- 按上述裁决，在 `report_quality.py` 将每个宽度的浏览器渲染放入独立进程组，父进程 5 秒硬超时后终止该组并记录 `script_hang`，继续下一宽度/报告；检查器异常仍报错。修正原 2.5 秒 set_content 超时与新 5 秒硬超时的冲突。`calibrate_report_quality.py` 支持选检查项、逐份保存回放、连续三份超时停止。对应死循环及下一报告可检查的夹具已新增；首次验证 **15 通过/1 失败**，修正超时冲突后 **16 通过/0 失败，33.59 秒**。
- 38 份只读回放完整完成：文字截断 **276**、文字重叠 **60**、空图 **0**、脚本错误 **0**、内容空洞 **14**、`script_hang` **2**（同一农业银行报告两个宽度；原始检出明细，不等于已确认缺陷数）。总耗时 **66.002322 秒**；37 份非超时报告两宽合计含启动 **1.383564–1.768086 秒**，均小于 3 秒。农业银行异常分支 **10.025597 秒**（两个宽度各 5 秒硬超时），按裁决不计正常 3 秒上限，不再作为停止条件。没有连续三份超时或检查器崩溃。
- 随机种子 **2026092602**；仅对 1200px 检出抽样，截断 **10 条/10 份报告**、重叠 **10 条/8 份报告**；共 **20 张**红框局部截图，重叠同时框出两方，均复核重新渲染能复现选中明细。空图按裁决不抽样；真假与准确率仍待 Claude 判定。38 份源报告 SHA256 全部不变，临时 Chromium/Playwright 进程已退出。两个仓库 `git diff --check` 通过。
- 交付：`Stock-Research/workspace/tmp/report-quality-calibration-r2/index.md`（待判定列）、`index.json`（坐标、另一方、裁切框、绝对截图路径）、`replay.jsonl`（38 份逐项明细与耗时），以及 `text_clipped/`、`text_overlap/` 下截图。同目录 `STOP.md` 保留恢复前的历史证据，以本次完整回放为当前结果，首轮 Claude 判定未覆盖。
- 已按要求停止在第二轮截图交付，不接入报告保存、DSH 重试或前端提示，不重启、不提交、不 push、不改已有报告。用户新增的 Jev 语义核对 Task 已读取并在 §9 记录排期：须质检经 Claude 确认接入并完成后，由当前窗口继续，尚未实施。

**2026-09-26 Claude 校准判定（第 2 轮，20 张，逐张写入 `report-quality-calibration-r2/index.md`）**

| 检查项 | 真问题 / 抽样 | 准确率 | 处理 |
|---|---:|---:|---|
| 文字重叠 | 10 / 10 | 100% | **拦截**：退回 Agent 修改一次 |
| 文字截断 | 8 / 10 | 80% | 未达 90%，**只记入 `quality_warnings` 作提示**，不退回、不拦截 |
| 空图 | 第 2 轮 0 条 | — | 保留为提示项 |
| 脚本卡死 `script_hang` | 农业银行死循环已检出 | — | **拦截** |
| 内容空洞 | 第 1 轮已验证 | — | **拒绝生成**，提示先刷新资料或发起公司研究 |

截断的两条误报：一条只超出少许、肉眼完整；另一条整段不可见，疑为有意隐藏的元素。补一条规则：**元素在裁切前就完全不可见（可见面积为 0）时，不计截断**。截断仍只作提示。

裁决：校准结束，按上表接入发布流程（§2 第 3 条的退回重做与 `quality_warnings`、DSH 重试、前端“排版待核”提示、第 4 条技能规则），完成后接 [报告引用语义核对（Jev）Task](报告引用语义核对_Jev_Task_2026-09-26.md)。

**2026-09-26 窗口 1：发布接入完成**

- 修改 Stock `backend/app/services/report_quality.py`、`wiki_reports.py`、`backend/tests/test_report_quality.py`、`dsh/src/report-tools.mjs`、`dsh/test/report-validation.test.mjs`、`dsh/resources/lieflat/SKILL.md`；沿用此前新增的 pyproject/uv.lock 与校准脚本。Vibe 修改 `desktop/src/verticals/finance/components/WikiReportPane.tsx`、`desktop/test/wiki_report_lifecycle.test.ts` 及本 Task。
- 重叠/脚本卡死与数字核对共用绑定页面、版本、会话的单次 retry_token；重试后仍有问题保存并记 warnings。空洞直接拒绝，截断/空图直接保存提示；完全不可见文字不计截断。前端显示指定灰色提示；技能增加 90px 交替/45 天合并规则。保留首次打开不自动生成及保存 HTML 动画。
- 自动检查：Backend 相关 **55 通过/0 失败（57.53 秒）**；全量 **925 通过/6 失败/7 跳过（92.32 秒）**，失败为五项已有年度报告原件夹具及一项研究链接 GBRAIN 配置，不在本 Task 修改范围。DSH **189/0**，Vibe **292/0**；typecheck、UI build、两个仓库 diff --check 均通过。固定 HTML/Jev 未参与，浏览器检查使用真实离线 Chromium；动画 SHA 不变、懒绘图、硬超时、共享重试均有夹具覆盖。
- 最终只读 38 份回放：截断 **169**、重叠 **60**、卡死 **2**、空洞 **14**，空图/脚本异常 **0**；完全不可见排除使截断由 276 降至 169。逐份明细及耗时见 `Stock-Research/workspace/tmp/report-quality-final/replay.jsonl`，汇总 `summary.json`。37 份正常检查 **1.276461–1.461412 秒**，总计 **60.460731 秒**；卡死仅同一农业银行报告两宽异常分支。全部原件 SHA 不变；没有检查器崩溃或连续三份卡死，没有新停止条件。
- 用户确认接受固定 HTML 完整保存前后各 2 份比较、真实 Agent 总耗时留部署后：前 **0.030144 / 0.000647 秒**（不运行质检），后 **1.347491 / 1.311648 秒**（真实质检），临时目录隔离保存；`report-quality-final/save-benchmark.json`。首次计时夹具 slug 不合规，修正夹具后重跑；未修改生产规则。此比较不包含模型生成，不能替代真实生成耗时或用户视觉验收。无需重启即完成源码与自动验收，按用户授权继续语义 Task；不提交、不 push、不修改旧报告。

**2026-09-26 用户确认（窗口 1 提问）**：本轮用“固定 HTML 的完整保存流程回放”各 2 份记录耗时，代替真实 Agent 生成的总耗时；真实生成前后的总耗时对比移到重启后的整体验收中，与三批改动的前后对比一起测量。窗口 1 继续实施语义核对。

**2026-09-26 Claude 审阅**：接入口径符合第 2 轮裁决（重叠与卡死拦截、截断与空图只作提示、内容空洞拒绝生成）。复跑质检、语义核对、报告保存与数字核对相关测试共 72 passed；Vibe 293/0。

**2026-09-26 上线事故与修复（Claude）**：用户重启后，万华化学报告连续 3 次保存返回 503。原因有两层：
1. Playwright 只写进了 `pyproject.toml`，Docker 镜像按 `requirements.txt` 安装依赖，镜像里既没有 Playwright 包，也没有 Chromium；
2. `report_quality.py` 在模块顶层导入 Playwright，缺包时整个模块加载失败，保存流程随之报错。

修复：
- `requirements.txt` 加 `playwright==1.63.0`，`Dockerfile` 加 `python -m playwright install --with-deps chromium`；
- Playwright 改为只在检查子进程里导入；
- 检查工具自身不可用时**不阻塞保存**：报告照常保存，`quality_warnings` 记一条 `quality_unavailable`，前端单独显示“这份报告保存时未做排版检查”，不计入“排版待核”条数；
- 补测试：检查工具不可用时照常保存；未安装 Playwright 时模块仍可加载。

教训：新增后端依赖必须同时核对 Docker 镜像的安装路径，自动测试是在本机 uv 环境跑的，发现不了这类问题。
