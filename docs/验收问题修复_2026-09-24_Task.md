# 验收问题修复（2026-09-24 批）

状态：2026-09-24 B 已实施；A 的一次无头复现未出现空白，未改样式；C 尚无修复后成功整理记录可核对。自动检查完成，真实视觉待用户验收。不提交、不 push。实施仓库：Vibe `desktop/`。

## 1. 问题与证据（Claude 核实）

### A. 关闭「查看依据」后公司页首卡空白

- 复现（用户）：个股研究 → 国电电力研究页 → 财务块任一「查看依据」→ 右侧依据面板正常显示 → 关闭面板 → 页首公司身份卡（`components/ResearchKnowledge.tsx` `WikiBody` 公司分支的 `GlassCard glow`：图标、“公司研究 · 日期”、名称、股票代码 / 所属行业 / 行业大类）只剩卡片底纹，内容与右上角光晕都不见；下方「经营」等卡正常。
- 线索（未证实）：依据面板属于 `finance-side-panel`，打开 / 关闭时 `research-surfaces.css` 的 `:has(.finance-side-panel)` 改变主区宽度；`.glass` 带 `backdrop-filter: blur(14px)`，`.glass-glow` 带 `overflow: hidden` 与 `::before` 光晕（`src/index.css`）。怀疑为宽度变化后合成层未重绘；也可能是面板关闭触发了该卡重新挂载或状态被清空。**须先定位真实原因再修**，不得用强制重挂整页等掩盖手段。

### B. 图文报告与公司研究同时进行时报告无法保存

- 复现（用户）：国电电力 15:51:08 开始公司研究（研究中），15:51:38 生成图文报告。报告任务按开始时的研究页版本钉住（Stock `dsh/src/research-material-tools.mjs` `state.reportTask.inputHash`）；期间公司研究发布了新版本，保存时 Backend `POST /wiki/reports` 返回 409「页面版本已变化」（`backend/app/api/v1/wiki.py` `create_wiki_report`），重读又被钉住版本拒绝，任务只能作废。钉住本身是正确设计（不静默换稿），问题在产品层：
  - B1 同一研究页已有进行中的公司研究或知识整理时，仍可点「生成图文报告」；反之报告生成中仍可开始公司研究 / 刷新资料。
  - B2 报告未保存，任务列表却显示「已记录」：`pages/MyResearch.tsx` 把会话 `run_status === 'completed'` 映射为 `recorded`，不看报告是否真的保存。
  - B3 报告任务标题显示 slug（“图文报告 · companies/600795-sh”），应为公司 / 页面名称。

### C. 旧知识整理任务摘要为英文（仅核对）

- 「我的研究 · 任务」15:04 的知识整理摘要为英文（“Settlement for 600309.SH …”）。该记录早于 15:09 提示词修复（知识整理用户可见字段须为中文）。只核对新产生的整理记录为中文；旧记录不改。

## 2. 冻结决定

- **A**：定位根因并做最小修复；修复须覆盖依据面板、任务过程面板、问助手面板三种右侧面板的开 / 关（同一 `finance-side-panel` 契约），以及公司页、行业页的首卡。
- **B1**：同一研究页（同 slug）上，公司研究（含其知识整理阶段）与图文报告互斥：一方进行中时，另一方入口禁用并提示“公司研究进行中，完成后可生成图文报告” / “图文报告生成中，完成后可刷新资料”。判断依据用现有任务绑定（`reportTasks` `sessions` 的 `kind` / `slug`）与原生会话运行状态（`taskRunning`），不新增存储。不同公司之间不受影响。
- **B2**：报告任务状态以“是否真的保存”为准：会话结束后按 slug 查 Backend 报告（`/wiki/reports?slug=`）是否有 `created_at` 晚于任务开始的记录；有 → 「已生成」；无 → 「未保存」，摘要说明“研究页在生成期间已更新，请重新生成”，行上提供「重新生成」（对当前版本新开报告任务）。用词与现有任务状态表统一（`MyResearch.tsx` 状态映射）。
- **B3**：标题用 `bind.title` 或对象登记层解析的名称；取不到时再退回 slug。
- 不改变报告钉住版本、Backend 409 语义与发布锁。

## 3. 范围

- A：`components/ResearchKnowledge.tsx`、`components/ui/GlassCard.tsx`、`src/index.css`、`components/layout/research-surfaces.css`（按根因择取最小集合）。
- B：`pages/MyResearch.tsx`（状态、标题、重新生成）、研究页报告 / 刷新入口所在组件（`pages/CompanyWiki.tsx` 或其动作零件）、`dsh/client.tsx`（若需暴露同 slug 进行中任务查询）、对应测试。
- C：无代码改动，仅回填核对结果。

## 4. 验收

- **执行方（自动）**：`cd desktop && npm run typecheck && env -u NODE_ENV node --test test/*.test.ts`；`git diff --check`；`node dsh/build-ui.mjs`；重启工作台。新增测试：同 slug 互斥判断（研究进行中 / 整理进行中 / 报告进行中 / 不同 slug 不互斥）；报告状态映射（已保存 / 未保存）；报告标题取名。
- **执行方复现**：A 用一次短时无头浏览器按 §1 复现并截图前后对比，回填根因与修复后截图说明；用后关闭浏览器。
- **用户验收**：
  1. 公司页、行业页：打开并关闭「查看依据」、任务过程、问助手各 3 次，首卡内容始终可见。
  2. 某公司公司研究进行中：该公司「生成图文报告」禁用并有提示；另一家公司可正常生成。
  3. 构造一次报告未保存（或用 15:51 这条记录）：任务显示「未保存」与原因，点「重新生成」可成功生成；成功保存的报告显示「已生成」，标题为公司名。

## 5. Out of Scope

- 报告任务自动重试；Backend 报告钉住或 409 语义变更；T3 面板改造；旧任务记录的回写修正。

## 6. Stop Conditions

- A 的根因在 DSH 包或浏览器缺陷且产品侧无正当修法：停止，回填复现与可选方案。
- B2 无法可靠判断“本任务是否保存了报告”（例如同 slug 并发多报告）：停止，回填可用字段。
- 与其他工具同时修改同一文件（`dsh/client.tsx`、`MyResearch.tsx` 有未提交改动）：先看 git 状态，冲突时停下告知用户。

## 7. 回滚

还原上述文件并重建。

## 8. 执行回填

- **A**：无头 Chromium 打开国电电力研究页，财务「查看依据」开关两轮。关闭后首卡 DOM 保有图标、名称、代码与行业，计算样式可见；滚动到首卡时截图正常，未复现空白。点击财务依据会滚动主区；关闭后焦点恢复使视口回到财务块，这解释了首卡不在视口，却不能证明用户看到的空白卡根因。未修改 `GlassCard` 或 CSS，也无法声称三种侧栏与行业首卡已通过复核。截图：`output/playwright/first-card-before.png`、`panel-open-top.png`、`first-card-after-close-top.png`。浏览器已关闭。待可复现时先比对 DOM、布局与像素，再决定修法。
- **B1**：按同 slug 任务绑定、DSH 原生运行态和整理状态判定入口互斥；正在启动的短暂 pending 也纳入。公司页报告生成期间禁用「刷新资料」和「开始研究」，报告页研究或整理期间禁用「生成图文报告」，均给出原因。启动研究或报告时，任务宿主再次检查，防止仅靠界面状态产生并发；不同 slug 不受影响。版本钉住及 Backend 409 未改。
- **B2**：任务列表从 Backend 报告目录读取候选，再从详情里的 `session_id` 与任务会话、输入版本和开始时间精确匹配。匹配到才显示「已生成」；已完成却没有制品显示「未保存」与重新生成入口；目录暂不可读时显示「结果待确认」，不猜测已保存。国电电力 15:51 的绑定会话为 `completed`，该 slug 的 Backend 报告目录为空，符合「未保存」。重新生成读取当前研究页版本后启动新任务。
- **B3**：报告绑定写入可读标题；既有任务由对象登记层解析页面名称，确实没有名称时才退回 slug。
- **C**：15:09 中文提示词修复后的新记录中，15:55 的记录为超时取消，摘要是运行时英文错误；尚无新成功整理的结构化摘要，因此不能验证其中文效果。15:04 旧记录保留不改。
- 自动检查：`npm run typecheck`、255 项完整测试、`git diff --check`、`node dsh/build-ui.mjs` 均通过；工作台已重启于 `http://127.0.0.1:5930`。真实页面操作与视觉仍待用户验收，A 保持未解决。
