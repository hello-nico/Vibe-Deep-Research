# Wiki 写入提速与读写不互斥

状态：2026-09-23 A 已上线、B/C 已部署；用户实测新建公司页约 9 秒，发布期间读取正常（见 §9.3–9.5）。验证资源已清理；本轮提交代码与记录，不 push。实施仓库为 Stock-Research Backend；工作台处理可重试错误的展示。

权威：[Human Checklist](../human-checklist.md)「产品核心协议」中 Backend 拥有 Wiki 发布与回执；[公司与行业 Wiki 确定性刷新闭环 Task](公司与行业Wiki确定性刷新闭环_Task_2026-09-23.md)（其真实验收依赖本 Task）。

## 1. 问题与证据（2026-09-23 实测）

用户反馈：新建公司研究页、刷新行业研究页非常慢；期间行业研究列表显示“Error: 资料正在更新，请稍后重试”。三个原因叠加：

1. **gbrain 每次调用固定多等 5 秒**。`docker-compose.yml:88` 与 `backend/wiki/gbrain_adapter.py:118,421` 设置 `GBRAIN_FLUSH_GRACE_MS=5000`，gbrain 按此值在退出前固定等待（源码 `src/core/cli-force-exit.ts`，默认 250ms）。容器内 `gbrain --version` 实测：5000→5251ms、500→747ms、0→270ms。一次 `publish()` 调用 sync、extract links、每条变动链接一次、`list_pages` 分页核对 1–4 次（每页 20，现 64 页）、`--version`，共 5–8+ 次，约 25–40s，实际工作不足 2s。
   - 5 秒由提交 385c220（2026-08-25）引入，用于规避 gbrain 退出过快导致 stdout 截断。gbrain 0.46.23 起以 `writeStdoutFinal` 在退出前完整送达输出；容器固定为 0.43.0.0（`backend/Dockerfile:2`、`PINNED_GBRAIN_VERSION`），不含该修复。
2. **全站一把锁，读等 2 秒即失败**。`wiki_publish_lock.py` 用同一 advisory lock：写者独占整个发布周期，读者 `READ_LOCK_WAIT_MS=2000` 后抛 `WikiPublishBusy` → 503“资料正在更新”。日志：06:15:54 行业刷新确认期间，06:15:18–06:15:51 `GET /wiki/industries/nbs` 连续 5 次 503。
3. **新建公司页在锁内联网**。`ensure_company_page` → `_refresh_company_page_unlocked` 在独占锁内 `initialize_page(providers=…)` 请求同花顺/腾讯，并同时发布公司页与所属行业页。公司“刷新资料”（`_refresh_company_api_locked`）已是锁外取数，可作对照。
4. 工作台对可重试 503 直接显示 `Error: …`（如 `pages/IndustryCenter.tsx`），无自动重试。

## 2. 目标

- 单次 Wiki 发布（行业时间线追加、公司字段更新）从数十秒降到数秒内；新建公司页锁持有时间不含外部取数。
- 写入期间读取不因锁直接失败：短暂等待后正常返回；仍超时时页面自动重试，不显示原始错误文本。
- 不牺牲现有保证：gbrain 输出完整性校验、发布幂等与 pending 恢复、页面完整性校验、读写一致性（读到的是某个完整已发布版本）。

## 3. 范围（按步骤）

### A. gbrain 调用提速（P1）

1. 在隔离环境（测试库 / 独立 wiki 根）评估 gbrain 升级到 ≥0.46.23（优先当前上游稳定版本）：`init / sources / sync / extract / list_pages / get` 等适配器用到的命令与输出结构、schema pack、已发布 64 页的读取与再发布兼容。
2. 兼容则升级 `Dockerfile` 与 `PINNED_GBRAIN_VERSION`，`GBRAIN_FLUSH_GRACE_MS` 改回 gbrain 默认（移除覆盖，或显式 250）。
3. 不兼容或需另行迁移：停止升级（§7），改为逐命令测量输出体量，只对确有大输出的命令保留较长等待，其余降至默认；以现有“完整 JSON 文档”校验兜底。不得无测量地统一调低。
4. 记录每条 gbrain 命令耗时与一次 publish 总耗时（改造前后对照），写入 §8。

5. **上线（执行方）**：严格按 §9.2 顺序执行，每步记录结果；任一步失败即按 §9.2 第 5 步回滚并停止。执行要点：
   - Stock 工作区已有 A 批未提交改动：`backend/Dockerfile`、`docker-compose.yml`、`backend/wiki/gbrain_adapter.py`、`backend/wiki/README.md`、`backend/wiki/schema_pack/stock-research.yaml`、`backend/tests/wiki/test_gbrain_smoke.py`。不要回退或重写。
   - 容器内访问 Postgres 用 `postgres:5432`；适配器默认连接串是宿主视角 `127.0.0.1:5434`，迁移时用 `STOCK_RESEARCH_GBRAIN_INIT_URL`、`STOCK_RESEARCH_GBRAIN_ADMIN_URL` 覆盖（migrate 账号，库 `stock_research_gbrain`）。凭据不得打印到终端、日志或本文档。
   - 备份放仓库外（如 `~/stock-research-backups/<时间戳>-gbrain-upgrade/`），记录文件大小与 sha256；打包 `workspace/wiki` 时跳过 `.gbrain/run/*.sock`。

### B. 新建公司页锁外取数（P2）

- `backend/app/services/wiki_refresh.py`：`ensure_company_page` → `_refresh_company_page_unlocked` 仿照 `_refresh_company_api_locked` 改为锁外先取 Provider 数据，锁内只做存在性复核、编译与发布；锁内发现页面已被他人创建则返回 `exists`，不重复发布。公司页与所属行业页仍在同一次锁内发布。
- 测试：锁内不发生 Provider 调用；同一公司并发创建只发布一次。

### C. 读取不因写入直接失败（P2）

- A 上线后按实测 publish 耗时上调 `backend/app/services/wiki_publish_lock.py` 的 `READ_LOCK_WAIT_MS`，使一次常规发布内的读取等待而非失败；等待上限仍有界，在 §9 写明取值依据。
- 工作台在现有取数层（`desktop/src/verticals/finance/lib/research.ts` 的 `researchRead` 或其调用链）对 `503 + retryable: true` 做有界退避重试（建议最多 3 次、尊重 `Retry-After`），不新增平行请求链；重试耗尽再显示人话提示与重试按钮，不显示 `Error:` 前缀（至少覆盖 `pages/IndustryCenter.tsx`）。
- 测试：可重试 503 重试后成功；非可重试错误不重试；重试次数有上限。
- 评估是否需要“读已发布快照不加锁”：仅当 A、C 后仍有明显失败时另立决定，本 Task 不做读路径重构。

## 4. 写范围

Stock：`backend/Dockerfile`、`docker-compose.yml`、`backend/wiki/gbrain_adapter.py`、`backend/app/services/wiki_publish_lock.py`、`backend/app/services/wiki_refresh.py`（新建公司页路径）及对应测试。
Vibe：可重试 503 的取数与展示（`desktop/src/verticals/finance/lib/research.ts` 或现有取数层、受影响页面的错误展示）及测试。

## 5. 验收

- 自动：Stock `cd backend && uv run pytest -q`；Vibe `cd desktop && npm run typecheck && node --test test/*.test.ts`；两仓 `git diff --check`。已知与本 Task 无关的失败（HEAD 上同样失败）：`tests/test_real_evidence_artifacts.py`（本机缺真实年报产物）、`tests/test_research_links.py::test_wiki_research_link_http_confirm_and_nbs_catalog`（需 `STOCK_RESEARCH_GBRAIN_ROOT`）；回填时注明仍为同样原因。
- 性能：同一行业刷新确认与新建公司页，记录改造前后 publish 总耗时与锁持有时间（Backend `wiki_lock` 日志或计时）。
- 真实运行：写入进行中反复打开行业研究、个股研究、深度对话引用，页面不出现 `Error:`；统计 Backend 日志 `WikiPublishBusy` 次数（改造前后）；已发布 64 页全部可读，页面完整性校验无报错，无残留 `publication.json`；刷新闭环 Task 的真实验收随后完成。
- 清理：上线验收后删除验证用数据库 `stock_research_gbrain_upgrade_check` 与验证镜像 `stock-research-backend:gbrain-0.46.25`。
- 重建 Backend 容器后生效；需先备份 wiki 根与 gbrain 数据库（见 §6）。

## 6. 安全与回滚

- 升级 gbrain 前备份 `workspace/wiki` 与 `stock_research_gbrain` 数据库；回滚 = 恢复 Dockerfile 版本与备份后重建容器。
- 不重写已发布页面内容。gbrain 结构迁移 125→135 已在生产副本验证（§9.1），按 §9.2 先备份再迁移；`init` 可重复执行，回滚以备份恢复。

## 7. Out of Scope

- Wiki 存储或锁模型重构（分对象锁、无锁快照读）、GBrain 以外的检索引擎替换。
- 刷新闭环的业务规则（检查、差异、确认、回执）；只改其依赖的发布速度。
- 研究会话、DSH 版本、工作台其他页面的加载优化。

## 8. Stop Conditions

- 新版 gbrain 命令或输出结构与适配器不兼容、需数据迁移，或已发布页面读取/再发布校验失败：停止升级，走 §3 A.3，记录差异。
- 降低等待后出现任一 JSON 截断或 publish pending 残留：恢复原值，记录命令与输出体量。
- 锁外取数导致同一公司并发创建出现重复发布或版本错乱：停止 B，保留原路径。
- 自动重试造成请求风暴（同页并发重试超过上限）：停止 C 的前端部分，改为手动重试。

## 9. 执行回填

### 9.1 A 批评估（2026-09-23，隔离环境）

- 目标版本 **gbrain 0.46.25.0**，不追最新 0.52.2：0.46.23 起 `call`/通用操作以 `writeStdoutFinal` 完整送达；0.46.25 起退出前统一排空被拦截的 stdout（#4383），覆盖 `sync`/`extract` 等直接打印的命令。0.43→0.46.25 新增迁移 126–135（会话上下文、OAuth、任务队列、事实索引、分块嵌入），均不触及适配器直接读取的 `pages`/`page_versions`/`links`；0.52.2 另含 150–159 页面版本与持久化重写，风险更高。
- 兼容面：CLI `--version / init / sources add|list|attach / sync / extract links / call list_pages|get_links / link / unlink / graph-query / list`；只读 SQL `pages`、`page_versions`、`links`。版本号仅写入回执，代码不强制校验。
- 新版单次 CLI：默认等待约 0.6s（0.43 + 5000ms 为 5.3s）。
- 冒烟（`test_gbrain_smoke`，隔离测试库）：0.46.25 空库初始化至 v135 并发布三类页面、建链、再发布、核对通过，17.5s。
- 生产副本（`stock_research_gbrain` 经 pg_dump/pg_restore 复制为 `stock_research_gbrain_upgrade_check`，0 恢复错误，页 333 / 链 327 / 版本 338 与生产一致；Wiki 根复制并挂到同一路径，配置改指副本库）：
  - 普通命令不自动迁移，125 结构下 64 个已接受页面全部可读、关联与更新时间 SQL 正常、行业页真实发布成功。
  - 以 migrate 账号 `init` 迁移 125→135 用时 0.8s；迁移后读取与发布同样正常，无残留 `publication.json`。
  - 逐命令计时发现 verify 以 `sort=slug` 每页 20 条翻遍全库（333 页），单次发布调用 `list_pages` 15 次；在 0.43 下约 80s，是写入慢的主因之一。
- 改动（Stock，未提交）：`backend/Dockerfile` 与 `PINNED_GBRAIN_VERSION` → 0.46.25.0；删除适配器与 compose 中 `GBRAIN_FLUSH_GRACE_MS=5000` 覆盖，沿用 gbrain 默认；verify 的 `list_pages` 限定 `source_id=wiki`、`sort=updated_desc`（刚发布页通常在首页，分页兜底；遵守 README“禁止逐页 get 验收”）；schema pack `gbrain_min_version`、Wiki README、冒烟版本断言同步。
- 验证：Backend `tests/wiki` 162 通过 / 7 跳过；全量 781 通过，`test_real_evidence_artifacts`（本机缺真实年报产物）与 `test_research_links::…nbs_catalog`（需 `STOCK_RESEARCH_GBRAIN_ROOT`）在 HEAD 独立检出上同样失败，与本改动无关。新镜像不设等待变量：冒烟通过；副本单次行业页发布 **2.6s**（sync 0.74s、extract 0.67s、list_pages 1 次 0.63s、version 0.56s），此前生产约 40s。

### 9.2 生产升级步骤（用户已授权，执行记录见 §9.3）

1. 备份：`stock_research_gbrain` 以 migrate 账号 `pg_dump -Fc`；`workspace/wiki` 打包（含 `.gbrain/config.json`、`.stock-research-wiki/`）。存放于仓库外。
2. `docker compose up -d --build backend`（短暂停服）。
3. 容器内以 migrate 账号执行适配器 `_init_postgres_schema()` 迁移 125→135 并重授权；`config.json` 的 `database_url` 恢复为升级前的值。
4. 验证：结构版本 135；64 页全部可读；工作台行业刷新与新建公司页各一次，记录发布耗时与 `wiki_lock` 读忙次数。
5. 回滚：停 backend；以备份重建 `stock_research_gbrain` 并恢复 Wiki 根；还原 Dockerfile/compose/适配器改动后重建容器。

执行方回填：9.3 上线（备份路径与校验和、schema 版本、64 页冷读、行业刷新与新建公司页实测耗时与锁持有、`WikiPublishBusy` 前后次数）；9.4 B；9.5 C（`READ_LOCK_WAIT_MS` 取值依据、重试策略）；验证命令与结果；改动文件清单；触发的停止条件或待裁决问题。

### 9.3 A 上线（2026-09-23）

- 备份目录（仓库外）：`/Users/apple/Documents/Stock-Research-Backups/2026-09-23-gbrain-04625/`。`gbrain-preupgrade.dump` 885056 字节，SHA256 `2b85f715512c0e6f9c230a65051e38784c9c835d8e3d4ad628c8cbadd9ec7ec7`（2026-09-23 Claude 验收时重算校正，原记录漏 8 位）；`wiki-preupgrade-clean.tar.gz` 593393 字节，SHA256 `26601ff5842b69bbc77ced361c142272b87d91b35209ba23528f7c04c98238ff`。备份含配置和页面台账、排除临时 Unix socket；`pg_restore --list` 与 tar 条目检查通过。旧容器停机后取一致备份。
- 新镜像构建成功；容器内以 migrate 账号执行 `_init_postgres_schema()`，结构 125→135，配置中的数据库地址保持原值。迁移后页/链/版本数仍为 333/327/338，无 pending 发布记录；重建后的 Backend 健康，`gbrain --version` 为 0.46.25.0。B/C 再次重建后 64 个已接受页全部冷读成功，健康与行业目录接口均返回 200。
- 工作台对用户指定的“石油和天然气开采业”执行真实刷新检查（`check-f6387752794a42dc95340a75ef948570`），显示“统计来源已收录，现有资料保持原样”。本次没有差异，未触发发布，故**没有生产发布耗时或锁持有时间**；副本 2.6 秒数据仍只证明隔离环境。新建公司页由用户自行测试。旧版已有 5 次读 503 的记录；本次部署后的短窗口未观察到读忙，但没有并发发布，不能据此判定 C 的真实验收通过。
- 行业刷新检查无差异，未触发写入；当时暂保留隔离检查库和验证镜像。未触发回滚条件；备份保留。

- 2026-09-23 用户实测新建公司页（万华化学 600309），Claude 核对 Backend 日志：07:57:40 读页 404 → 07:57:49 `POST /wiki/pages/ensure` 200（含 Provider 取数与公司 / 行业页发布，约 9 秒，此前数十秒至分钟级）；期间与之后的读取均 200，`WikiPublishBusy` 0 次。用户确认体验“很快”。**生产写入与写入期间读取验收通过**；执行方可按 §5 清理（另核对备份目录中未记录的 `wiki-preupgrade.tar.gz` 是否保留）后提交。

### 9.4 B 新建公司页锁外取数

- `wiki_refresh.py` 对已存在页面先在共享读锁下返回 `exists`；缺页时在发布锁外构建并用 Provider 初始化 PageSpec，进入独占锁后恢复 pending、复核存在性、编译并一次发布公司与行业页。并发第二个请求返回 `exists`。`test_wiki_refresh.py` 增加锁外 Provider 与并发创建断言，13 项通过。生产新建公司页约 9 秒、回读 200（§9.3）。

### 9.5 C 有界读取等待与重试

- `READ_LOCK_WAIT_MS` 从 2000 调到 8000：隔离环境单次发布 2.6 秒，取约 3 倍等待空间，仍保留有界失败。生产发布分布尚待实测校准。
- 工作台 `researchRead` 只重试 GET 的 `503 + retryable: true`，最多补发 3 次；采用 `Retry-After`（上限 5 秒），缺失时 0.5/1/2 秒退避，支持中止。重试耗尽提示“资料仍在更新，请稍后重试”；行业页移除 `Error:` 前缀并提供按钮。前端新增 2 项行为测试通过，正在运行的 Vite 已提供新代码。
- 验证：Stock `tests/wiki/test_wiki_refresh.py` 13 通过，读锁相关 30 通过/5 跳过；全量 781 通过、7 跳过，6 个失败仍是既有两类本机条件（5 个年报产物缺失参数例、1 个 `STOCK_RESEARCH_GBRAIN_ROOT`）。Vibe typecheck 通过、203 项测试通过；两仓 `git diff --check` 通过。公司页创建及发布窗口读取已实测（§9.3）；更广泛的发布耗时分布仍按后续运行观察。

### 9.6 上线资源清理

- 删除临时数据库 `stock_research_gbrain_upgrade_check` 与验证镜像 `stock-research-backend:gbrain-0.46.25`；生产数据库和运行镜像保留。
- 仓库外升级备份目录及其中的 `gbrain-preupgrade.dump`、`wiki-preupgrade-clean.tar.gz` 保留；额外的 `wiki-preupgrade.tar.gz` 也暂保留，不作为本轮清理对象。
