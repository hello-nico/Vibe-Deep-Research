# 事实候选对齐准入白名单 · 会话标题可读

状态：2026-09-24 Codex 已实施并完成自动检查，待用户实测。**执行方只跑自动检查**。不提交、不 push。A 在 Stock-Research（`dsh/` 为主），B 在 Vibe `desktop/`。

## 1. 问题与证据（2026-09-24 Claude 诊断）

- **A. 事实候选大量被拒**：深度对话结束显示“本轮有 1 条事实未通过核对”。Backend `agent_extractions` 最近 4 条 `rejected` 原因全部为 `admission policy rejects metric predicate: <名>`（`top_five_customer_sales_share`、`equity_installed_capacity`、`trading_business_gross_margin`、`overseas_revenue`）。准入白名单在 `backend/config/fact-admission-policies.json` `allowed_metric_predicates`（26 个：`revenue`、`net_profit`、`net_profit_attributable`、`gross_margin`、`operating_cash_flow`、`total_assets`、`total_liabilities`、`eps`、`roe`、`installed_capacity`、`electricity_generated` 等），Stock DSH `stage_extraction`（`dsh/src/extraction-tools.mjs`）只校验谓词格式，**不告诉模型哪些可准入**，模型自造谓词，暂存后在研究结束时整批被拒（海螺水泥一轮暂存 13 条）。
- **B. 会话标题不可读**：@ 对象后发送的首条消息，DSH 自动标题取自发给模型的序列化文本，显示为“引用材料：宁德时代 \`compan…”。DSH 会话控制接口提供 `sessions.rename`（`@deepseek-ai/dsh-api-session-controller`）。

## 2. 冻结决定

- **A**：
  - `stage_extraction` 的 metric 谓词改为**枚举**，取值与 `fact-admission-policies.json` 当前策略的 `allowed_metric_predicates` 同源（构建时读取该文件生成，或 DSH 启动时从 Backend 读取，二选一并回填理由；**不在两处手写两份**）；不在白名单的指标在本地直接拒绝，错误提示列出可用谓词并说明“其余指标只在回答中说明，不暂存为事实”。
  - 工具说明加一句：只有白名单指标能成为公司事实；经营比率、客户集中度等非白名单指标照常在回答中引用原文，不暂存。
  - relation / lifecycle 候选不变。
  - 不放宽 Backend 准入；是否扩充白名单（如海外收入、前五大客户占比）是另一项决定，不在本 Task。
- **B**：
  - 首条消息发送后，若会话标题仍是 DSH 自动生成（未被用户改过）且包含产品序列化引用（`引用材料：`、`引用议题：` 等 `lib/researchMentions.ts` 能折叠的格式），用同一折叠规则生成可读标题（如“宁德时代 · 华友钴业供应关系”，取折叠后的首条消息前 30 字）并调用 `sessions.rename`。
  - 用户手动改过的标题永不覆盖；只处理本产品发起的深度对话会话，不处理后台任务与问助手会话。
  - 若 DSH 无法区分“自动标题 / 用户标题”：停止并回填（见 §6）。

## 3. 范围

- A：`dsh/src/extraction-tools.mjs`（枚举与本地拒绝）、构建或启动读取逻辑、测试；Stock `docs/human-checklist.md` 记一句“暂存事实指标与准入白名单同源”。
- B：Vibe `dsh/client.tsx`（发送后重命名）、`lib/researchMentions.ts`（导出折叠为纯文本的函数，若尚无）、测试。

## 4. 验收

- **执行方（自动）**：Stock `cd dsh && pnpm run test`；Vibe `cd desktop && npm run typecheck && node --test test/*.test.ts`；两仓 `git diff --check`；重建 `dsh/dist` 与 finance-ui，重启工作台。新增测试：白名单外谓词本地拒绝且提示含可用列表；白名单与 Backend 配置一致（读同一文件断言）；白名单内谓词照常通过；标题折叠（含 slug、反引号的输入 → 可读标题）；用户改过的标题不重命名。
- **用户验收**：
  1. 深度对话问一个会读年报、涉及客户集中度或海外收入的问题，结束后不再出现“事实未通过核对”（这些指标只在回答里说明）；轨迹中 `stage_extraction` 调用明显减少。
  2. @ 一家公司发问，会话标题显示公司名与问题，不含 `companies/…` 与反引号；手动改名后再发消息不被改回。

## 5. Out of Scope

- 扩充准入白名单；relation / lifecycle 准入；历史会话标题批量修复；DSH 标题生成逻辑本身。

## 6. Stop Conditions

- 白名单无法与 Backend 配置同源（只能手写副本）：停止，回填方案。
- DSH 不暴露标题来源（自动 / 用户）且无其他可靠判断：停止，回填可用字段。
- 与其他工具同时修改同一文件：先看 git 状态，冲突时停下告知用户。

## 7. 回滚

还原上述文件并重建。

## 8. 执行回填

- A：`dsh/scripts/build-research.mjs` 构建时读取 Backend 唯一配置 `backend/config/fact-admission-policies.json` 的 `agent-source-bound-v2.allowed_metric_predicates`，生成运行资源；选择构建时读取，是因为 DSH 工具 schema 需要启动时就有枚举，且不增加 Backend 运行接口。`dsh/src/extraction-tools.mjs` 的 metric 谓词使用该枚举；白名单外指标在暂存前本地拒绝，错误列出可用谓词，并告知只在回答中说明、不暂存。relation / lifecycle 保持原规则。`docs/human-checklist.md` 已记录同源决定。未扩充白名单。
- B：`desktop/src/verticals/finance/lib/researchMentions.ts` 增加复用现有引用识别规则的纯文本折叠与标题生成；`desktop/src/verticals/finance/dsh/research-title.ts` 依据 DSH `session/title.source.kind` 区分自动与用户标题，并从首条用户消息生成可读标题；`dsh/client.tsx` 只监听本产品新建的前台深度对话会话，重命名前再次检查标题来源，跳过用户手动标题和后台 / 问助手会话。
- 新增 `dsh/test/metric-admission.test.mjs` 和 `desktop/test/research_title.test.ts`：覆盖 Backend 配置与工具枚举一致、白名单外本地拒绝、白名单内暂存、引用 slug / 反引号折叠和手动标题保护。
- 自动检查：Stock `pnpm run test` 170/170；Vibe `npm run typecheck` 通过、`node --test test/*.test.ts` 249/249。桌面测试首次在沙箱内因本地端口权限导致 `lan_proxy` 一例失败，获权限后完整重跑通过。两仓 `git diff --check` 通过。Stock DSH `pnpm run build`、Vibe `npm run build` 均通过。工作台已重启，页面 HTTP 200；DSH 未带凭据返回 401，符合鉴权行为。
- 执行方未做真实模型对话或用户视觉验收；§4 的两项用户验收仍待实测。本 Task 未提交、未 push，保留两仓其他并行改动。

### 8.x 用户实测与 Claude 补修（2026-09-24）

- 会话标题：实测通过（“宁德时代 · 最近的经营情况怎…”）。
- 白名单：生效——海外收入、客户占比不再暂存；但营收、归母净利润仍被拒，原因 `MetricUnitRejected`：模型把单位写成 `CNY`，Backend 单位表只认中文单位。Claude 补修：`backend/app/services/metric_units.py`（单位唯一 Owner）加别名 `CNY/RMB/人民币 → 元`、`CNY_yi → 亿元`、`CNY_wan → 万元` 等并补测试；`stage_extraction` 的 payload 说明列出可用中文单位。
- 议题建议卡片：边框用了未定义的 `var(--border)` 显示为黑色、理由为英文。Claude 补修：`dsh/result-node.tsx` 三处卡片改用产品色（议题建议为浅橙底 / 淡橙边），知识整理提示词要求所有用户可见字段为中文。
- 检查：Stock DSH 170/170、Backend 相关 28 通过、Vibe typecheck 与 249/249。Backend 改动需重建容器生效。
