# 经营数据提取（整理失败不丢数据、单位校验、行业指标）

状态：2026-09-26 Claude 写定；同日用户确认 §2 第 3 条采用通用 `industry_metric`，并决定**立即派发**。待 Codex 实施并回填 §8。执行方只跑自动检查，不重建、不重启、不提交。实施仓库：Stock-Research（`backend/`、`dsh/`）。

权威：AGENTS.md「研究纪律」（数字必须能回到真实证据；数据缺失不等于零）。同日排查结论见本文 §1。页面文案已由 Claude 同日改为不承诺“补齐”（Vibe `ResearchKnowledge.tsx` `SectionGap`），不在本 Task 内。

## 1. 问题

中际旭创研究页的“经营”块在研究后仍为空。2026-09-26 10:17 那次研究的实际过程：

| 阶段 | 实际做了什么 |
|---|---|
| 公司研究 | 提交 2 份定期报告入库，读原文 1 次，抓取 4 个网页；这一阶段不提取经营数据 |
| 知识整理 | 读了若干段年报与半年报，只暂存 **1 条**数据（`stage_extraction`） |
| 结果 | 整理校验失败，这 1 条也没入库 |

三个独立原因：

1. **暂存数据跟着整理失败作废**：`dsh/src/background-review.mjs` 里 `finalizeTurn`（把暂存数据交后端核对入库）排在 `validateReview` 之后；校验失败抛错时不会执行。经营数据入库本来不依赖研究结论草案。
2. **单位错配拦不住**：唯一那条是 `cash_dividend`，值填了 `1,111,118,334.00 元`，实为总股本股数（原文“以总股本 1,111,118,334 股为基数，每 10 股派发现金红利 10.00 元”）。这个数字确实出现在原文里，现有 `require_metric_unit_and_range` 只查单位与范围，拦不住。
3. **行业指标无法入库**：研究页 `operating_facts.required_metrics` 来自申万行业画像（`wiki/business_compiler.py` 约第 122 行），如通信设备（801102.SI）的“高速光模块出货量”“光模块市占率”。准入策略 `backend/config/fact-admission-policies.json` 的 `agent-source-bound-v2` 只允许 26 个 predicate（通用财务 + 电力专用），所以非电力行业的经营指标即使被提取也会被拒。整理 persona 也没有要求按清单逐项找。

## 2. 冻结决定

1. **暂存数据与研究结论脱钩（原 A）**：
   - 整理子任务正常结束但研究结论校验失败时（含纠正后仍失败），照常执行 `finalizeTurn`，再按失败返回。
   - 超时和用户取消时不补交：`signal` 已中止，请求本身发不出去。
   - 成功路径的顺序不变。
   - 任务台账照常记录 `extraction` 结果，“我的研究 · 任务”里能看到本次入库或被拒的条数。
2. **数值与单位必须能在原文对上**：
   - 后端核对 metric 时，`payload.value` 必须在支撑块原文中出现，比对前去掉千分位。
   - 原文里这个数字后面紧跟单位时（3 个字符以内，如“股”“元”“万元”“亿元”“%”“千瓦时”），该单位必须和 claim 的单位属于同一类，否则拒绝，原因写 `unit_mismatch_in_source`。
   - 数字后面没有单位（常见于表格单元格）时不按这条拦截，交给第 1 步“数值出现”检查。
   - 数字比对与单位换算复用 [报告引用数字核对 Task](报告引用数字核对_Task_2026-09-26.md) 的 `app/services/citation_numbers.py`。那个 Task 未完成时，本条等它，不另写一套。
   - **2026-09-26 修订**：`number_match` 带容差，不适用于入库。改用该 Task §2 第 7 条的严格核对入口：数值相等、不做换算、单位分类含“股”、无单位表格放行，返回 `value_not_in_source` / `unit_mismatch_in_source`。由窗口 1 提供，本 Task 只调用。（2026-09-26 再修订：窗口 1 未实现该入口且已停止，改由本 Task 在 `citation_numbers.py` 中按报告引用数字核对 Task §2 第 7 条实现，并补独立测试；不改动已有的 `number_match` / `citation_number_match`。）整理提示同步要求：暂存数据时数值和单位照原文抄写，不自行换算。
3. **行业指标入库（原 C，2026-09-26 用户确认）**：
   - 推荐方案：新增一个通用 predicate `industry_metric`。`payload` 必须带 `metric_name`，值必须**逐字等于**该公司研究页 `required_metrics` 里的某一项，再加 `value`、`unit`、`period`。
   - 后端核对时读取当前页面的 `required_metrics` 做校验，并执行第 2 条的数值与单位检查。
   - 编译研究页时，`industry_metric` 事实按 `metric_name` 归入“经营”块，显示名就用 `metric_name`。
   - 不采用“为 41 个行业逐一扩充 predicate 白名单”，维护量太大。
4. **整理按清单找**：
   - 整理 persona 增加要求：公司页若有 `required_metrics`，对已读到的定期报告原文，逐项查找并各暂存一条；找不到就跳过，不猜、不推算。
   - 预测、市场规模、行业资本开支等不在公司定期报告里的项目，明确跳过。
   - 同一次整理最多暂存 8 条经营数据。
   - 研究阶段的任务提示（Vibe 发起公司研究时的 prompt）不改。
5. 不改整理时限（`RESEARCH_SETTLEMENT_MS` 180 秒）。先测耗时，见 §4。

> 2026-09-26 用户确认第 3 条采用通用 `industry_metric` + 按页面清单校验。第 2 条依赖 [报告引用数字核对 Task](报告引用数字核对_Task_2026-09-26.md) 的 `app/services/citation_numbers.py`，该文件落地前第 2 条停下等待。

## 3. 范围

- Stock dsh：`src/background-review.mjs`（第 1、4 条）与测试。
- Stock backend：
  - `app/services/agent_extractions.py`（第 2、3 条）、`config/fact-admission-policies.json`（第 3 条）；
  - 事实编译进“经营”块的位置（从 `wiki/business_compiler.py` 查起）；
  - 对应 pytest。

## 4. 验收

- **自动**：
  - Backend 相关 pytest；Stock DSH `pnpm test`；Vibe `npm test`（其中 `background_review_runtime.test.ts` 直接调用 Stock 的整理代码，也要跑）；`git diff --check`。
  - 新增测试：
    - 校验失败时暂存数据仍被提交；
    - 取消时不提交；
    - “1,111,118,334 股”被当作“元”时拒绝，原因为 `unit_mismatch_in_source`；
    - 表格里无单位的数字能通过；
    - `industry_metric` 的 `metric_name` 不在清单内时拒绝；
    - 清单内的项正确归入“经营”块。
- **评估（执行方跑，结果写 §8）**：对中际旭创、万华化学、长江电力各跑一次公司研究，记录：
  - 暂存条数；
  - 入库条数；
  - 拒绝原因分布；
  - 知识整理耗时；
  - 人工核对 5 条入库数据与原文是否一致（报告期、单位、口径）。
- **用户验收**：中际旭创研究后，“经营”块出现定期报告里能找到的指标，每条“查看依据”能对上原文；找不到的项目仍列在“值得关注的指标”里。

## 5. Stop Conditions

- 三家里任一家知识整理耗时超过 150 秒：停下，回填耗时与工具调用次数，由用户决定是否调时限或减少条数。
- 人工核对的入库数据出现 1 条与原文不符：停下，回填样例，不放宽规则。
- `citation_numbers.py` 尚不存在：第 2 条停下等待，不另写比对逻辑。

## 6. Out of Scope

- 研究阶段任务提示与工具；
- 电力 predicate 的调整；
- 旧研究页数据回填（下次研究自然补上）；
- 整理时限调整；
- 页面文案（已改）。

## 7. 回滚

还原代码与准入策略配置。已入库的 `industry_metric` 事实，按 `predicate = industry_metric` 撤销（执行方须提供幂等撤销脚本）。

## 8. 执行回填

窗口 3 执行回填（2026-09-26）：§2 第 1、2、3、4 条均已实施。第 2 条原先等待窗口 1；用户确认窗口 1 已停止后，将严格入口实现责任转给窗口 3，现已完成接口与事实准入接入。真实三家公司评估仍留给用户，自动验收结果见下。

用户补充确认：依《报告引用数字核对 Task》§2 第 7 条，严格入口去千分位后比较数值相等，不换算、不设容差；同类不同量级也返回 `unit_mismatch_in_source`，无单位数字放行。现有报告 `number_match` 不用于经营事实准入。

同日补充：已在整理 persona 明确加入“暂存数据时数值和单位照原文抄写，不要自行换算”，对应测试断言已补。

已改文件：Stock `dsh/src/background-review.mjs`、`dsh/test/background-review.test.mjs`；Backend `app/services/agent_extractions.py`、`app/services/citation_numbers.py`（仅新增严格入口）、`app/services/company_facts.py`、`config/fact-admission-policies.json`、`tests/test_agent_extractions.py`、`tests/test_strict_metric_source_match.py`、`tests/test_company_facts.py`、`tests/wiki/test_business_compiler.py`，及下述撤销脚本、本 Task §8。沿用 `business_compiler.py` 现有经营块编译与指标名显示，无新增编译路径；保留 Claude 原有纠错、已有依据、快照 hash 与错误详情改动。

回滚：已提供 `Stock-Research/backend/scripts/revoke_industry_metrics.sql`，按 predicate 关闭 Claim 的 `known_to`，同事务将关联的已接受候选标为 rejected，避免相同候选重放伪报已接受；重复执行不再改变已关闭记录，保留 Evidence/审计历史。只写未执行；撤销结果需后续正常刷页读回。

自动检查（严格入口补齐后的最终结果）：Backend `UV_CACHE_DIR=/private/tmp/window3-uv-cache uv run pytest` 全量 **903 通过、6 失败、0 错误、7 跳过**（45.02 秒），全量未通过，含新测试与 `tests/wiki`。剩余 5 项 `test_real_evidence_artifacts.py` 历史报告原件/revision 缺失、1 项 `test_research_links.py::test_wiki_research_link_http_confirm_and_nbs_catalog` 因缺少显式 `STOCK_RESEARCH_GBRAIN_ROOT` 返回 Wiki catalog 503；未改范围外配置或伪造历史原件。定向检查 **86 通过、0 失败**。DSH `pnpm test` **186 通过、0 失败**；Vibe desktop `npm test` **289 通过、0 失败**；两个仓库 `git diff --check` 通过。日志：`/private/tmp/window3-strict-final.log`、`window3-strict-focused.log`、`window3-strict-dsh.log`、`window3-strict-desktop.log`。未额外构建或重启，DSH 构建仅由授权的 `pnpm test` 脚本执行。

审查：只读审查指出非定期来源可准入、撤销候选状态不一致、网络失败的 pending 生命周期三项。前两项已补齐：Backend 限定 annual_report/quarterly_report（新增研报/新闻/用户材料拒绝测试），撤销脚本同步候选终态。补齐后定向检查 **53 通过、0 失败**，DSH **186 通过、0 失败**，Vibe **289 通过、0 失败**。经只读生命周期复核，网络失败的 retryable pending 随子会话清理是本 Task 前已存在的缺口，不属本轮新增回归；本轮只保证失败也尝试一次提交并如实记录结果，不承诺网络故障后自动续交。已补 mock 网络失败台账断言；持久化重试涉及生命周期、授权期限和幂等 Owner，应另行裁决。

准入测试使用真实 PostgreSQL 测试库；当前页面和 GBrain 读锁采用 fixture，DSH 模型结果/Backend 响应采用 mock，不能证明真实研究或页面发布验收。

第 2 条状态：已完成。新增 `backend/app/services/citation_numbers.py::strict_metric_source_match(value: object, unit: str, source: str) -> str | None`，Decimal 去千分位比较数值相等，不换算、不设容差；数字后 3 字符内识别金额、股数、百分比、电量、装机等单位类别及量级，不匹配返回 `unit_mismatch_in_source`，无相等数值返回 `value_not_in_source`，无单位数字通过返回 `None`。不跨表格单元格或换行借用单位，多个相等数字中有符合者即可通过。报告用 `number_match` / `citation_number_match` 保持原样。

入库在写 Evidence/Entity/Claim 前读取候选固定 parse revision 的完整 Markdown（存储层核对 hash），仅拼接已校验的 supports 块调用公共严格函数，不使用截断预览或整份报告中其他块的数字。新增独立测试 `tests/test_strict_metric_source_match.py`（29 项）及入库层 4 项回归：股数误作金额、同类不同量级、数值只在其他块出现均拒绝且无事实/证据写入，无单位表格接受。最终相关检查 `tests/test_strict_metric_source_match.py tests/test_agent_extractions.py tests/test_company_facts.py tests/wiki/test_business_compiler.py -q` 为 **86 通过、0 失败**，含真实 PostgreSQL 入库测试。

严格入口增量审查：补齐 `元/吨`、`元/10股` 复合单位不得截为 `元`、人数/美元不得当作人民币金额，以及当前 `metric_units` 既有单位别名的归一化；别名只统一名称，不换算数值。未按审查建议把裸数放行收紧为“仅表格”，因为冻结条款明确数字后没有单位时不按此条拦截。二次复核的数字分母问题已补回归并通过上述最终 86 项检查。单位识别是确定性词表（含本 Task 类别及上述反例），不是对全部自然语言单位/指标语义的验证；不证明报告期和口径一致，仍需用户抽查。

停止条件：早期曾因严格接口未就绪暂停第 2 条，用户转交实现责任后已解除等待；本轮未触发新的停止条件。三家公司真实评估未执行，未判定耗时或人工一致性门槛。没有重启、提交、push、读取/打印凭据或执行撤销脚本。

用户真实评估步骤（执行方不跑）：依次对中际旭创、万华化学、长江电力各发起一次公司研究，等“知识整理”结束；记录公司、任务 ID、暂存条数、入库条数、拒绝原因分布、知识整理开始/结束时间与耗时、工具调用次数。每家公司人工抽查 5 条入库数据（不足 5 条则全查并记录样本不足），逐条记录指标名、数值、报告期、单位、口径、依据定位及是否一致；确认“经营”块及“查看依据”读回，缺项仍保留在关注清单。

停止规则：任一家整理耗时超过 150 秒，或任一抽查事实与原文不符，立即停下并记录耗时/调用次数或不符样例，由用户裁决，不放宽规则、不调整时限。

**2026-09-26 Claude 审阅**：严格入口与第 1–4 条实现符合 Task。复跑：`tests/test_strict_metric_source_match.py` 与 `tests/test_agent_extractions.py` 共 63 passed；Stock DSH `pnpm test` 186/0。用今天中际旭创的真实错误回放：把“总股本 1,111,118,334 股”当作现金分红（单位“元”）返回 `unit_mismatch_in_source`；照原文写“股”通过；原文“210.50 亿元”写成“元”返回 `unit_mismatch_in_source`；表格无单位数字通过。执行方额外加固的几处（只接受定期报告作为来源、复合单位、人数与美元）合理，予以保留。撤销脚本未执行。三家公司的真实评估留给用户。
