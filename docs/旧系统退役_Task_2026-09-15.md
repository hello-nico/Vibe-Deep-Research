# 提交前旧系统退役

## 权威与目标

- 决定：[Human Checklist](../human-checklist.md#提交前旧系统退役2026-09-15)。
- 保留 M8.5/M8.6 现用链，退出旧资料库、六阶段研究与隐藏旧产品能力。
- Backend 拥有研究数据；DSH 拥有会话执行；Client 保留选择与偏好。

## 基线

- 两端 HEAD、工作树状态与修改补丁固定在 `.local/retirement-baseline/`，不提交。
- 不回退两端已有修改；Backend 持久数据保持原样。
- 旧非 Backend 数据不需要兼容读取或迁移，不删除现用选择与会话。

## 实施顺序

1. 固定基线并核查现用页面、服务和工具消费者。
2. 删除 legacy 页面、入口、旧研究/报告/对话/任务 HTTP 与 MCP 执行链。
3. 删除失去消费者的状态机、旧资料库、专用适配器与测试；保留真正共用能力。
4. 同步 README、skills、运行入口及产品文案，避免恢复旧流程。
5. 类型检查、保留能力测试、接口退役与现用运行检查；交用户验收。

## 验证

- 旧 HTTP 入口不能启动研究或读写旧资料，旧 MCP 不注册研究启动工具。
- 现用页面路由、DSH 接缝、页面助手、选择偏好、Backend 资料和研究读取仍有效。
- 当前消费者不引用删除文件；编译和相关测试通过。
- 真实服务负向请求覆盖旧入口，现用服务健康与只读路径回读。
- 用户仍负责最终视觉和研究效果验收；不以静态检查替代。

## Out of Scope

- M9 新资料库、跨分类映射、Topic 新复测能力及存储迁移。
- Backend 数据清理、历史重写、公开发布。
- 用户验收前提交。

## Stop Conditions

- 删除目标仍是现用功能的唯一实现：先明确消费者和保留能力，再继续。
- 发现必须改变现用产品语义或 Backend 数据才能退役：保留证据，请用户判断。

## 执行结果

- 已固定两端 HEAD、已跟踪修改的 binary patch 和完整状态清单；未跟踪文件仅登记路径，未制作独立内容备份。原有 M8.5/M8.6 修改继续保留。
- 删除旧页面/路由、旧资料库与召回、六阶段状态机、旧对话/任务执行器及其专用测试；同步当前 API、MCP、客户端适配器、依赖、启动检查、README、AGENTS 与官网文案。
- 保留当前 DSH 原生会话、页面助手、Backend 资料/Wiki/Topic/记录链，以及页面取数、Client SQLite、鉴权、数据快照校验与取消机制。
- `orchestrator`: `VRA_PYTHON=<repo>/.venv/bin/python npm test`：132 项，131 通过、1 项 Windows ACL 专属检查跳过、0 失败；两端 `npx tsc --noEmit` 通过。
- `desktop`: 76 项测试通过，构建通过；导入可达性核对未发现孤立源码，`git diff --check` 通过。审阅重点覆盖 API 鉴权、保留页面消费者、删除范围及启动依赖。
- 新增 `orchestrator/test/retirement.test.ts`：临时 SQLite 关闭连接后重新打开并读回选择；真实 stdio MCP 仅注册数据工具，拒绝旧研究工具。该测试使用临时目录与测试 token，不写用户数据；最终单独复跑 2/2 通过。
- 重启源码工作台后，真实同源健康、Client 自选/研究名单/偏好 GET 返回 200；旧研究、报告、聊天、任务、工具、辩论 POST 与旧报告/运行 GET 返回 404。
- 浏览器实测：DSH 首页保留历史对话；41 行业入口、Backend 上传文档、2 个议题与 6 条记录可读；记录全文可展开；空研究名单未变成全部公司；页面助手可打开；旧 `/research/legacy` 显示不存在。
- Backend 的 tracked diff SHA-256 与状态清单均与基线一致；未执行 Backend 数据写入或清理。未删除旧本地数据目录。
- 用户确认当前只交付 Web；旧 macOS 打包退出产品范围，不是遗留缺口，不列入 M9。旧构建入口已拒绝执行，Web 启动正常。
- 工作台留在 `http://127.0.0.1:5930/`。未提交、未 push。尚待用户验收真实模型研究、引用/图表、资料上传与 Topic 写入闭环；本轮只读浏览与临时库测试不替代这些验收。

## 验收修复：行情图表缺失

- 真实「华能国际今日行情查询」已有 243 条日线成果；DSH 在 JSON 后追加 spill 文件提示，导致前端整段 JSON 解析失败、图表节点未创建。
- 生成工具通过 DSH `presentationMeta` 单独传递成果引用；前端优先读元数据，并兼容历史结果首字段引用。读取成果仍不生成新图表，错误结果不展示。
- 此项调整涉及 Stock-Research 的 `dsh/src/result-tools.mjs` 与测试，故上方 Backend 与基线一致的结论仅对应退役清理结束时。
- 图表投影测试 6/6、Stock DSH 成果工具测试 4/4 通过；两端相关构建及产品 `git diff --check` 通过。测试覆盖长文本尾注、截断、元数据和非引用文本。
- 刷新真实历史会话，已看到 K 线、均线与成交量；未重新取行情或启动模型。新生成工具的元数据经过工具测试，未另开真实模型回合验证。
- 退役前已有图表注册；相关源码当时未跟踪，基线仅登记路径，不能据此断言清理前后正文完全一致。确认根因是长结果与展示引用耦合，未发现图表入口被删除。

## 累计改动提交准备（2026-09-16）

- 用户要求当前范围收尾并准备提交；不扩展 M9。当前阶段形成清单和检查结果，不执行 commit/push。
- 拟分两笔、先研究核心后产品：`feat(research): persist research results and isolate DSH maintenance`；`refactor(workbench): retire legacy execution and consolidate DSH research UI`。
- 清单为 `.local/commit-preparation-2026-09-16/{research,product}-manifest.json`，逐文件记录状态及 SHA-256；对应 `*-files.txt` 为完整拟纳入路径。产品 307 文件（39 新增、174 删除），研究核心 119 文件（58 新增）。大量删除来自旧执行链及专用测试，未删除用户数据。
- 排除：两端 `artifacts/` 本地设计稿与真实轨迹/数据回执、行业草稿 ZIP、临时截图及 `workspace/research/product-notes/note-digest.json`。未删除这些文件；本地证据链接不属于发布附件。未用 `git add .`。
- 候选文件私钥/常见 token 规则扫描未命中，非完整公开发布安全审计；未进行 push，完整历史与发布附件扫描仍按公开发布协议另验。
- 产品构建/类型检查通过；前端 88/88；本机服务使用项目 `.venv` 的 `VRA_PYTHON` 后 131 通过、1 项 Windows ACL 跳过。默认系统 Python 首次运行有两项环境失败，未改测试规避。
- Stock DSH 69/69、Pi 15/15、冻结 hash 检查通过。
- Backend 首次运行暴露真实资料卷路径未按现用 Compose 配置，以及旧 Provider 引用测试预期过时。已从容器 Mounts 确认宿主文档卷，按该路径重跑；测试改为验证缺快照引用被拒绝、Topic revision/判断不变，未改运行校验。
- Backend 最终全量：641 通过、2 跳过、2 项依赖弃用警告；包含本机真实五份原件的 hash、解析版本与页数检查。
- 研究核心 README 明确区分 Pi 开发入口与产品 DSH 的 Topic 权限，避免旧入口说明覆盖本轮收敛。
- 保留验收边界：新模型是否减少重复出图、主会话与独立复核的自然研究表现，以及最终视觉接受，不能由测试数量替代。
- 提交准备独立复核无新阻塞：迁移链 0008→0009→0010 连续且启动等待 migrate，新 API 已注册；存活前端相对 import 无缺失，旧执行器未被启动链引用；Provider 测试预期修正成立。此次为静态/聚焦复核，非全量业务验收。
