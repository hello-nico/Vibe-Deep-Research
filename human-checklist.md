# Human Checklist

## 产品核心协议：交互维护与 Topic 判断边界（2026-09-22）

用户再次明确以下协议。本节覆盖下文及旧 Task 中冲突的入口、触发与判断维护描述；记录产品决定，不代表已实施，不扩大为自动发布授权。

### 同日补充：统一授权与 Topic 容器（以后述补充为准）

- 用户确认按一次可理解的维护意图组织一份变更提案，展示具体项目，允许选择后统一执行；各项保留独立执行结果与审计。无新增不弹框，部分失败不得显示整批成功，不为每个实体/关系重复弹窗。
- 硬关系明确为核心对象之间的直接关系，不把间接路径、推导关联或一般证据引用当作硬关系。类型沿 Stock `docs/standard.md`：公司间隶属、股权、供应/客户，公司到 NBS 行业归属，NBS 行业之间直接上下游；公司/申万/NBS 上市口径映射为系统维护的分类映射。原端点闭集、谓词方向、证据与双时间规则保留；Profile、Topic 与资料引用不因此成为硬关系端点。
- 核心对象维护统一包括新建对象、更新资料时间线、更新实体与关系等。所有维护写入都须用户二次确认：展示目标、拟做的具体变更与依据，用户通过 option 组件授权后执行，明确展示实际写入结果，审计记录关联提案、用户选择及执行回执。此条替代下文及旧记录中“正常维护无需逐项确认”的表述；Jev 判断或 Agent 提议均不是写入授权。
- Agent 模式每次交互都判断是否有维护需要；Jev 由 Backend 提供服务，重点判断是否需要新建对象、更新关系等。页面按职责配置实际工具，Agent 获取上下文、工具返回与 Jev 判断后，给出具体维护选项，确认后沿正式接口写入。不需要维护时不强造写入提案。实体与硬关系同样遵守确认、准入与审计，不设自动写入例外。
- 确定性刷新按钮是独立路径：由确定性 Backend 工具更新数据/资料时间线，不需要 Agent 或 Jev 判断。它仍遵循上述用户授权与动作记录原则，确认交互不必经过 Agent；不能夹带研究判断更新。
- Wiki 的研究判断、主张及成立/失效条件仍只在「我的研究」绑定 Topic 的 session 中，由用户与 Agent 共同研究后修订。Topic Agent 读取对象更新及相对上次依据的 diff，结合原文更新认识，再提出判断写入；用户确认后保存。其他入口的维护判断不得越权改主张。
- Theme Wiki 与 Comparison Wiki 的后续产品方向收敛为主题研究、对比研究 skills，产出直接组织在 Topic 容器中，不继续扩展为独立 Wiki 类型。skills 在议题场景下读取对象、差异与证据，辅助分析和形成判断；原件、核心 Wiki、证据与关系仍沿原 Owner 保存。此决定不授权删除或迁移已有 Theme/Comparison 数据，存量处理在原 Task 单独明确。
- 复用 DSH 现成的用户选项能力，而非自建聊天确认组件。参考仓已存在 `packages/interaction/tool-ask-user` 的 `ask_user_question`，支持选项及多选；这是交互能力，不代表产品写入授权绑定与审计已实现。参考实现不允许运行中的受委派子 Agent 直接询问用户，因此后台结果须交回可承接用户交互的主会话/产品入口，不靠子 Agent 自行确认。产品实际安装版本与接线待实施前核实。
- 下一步在原 Task 形成“页面/角色 → 读取与候选工具 → Jev 判断 → option 授权 → 正式写入与审计”的能力矩阵，标出缺少的工具；保持确定性刷新与 Agent 交互两条触发路径共用正式数据服务。本节下方历史表述如与本补充冲突，以本补充为准。

- 每次 Agent 模式交互都要判断本轮是否需要关系链接、内容更新或新建对象，并按正式契约处理；不是仅在用户明确说“保存”时才判断，也不是每轮必然写入。Ask 仍不写入。这里的维护判断不等于研究主张的更新。
- 原件、解析、数据与资料时间线可以提前通过确定性 Backend 工具更新。公告场景“更新到对应公司资料里”指更新资料时间线，Agent 的职责是调用工具完成更新；不得据此附带触发经营解释、研究主张、判断或成立/失效条件的修订。
- **只有「我的研究」中绑定具体 Topic 的 session，才可在用户与 Agent 共同研究判断时更新 Wiki 的研究判断、主张及条件。** 其他 Agent 入口及其后台维护不得绕过这一边界；数据刷新、关联维护与对象创建不授予判断修订权限。Topic 场景资格也不等于已完成正式校验或发布。
- 有值得持续研究的问题时，Agent 应主动提出创建 Topic，并直接提供可选择的 option 组件，由用户选择采用；不得只在回答正文里说“可以创建 Topic”，也不得把浏览快讯分类等同于用户采用。Topic 是长期问题与逻辑知识空间，绑定 DSH 会话及已有核心对象、资料、关系和研究状态，不复制原件或另建会话执行器。
- 关系沿 Stock-Research `docs/standard.md` 的硬关系/软关系定义及后续 Human Checklist 决定执行，不另造分类：硬关系包括隶属、股权、供应/客户、公司行业归属、行业上下游；上市口径映射由系统维护。硬关系端点、方向、证据、时间与准入受原契约约束；产品列出的类型不等于当前抽取器全部支持。研究 Link 可连接核心与非核心对象，经代码校验维护，不升级为硬关系；Theme、Comparison 与公司页研究关联沿各自对象契约。旧文档中 Topic 为当轮记忆、活跃池可淘汰等已被长期议题决定替代。
- Jev 由 Backend 服务承接，为每次 Agent 交互的维护判断提供规范化分类与关系判定，DSH 经业务接口使用；不能把维护范围缩成仅显式提交候选才处理。Jev 与后台 subagent 均不得在非 Topic session 中更新研究主张、判断及条件。具体调用粒度、类型到服务的映射和生命周期在原 Task 收敛，不新增平行执行或存储。
- 公司/行业 Wiki 的资料时间线支持确定性刷新，行业更新通过 Backend 稳定来源接口实现；产业研究的 Industry Profile 引导注意力，围绕具体问题的动态关注与判断组织在 Topic 中。落实时同步修订 M9.7、独立 Topic 对话及相关消费者契约中的冲突条款，不能直接按旧 Task 执行。

## 当前工作重点（2026-09-22）

用户决定：治理对齐的遗漏先登记到[原 Task §9](docs/治理对齐修复_Task_2026-09-21.md)，当前聚焦 Jev、真实研究闭环与“我的研究”。此决定调整原先“治理全部收尾后再推进 M9.7”的顺序，不代表治理已验收完成；涉及证据身份、版本与持久化的具体依赖仍按原契约处理。沿既有 M9.7 和独立 Topic 对话 Task 推进，不另建平行执行或知识存储。

## 真实阅读与数据引用优化（2026-09-21）

- Wiki 读取遵循 Backend 正式标识契约，明确支持已有中文行业 slug，不重命名现有对象来迁就读取正则。
- 单页 URL 阅读优先 Jina Reader，无需配置密钥；失败再用现有 Fetcher。不引入本地浏览器、Playwright 或浏览器服务。本机产品当前不以 SSRF 地址黑名单作为需求约束，允许收敛妨碍实际网络访问的 DNS 检查与固定 IP 连接机制。
- 新证据链接采用产品真实提供的标准 HTTP(S) 地址，保留底层证据身份、固定版本与旧会话引用读取；工具返回完整链接，模型原样引用。数据仍须可追溯，不能用纯文字来源名替代依据入口。
- 实施与运行证据归 [真实阅读与数据引用优化 Task](docs/真实阅读与数据引用优化_Task_2026-09-21.md)。本轮仅编写文档，交其他 Agent 实施，不代表验收完成。

## 启动前端口回收（2026-09-21）

用户确认：`scripts/start` 增加 pre-start 阶段，先回收上一轮遗留的进程再启动，不再要求人工 kill 端口占用者。约束如下，`scripts/start.ps1` 与 POSIX 启动器同语义。

- 只回收可确认属于本仓库的进程（命令行含本仓库根路径），覆盖启动器自己管理的 8765（本机 API）与 5930（Vite）。默认先 SIGTERM，宽限期后升级 SIGKILL，仍不释放就明确报错；不把失败伪装成启动成功。
- 无法确认归属的占用者默认不杀，按原 M2 边界报错并给出 PID 与命令行，由用户裁决；`--reclaim-any` 才允许一起结束。这条不改变「启动器不自动连接占用者」的既有决定。
- 不属于启动器职责的端口（如 DSH 5941、Backend）不在回收范围内。

## 问助手 Ask 即答与 Agent 深查（2026-09-21）

用户基于真实使用确认：问助手的典型用法是 `@` 一个对象、问一个具体问题；读/写差别用户几乎感受不到，用户能感受到的差别是"快不快、查多深"。大盘行情页问"解释下当前的行情"时，页面已显示指数点位、涨跌幅、涨跌家数、市场情绪，模型却未收到这些数值，转而进行二十余次行情读取、区间计算与外部搜索，耗时约一分钟。用户认为这是体验问题，不是模型错误。本节替代 2026-09-20「核心对象」一节中 Ask 的定义及 [M9.8](docs/问助手双模式与Agent面板_Task_M9.8_2026-09-20.md) §2「不注入页面快照」、§3「Ask 只读工具集」的相应条款；读/写护栏保留但不再是模式的定义。

- Ask = 即答：只使用发送时固定的上下文直接回答，不注册任何工具，不写入；上下文不够就说明缺口并提示可切 Agent，不自行取数、计算或搜索。
- Agent = 深查：同样先用固定上下文，只针对问题所需的缺口调用该角色已授权工具；允许写入不等于每次写入，也不等于默认全面研究。默认范围是"这个问题、这个对象、这个时点"，全面研究属于「我的研究」与「深度对话」。
- 固定上下文由两部分组成并分开标注：页面当前已加载并展示的数据（含来源与取数时点、业务日，不新增请求）；`@` 对象在发送后由宿主按其正式读取能力确定性读取的结果（含 asOf / 版本 hash，绑定本轮依据），不寄望模型自行调用工具完成读取。页面未加载或无读取契约的内容明确写为缺口。页面快照是本轮输入，不进入证据层，不生成成果。
- 大盘行情已是独立 DSH 角色。该角色不以 `observe_market` 与确定性计算器（`calculate_metrics` / `calculate_market_result` / `generate_market_result`）作为默认能力，角色提示词不再指示使用它们；Agent 工具按"解释指数与市场表现、观察时点与口径"的定位从现有工具中组合选取，不继承为研究任务写的多窗口展开纪律。历史区间与跨指数比较问题在大盘助手内的归属待定，见 todo。
- 五个角色的 Ask 一起切换为免工具，前提是 `@` 对象的宿主侧读取覆盖现有六类读取身份（market / wiki / topic / document / url / profile）；不接受先切大盘、其余角色 Ask 仍带工具的过渡态作为验收。
- 轨迹面板按调用参数区分同名工具步骤，不再把不同计算统一显示为裸工具名。
- 验收以真实轨迹为准：Ask 会话零工具调用且回答引用快照中的具体数值；Agent 对同一问题的调用次数明显下降；实际生效的 system prompt 与工具注册清单按角色核对。不提交、不 push、不重启现用服务。

设计原则（2026-09-21 review 后用户澄清）：问助手是独立设计的产品能力，Ask / Agent 只是问助手的模式；深度对话没有模式，也不承担问助手角色。问助手各角色的工具集与 system prompt 独立定义，不从深度对话工具集派生子集、不从研究纪律文本删段得到、不在研究纪律之上叠加模式段。因此：`assistantBindingForPage` 不得回退到 `deep_research` 插件，DSH 侧 `assistantFor` / `createResearchHost` 不得把 Ask/Agent 映射为 `deep_research`；没有定义助手角色的页面不开问助手会话。当前落入回退的页面中，用户已定：我的资料（含资料阅读）不要问助手；我的研究需要问助手，但按同一原则单独设计，细节未定，不在 M9.9 内做；深度对话 Home 与 Signals 不绑定。先把五个页面角色做出效果。实现形态与首轮必修项见 [M9.9](docs/问助手即答与深查边界_Task_M9.9_2026-09-21.md) §4 / §7b。

## 研究内核推进顺序（2026-09-21）

用户确认内核按 09-20「核心对象、数据更新与按需判断」一节理解：对象 + 版本 + 数据先更新/判断按需 + 研究后 hook。推进顺序：

1. M9.9 闭合（宿主绑定固定上下文、消费者登记表、问助手独立模块）。这两块是内核的基础件；Topic Agent 的基础就是 M9.9 这次拆分，不是新机制。
2. [治理对齐修复](docs/治理对齐修复_Task_2026-09-21.md)：在 M9.9 闭合后、M9.7 之前整体实施，不与 M9.9 并行（用户 2026-09-21 决定，替代同日"部分可并行"的安排）。
3. M9.7 按三类分别启动：先做服务已就绪的疑点复核与实体关系，连续性按现有 Topic 契约做初版，不等版本统一。
4. 并行起一份对象版本与 diff 的设计决策（一页决策，不是 Task）：一次 commit 的对象范围、Wiki `input_hash` / Document `parse_revision_id+sha256` / Topic `revision` 三套口径如何映射到统一标识、`basis_refs` 携带版本的格式、diff 的最小读取接口。定稿后 Jev 连续性升级为版本差判断。
5. Topic Agent（[我的研究独立 Topic 对话](docs/我的研究独立Topic对话_Task_2026-09-18.md)）后置：实现为登记表里 `my_research` 一条独立的工具集与 prompt + 宿主绑定 Topic 快照与 diff，复用问助手模块的绑定与快照机制。

版本与 diff 目前没有 Owner Task，是内核里唯一无归属的一块；它不阻塞 M9.7 的前两类。

同日用户看过 Vibe 侧目录结构后确认以下整理时机（记录，未实施）：
- 随 M9.9 闭合一起落：`lib/` 中旧的 `assistant*` 与 `pageAssistantObjects` 删旧留新；`FinanceAiDock` 与 `core/ai/pageContext` 迁入 `assistant/`；`core/ai/AiDock` / `AiMessages` / `useAiChat` 死代码删除（此前 todo 标"未裁决"，现已定）。
- 放治理对齐 Task：`lib/backend.ts`（实为 orchestrator 客户端）改名，与 `research.ts`（Stock Backend）区分；顶层遗留目录清理（治理 Task §1.F）。
- Topic Agent 开工前作为其准备包：`lib/` 按客户端 / 领域对象 / 展示格式化 / 偏好拆目录；`components/ui/` 只留无业务原语；`dsh/client.tsx` 拆 apply 与研究会话；是否取消 `verticals/` 单纵向抽象一并评估。

## 治理对齐审计处置（2026-09-21）

以当前代码对照本文件做了一轮只读审计（orchestrator / 前端 / DSH 插件 / Backend 四区域）。用户对审计发现的裁决如下；本节只记录决策，修复交 Task 实施。

- Wiki 判断维护的现行口径是"草案待授权"：后台只 `POST /wiki/page-drafts/research`，由用户明确发布；下文 M9.3 追加决定中"后台可自动更新 Wiki 和已有 Topic 的研究内容、而非每次人工批准"的表述被替代，Topic 仍只产生候选。`awaiting_authorization` 的状态归属（Backend 只返回 `draft_token` + 过期时间，"待授权"由 DSH 插件合成，草案存 tmp 会过期）用户尚未想清，待后续讨论，不在本轮决定。
- 六阶段编排退役已确认为现行状态；下文 2026-09-09「保留旧 `/research` 六阶段编排」被 2026-09-15 退役决定替代，代码已无该路径。
- Backend 的长期定位是数据与工具实现的 Provider，信任本机调用方，不做调用方身份/角色校验；角色与 Ask/Agent 权限由 DSH server 端插件执行，DSH web 端只负责展示。此模型为长期设计，不视为缺陷；事实准入与发布仍走 hook token fail-closed。
- 以下为必须修复项：技术身份（版本 hash、revision、result/topic 内部 ID）不得出现在用户界面，包括轨迹依据行、chip 提示与 URL 查询参数；对外来源名统一"同花顺"，在 Backend 输出源头修正（`wiki/markdown_renderer.py`），删除前端用正则把"扶摇"替换为"同花顺"的补丁——不允许前端给 Backend 输出打补丁，也不允许用正则静默实现命名纪律；DSH 子进程不得继承完整 `process.env`，改为与取数进程同一套白名单机制；`.pnpm-store/` 等本地缓存写入 `.gitignore`，包管理器只保留一种并声明。
- 问助手对话的「存入沉淀」入口需补回，落在 M9.9 已调整的问助手位置，连同对应记录一起作为 M9.9 后续包处理；保存只写 Backend 记录，不触发研究或 Topic 自动关联。
- 四个承担产品 UI/行为的 DSH runtime patch（conversation 文案、model-selection 搜索、settings-models、session-controller retain）经逐个核对 rc.1 扩展点：conversation 无 headline/placeholder slot 且 locale namespace 单占位；model-selection 仅 composer 座位可经 `conversation.input.model` 替换，`/model` 弹窗无 slot；settings-models 主编辑器无细粒度 slot；`retainSubagent` 为产品只读过程面板硬依赖且无替代。用户决定：只要整体系统运行正常，patch 留着，不做迁移。补充要求：`desktop/dsh/runtime/README.md` 的 patch 表补"类型"与"移除条件"两列，把其中的纯 bug fix（session-controller 的 JSON 安全与 cwd 检查、llm 401/403 拆分、chat/trajectory 错误码映射）与产品功能/文案区分；重评时点为下次 DSH 版本升级。是否向上游提交 bug fix 由用户另行决定。

修复 Task 见 [治理对齐修复](docs/治理对齐修复_Task_2026-09-21.md)。

## 近期实施优先级（2026-09-20）

用户确认优先完成 M9.8 问助手与 M9.7 Jev 两个 Task：前者围绕对象引用、上下文及 Ask/Agent 工具集合，后者接入研究后 hook 的结构化判断与 Backend 维护。先打通对象读取与维护入口，再在同一研究闭环接入 Jev，不建设两套执行链。Topic 对象逻辑及「我的研究」设计后续集中优化；本期可复用已有 Topic 读取、引用与授权维护能力，不扩展其创建规则、生命周期或重做 UI，也不把该重构作为两个 Task 的前置条件。当前对话继续负责具体计划，沿此前分工交实施 Agent 落地。

## 核心对象、数据更新与按需判断（2026-09-20）

用户要求以业务对象及其生命周期设计 M9.7/M9.8，而非以页面组件、import 或聊天容器划分功能。本节记录最新确认，覆盖此前冲突的整页上下文、Chat 命名和更新触发描述；本轮仅记录设计，未实施。

- 公司 Wiki 与行业 Wiki 是身份有客观标准的一类核心对象：公司对应上市公司，行业对应固定 41 行业。行业 Wiki 已在行业研究准备，不设计日常新建行业流程；公司 Wiki 缺页的新建沿研究问题结束后的 hook，由后台 subagent 处理。不得把每次打开页面或引用公司变成新建知识。
- 数据与资料本身也是对象。快信息（新闻、自媒体等）默认只读取分析，不积累入库；慢信息（公司财报、研报、公告等）需要作为可追溯资料入库。来源类别决定保存策略，不代表研报观点或资料全部内容自动成为已核验事实。用户上传的 Document 仍沿既有资料库契约保存。
- Backend 提供统一 Fetcher 抽象：输入 URL，获取正文及来源信息，形成 Document 对象；具体 Provider、网页或 PDF 获取差异在内部处理。对象形成与持久入库区分，快信息不因被读取而自动积累。上传文件与 URL 来源使用同一 Document 对象语义，Wiki/Topic 引用资料及证据，不各自拥有原件副本。
- 数据可先更新：原件、解析、指标和资料时间线属于数据更新。将多份公告解释为“回购计划 → 首次实施 → 完成”、判断影响与事件接续，属于判断更新，不随资料到达自动执行。判断在用户与 Agent 研究具体问题、Topic、Wiki 时按需更新；保留既有判断及其依据，不做全库主动重写。
- 变化表达采用 Git commit / diff 的设计方向：以 commit ID 标识对象版本，支持追溯、比较和回滚；Agent 应能读取“上次研究所依据版本 → 当前版本”的具体新增、修改及移除差异，而非只收到“存在新材料”。差异须能回到对象及对应证据，上次判断依据的版本不因数据刷新而移动；首次研究无基线时读取当前内容。diff 辅助定位变化，不替代所需完整上下文，也不自动判定旧结论失效。
- commit 版本统一是待设计落地的方向，不是当前能力声明。本次未授权替换现有 revision/hash、迁移数据库或对产品数据执行 Git 回滚。实施方案须明确一次 commit 的对象范围、原件/解析/证据绑定及恢复语义；回滚不能抹掉历史或使既有引用失效，不将源码仓库提交自动等同于知识版本。
- 模式统一为 Ask / Agent，默认 Ask。Ask 是颗粒度更细的只读问助手，支持引用对象并读取必要上下文；Agent 是相关核心对象的创建、读取、更新与关联维护模式，不删除内容。正常维护不要求用户逐项重复说“保存”；新 Topic 的建议与采用仍沿既有对象创建规则。实际激活工具集合落实模式边界，后台 hook 同样遵守，不从 Ask 绕路写入知识。（2026-09-21：Ask“读取必要上下文”的定义由顶部「问助手 Ask 即答与 Agent 深查」一节替代——Ask 不注册工具，上下文由宿主在发送时固定；Agent 与写入护栏的表述不变。）
- DSH 主 Agent 围绕对象取证和分析，调用确定性 Backend 工具持续维护；研究后 hook 中 Jev 补充结构化判断、实体关系判定与研究接续，结果由代码映射到正式服务。需要补查、开放式候选发现或文字修订时交 subagent。Jev 不承担研究前材料裁剪，也不因单纯数据更新就自动重写判断。
- 页面设计从“对象、可引用身份、上下文、允许动作、变化与回执”出发：表格行、卡片、图表只是对象或对象集合的展示。引用须保留具体对象与观察范围、来源和版本；组件名称及页面位置不是对象身份。

相关计划：[M9.7](docs/Jev知识关系导航试验_Task_M9.7_2026-09-18.md)、[M9.8](docs/问助手双模式与Agent面板_Task_M9.8_2026-09-20.md)。后续实施前按本节更新 Task，尤其 M9.8 的旧 Chat/整页快照方案不能直接作为新需求实施依据。

## 我的研究栏目样式（2026-09-20）

用户确认「我的研究」的议题、记录、任务、记忆页内切换对齐资讯雷达页内 Tab：带图标、无描边、选中浅橙底。栏目 Tab 留在面板外；搜索、研究中/已归档、发起与列表收进同一块 glow 面板，条目用面板内行而不是再套小卡。不增加侧栏子栏目、不改独立路由、不加「集成」徽章，也不改成资料总览那张表。资讯雷达页内 Tab 抽成共用控件，侧栏子导航保持原样。

2026-09-20：行业 Wiki 的「研究页 / 图文报告」与个股研究对齐，放进工具栏同一行，不再单独占正文上一行。行业、产业列表页的搜索和入口按钮换到标题、副标题下一行，不再和说明挤在同一行。

## Jev 判断接入边界（2026-09-20）

用户重新确认 [M9.7 Task](docs/Jev知识关系导航试验_Task_M9.7_2026-09-18.md) 为「分析后判断与知识维护」。本节替代此前研究前候选排序的记录。本轮只改文档，未实施、未调用 Jev、未改业务行为。

以广义贝叶斯分类器理解 Jev：基于充分研究上下文提出明确分类问题，返回结构化判断。主 DSH Agent 负责取证、分析和回答；分析完成后的固定判断由现有后台交接 Owner 调度，不新增用户可见 Jev Agent、主模型常驻工具或第二执行器。

范围包含三类：为后台维护识别疑点和边界冲突、帮助知识沉淀保持研究连续性、承接实体与关系整理中的分类和消歧。复核不逐句盖正确章；候选发现与分类分开，未知实体或上下文不足时补查，不硬套已有选项。三类分别验证，既有 Topic 接续真实验收仍须完成，不以此将连续性排除出设计。

明确撤销研究前材料筛选、排序与上下文剪枝。缺乏充分上下文时不能判断哪些材料值得读；降低优先级也可能造成漏读。本 Task 不改变主线阅读范围、检索顺序或用户 @ 引用。

结构化判断由代码直接映射到 Backend 正式服务处理，无需每次再由主模型或生成式 subagent 转述和批准。Backend 保留证据绑定、身份、权限、幂等、版本与写入校验；需要补证、开放式候选发现或文本修订才转生成式 subagent。自动维护、新建 Topic 建议和采用沿用既有产品授权。

不以每次判断必然正确为记录前提。保留原证据、旧判断、修改原因和来源轮次，使未来用户或 Agent 能发现并修复错误。复用「我的研究」后台任务和只读过程，下一问不等待、不灌回主线、不混入普通对话历史。

凭据 Owner 为 Backend：由 Backend 持服务端 key 并提供内部接口，产品只调该接口。不复用用户全局 CLI 认证或全局 `AI_GATEWAY_API_KEY`，不在浏览器放密钥。向外部评估服务发送的数据在该内部接口一处收口并可审计；旁路对照使用真实会话，因此出境范围须在试验开始前确定并记录。

每次结果绑定输入版本与判断契约。接口失败、未检查、证据不足、无关联必须是不同状态；概率不授予写入权限，也不跨用途合并为统一可信度。按三类真实任务分别评估误报、漏报、错误关联/写入和后续纠正；校准集与验收集分离。不再使用压缩比例或无法在线获知的整批语义收益作为质量闸门。

## 资料优先与 Topic 对话边界（2026-09-18）

用户确认先完成 M9.6 我的资料，再推进[我的研究独立 Topic 对话](docs/我的研究独立Topic对话_Task_2026-09-18.md)。本轮仅整理后者文档。Topic 研究与深度对话分别拥有业务 system、实际工具集合和 Chat UI，共用 DSH 执行、持久化、轨迹与中止机制；不要求完整复用深度对话页面。深研 Topic 只读，我的研究承担明确入口创建及绑定后的维护；后台子 Agent 仍为独立任务。

## 分批提交（2026-09-18）

用户授权将近期工具协议审计修复、报告与后台任务、UI 调整及设计文档分批提交。此授权更新下文各次实施记录中的“未授权提交”；不包含 push、重启服务或真实交互验收。

## M9.6 我的资料与对话引用（2026-09-17）

用户要求调研主流资料管理并整理 [M9.6 Task](docs/资料管理与对话引用_Task_M9.6_2026-09-17.md)，本轮仅写文档。
用户上传 PDF、TXT、MD 等资料后可在「我的资料」看到、阅读并通过 @ 引用；深度对话附件上传也进入同一资料库。资料不限公司研报。URL、YouTube 链接导入为未来方向。
列表/预览、通用文件契约、双入口与版本化引用构成本期建议范围；具体接缝须按安装版 DSH 和 Backend 核实，不恢复旧资料库。M9.4 复杂交互继续暂停。

2026-09-18：用户要求实施该 Task。已按现用 DSH 扩展点与 Backend 文档契约接入双入口上传、列表预览、阅读和 @ 引用；未改原生 `+`、未恢复旧资料库。真模型问答与最终视觉仍由用户验收。
2026-09-20：用户确认「从我的资料移除」只拿掉资料库条目，不永久删除。原件、解析、证据和旧引用仍在；旧对话能打开，读不到时明确说已移除。同内容再上传会重新出现在「我的资料」。

## M9.5 公司 Wiki 生成与分类型交互报告（2026-09-17）

用户要求先分析并整理 [M9.5 Task](docs/公司Wiki生成与交互报告_Task_M9.5_2026-09-17.md)，交其他 Agent 实施。本轮只改文档。

- 缺少公司 Wiki 的详情应能点击「开始研究」生成公司 Wiki，并看见执行状态与回读结果。
- Company / Industry / Theme / Comparison 各自有 HTML 交互模板底稿保底，再使用 lieflat-charts 根据该 Wiki 的具体内容和数据生成；不是通用 Markdown 换皮，也不是仅四套静态排版。
- 参考 `/Users/apple/workspace/harness-books-v2` 的图文表达，样式贴合产品，保留真实数据、引用、缺口与明暗主题；最终视觉由用户验收。
- 用户已暂停 M9.4 的复杂交互；M9.5 不恢复画布。Task 中生成时机与最小接口是待实施核对的方案，不代表现有能力已验收。
- 2026-09-18：给人看的保底改用现有 Wiki 研究页；React 底稿只作为生成契约，不再单独展示。图文报告入口放到个股顶栏；生成版操作条收进报告；报告页不再挂历史对象成果列表。

## M9.4 多口径映射与研究画布（2026-09-16）

2026-09-17 状态校准：用户明确「这个交互先暂停，复杂了」，暂停复杂画布交互；保留下列方向记录，不启动实施。

用户确认将方向写入 [M9.4 Task](docs/多口径映射与研究画布_Task_M9.4_2026-09-16.md)，先记录范围，之后由用户梳理 UI；本轮不实施。

保留东财、申万及后续来源各自身份和统计口径，不默认统一为申万二级；通过可维护映射打通「看到市场变化 → 阅读产业机制 → 继续研究」。映射支持多对多和口径差异，不强行将所有产业归入 41 个工业行业。

以 Topic 研究画布为初步承载，支持加入/拖入核心对象、手动连线说明关系、选中对象继续研究与结果回挂。节点引用原对象；用户假设可以记录和纠正，保留来源与历史，不自动成为核验事实或公共分类映射。具体 UI、未选 Topic 的流程与布局保存待梳理；不因拖拽自动建 Topic，不另建知识副本或执行器。

视觉补充：用户要求 M9.4 样式贴合产品本身，效果尽量酷炫。沿现有视觉体系强化拖拽、连线、展开、聚焦与结果回挂的动效和空间感，兼顾可读性与流畅度；具体展示方案仍待用户梳理。

## M9.3 研究记忆与按需维护（2026-09-16）

用户确认以 [M9.3 Task](docs/研究记忆与按需维护_Task_M9.3_2026-09-16.md) 交其他 Agent 实施，原对话 Agent 后续 review。本轮仅编写交接文档，不修改业务代码、不重启、不提交。

四类 Wiki 的基础资料和 timeline 沿现有机制更新；研究结论与边界条件在主 Agent 实际研究触及时判断，成功问答结束后由独立后台子 Agent 根据本轮问题、回答、证据及用户反馈按需整理。不建设新证据驱动的全库主动重写，不阻塞主线或下一问。

新增用户画像 `soul.md` 与近期重要研究记忆 `memory.md` 的设计：画像保存稳定偏好，记忆保存持续问题、关键纠正与未决分歧，不记流水账或复制金融知识。用户可查看、纠正、删除；推断与明确表达分开，旧后台结果不得覆盖新纠正。参考 OpenMinis 与 hermes-agent 的有界记忆、修订与后台机制，不照搬助手人格或日记语义。

Agent 可判断 Wiki/Topic 的研究关联，结合当前问题、相关记忆与已有 Topic 识别持续主线并形成候选；次数不等于连续性，同公司不等于同问题。候选由用户采用后才正式创建/复用 Topic；事实关系准入及 Wiki 草案/发布权限不扩大。此项扩展 M9.1 的公司限定与无记忆整理范围，其余执行隔离与成果 Owner 延续；具体维护时机、交接、候选处置及 review 标准以本 Task 为准。

补充确认：充分利用 DSH UI 原生主动反馈与建议能力，尤其是可采用、编辑、忽略的 Topic 建议。建议在后台形成后可异步展示，与「我的研究」同源、持久回读及处置去重；不唤醒主模型续写、不阻塞下一问、不跨会话串入、不因展示建议自动创建正式 Topic。实施须核实安装版本的真实原生接口，不能仅用回答末尾纯文本邀约代替交互。

M9.3 实施后追加决定：用户认可允许记录尚可能被纠正的研究判断，后台可自动更新 Wiki 和已有 Topic 的研究内容；核心是证据优先、中立分析、当前版本与历史修订可追溯，而非每次人工批准才记录。用户负责采纳与行动，记录不等于采纳；原结论、条件、修订理由、来源和证据不得因纠错被抹去。此决定覆盖上文对研究内容每次更新的草案/确认发布限制，不扩大事实/硬关系准入或新 Topic 自动创建。按用户要求，待首轮实施完成后写回同一 Task，作为后续调整包，与原 Task 实现缺口分开 review。（2026-09-21：本段"后台可自动更新、不必每次人工批准"的表述被顶部「治理对齐审计处置」替代，现行口径为草案待授权；"记录不等于采纳、修订可追溯"的原则保留。）

## M9.2 DSH 接入收敛与产品护栏（2026-09-16）

用户确认将本轮 DSH 机制对齐审计与「你把你内部原理实现告诉我」真实回答暴露的护栏问题纳入 M9.2。先核实当前源码与原始轨迹，再拟写 [M9.2 Task](docs/DSH接入收敛与产品护栏_Task_M9.2_2026-09-16.md)，交其他 Agent 实施，原对话 Agent 后续 review。本轮只更新文档，不修改业务代码、重启服务、提交或 push。

范围收敛于产品信息与能力说明边界、主线/维护生命周期、遗留模型入口及 DSH 接入契约。保留正常方法说明；不将审计报告所有建议直接转成重构，不默认采用 jobs/storage 替换，不转移 M9.1 未完成验收。

## M9.1 后台知识沉淀与执行记录（2026-09-16）

用户认可当前主线问答的总体效果，确认把报告入库、Company Wiki 创建/维护、实体与关系候选提取交给 DSH 独立后台子 Agent，不阻塞主线回答、下一问或占用主线维护上下文；当前回答必需的证据仍由主线取得。后台执行记录在「我的研究」集中可见，可查看 DSH 原生轨迹、来源及 Backend 成果，不要求自动创建 Topic。Topic 仍由用户在「我的研究」主动发起；Wiki 发布与事实/关系准入沿现有权限和校验执行。

确认以此作为 M9.1 首个有界闭环，优先复用 DSH 子会话、目录、轨迹、持久化查询及用量组件；执行归 DSH，知识成果归 Backend，不在 Client 建平行执行器。历史可回看不包含重启自动恢复。此决定允许启动该范围的交接与设计，覆盖旧条目对 M9 的整体暂缓，但不授权全面 M9 重构；实施前先明确当前工具收敛基线，旧可靠性缺口仍归原 Task。

本轮仅更新 [M9.1 Task](docs/后台知识沉淀与执行记录_Task_M9.1_2026-09-16.md)，用户交其他 Agent 实施，原对话 Agent 后续 review，最终验收由用户负责。不包含提交或 push 授权。

## DSH 同类读取合并（2026-09-16）

用户确认实施工具同类读取合并及非必要常驻能力收敛，命名随最终边界一次完成。研究契约见 Stock-Research Human Checklist「DSH 同类读取合并与工具命名」；本轮沿工具与 Topic 审计 Task，产品同步新调用名并保留已有会话展示。不扩展 M9、不提交或 push，机械验证与真实模型效果分别报告。

## 累计改动收尾与提交准备（2026-09-16）

用户要求停止扩展当前范围，收尾多轮累计改动并准备提交。按产品工作台与研究核心整理文件、排除本地轨迹/运行数据/临时设计稿，执行完整可运行检查并列出拟提交说明。此轮不将尚未完成的真实模型与视觉验收标成通过，不启动 M9、不 push；“准备提交”阶段先形成可核查清单，尚不执行提交。执行记录沿用旧系统退役 Task。

## 工具与 Topic 生命周期系统审计（2026-09-15）

后续授权：用户要求依据多轮真实 sessions 作出初步判断并收敛工具、补齐协议，允许在现有 DSH/Backend 边界内实施。原审计阶段“不改业务代码”范围至此扩展；不新增事实准入、发布权限、存储迁移或历史数据清理。具体研究协议决定与实现记录见 Stock-Research Human Checklist「多会话工具收敛与协议补齐」和同一 Task。

用户要求并授权启动全面审计：盘点模型实际加载的全部工具，检查数量、职责重叠、场景可见性、协议完整性及 Backend 校验一致性，避免靠报错逐项猜参数；重新分析深度对话创建 Topic 的必要性、触发时机及后续动作。公司 Wiki 可创建的产品方向保留。先交付证据、逐工具处置建议与生命周期方案，不把本轮授权解释为已经批准取消自动 Topic 创建、修改发布权限或重构业务代码。现有研究边界在新方案确认前不变。执行与结果见 [工具与 Topic 审计 Task](docs/工具与Topic系统审计_Task_2026-09-15.md)，研究协议 Owner 仍为 Stock-Research。

## 提交前旧系统退役（2026-09-15）

用户补充确认：当前只交付 Web 端，旧 macOS 独立客户端不再需要；原生打包不属于收敛验收剩余项，也不列入 M9 待办。此决定覆盖此前保留 Mac 外壳路线的要求。

用户授权完成当前基线固定、旧产品入口与执行链退役，以及失去消费者的实现删除。范围包含旧资料库、六阶段研究、旧对话及同类遗留页面；保留现用 DSH、Backend 上传/Wiki/Topic/记录/成果和 Client 选择偏好。此决定覆盖此前保留六阶段入口的决定。

非 Backend 的旧资料和运行数据不要求迁移、归档或兼容回读，不为其保留旧执行与读取链。Backend 数据不删除；现用 Client 选择与 DSH 会话仍按现有职责保留。删除代码不要求顺带清空本机目录。

完成确定性验证和运行检查后，由用户参与收敛验收；验收通过后才提交，不 push。M9 新能力以提交后的基线继续设计实施。本轮执行见 [旧系统退役 Task](docs/旧系统退役_Task_2026-09-15.md)。

## M8.6 研究读取与证据可靠性（2026-09-15）

用户确认开始实施公司读取轻路径、LiteParse 升级验证及计算证据绑定；本轮只用 LiteParse，不引入增强解析器。研究语义与历史证据边界由 [Stock Human Checklist](../Stock-Research/docs/human-checklist.md#m86-公司读取解析与计算绑定2026-09-15) 拥有；执行及验收归现有 M8.6 Task。

- 2026-09-15：首次行情默认最近一年日线；分析窗口与保存范围分开，后续显示范围切换和分析复用原数据。需要更早区间时才扩展，均线保留前置计算数据。

## M8.6 真实轨迹修正（2026-09-15）

用户确认记录后实施：模型获得已有成果完整数据，分析复用同一快照，仅数据确实不足时补充；内部读取不重复出图。保留顺手积累，补齐工具参数与失败反馈；按问题需要使用核心对象，不将普通追问等同 Topic 跟踪。具体问题、处理和验收集中在 M8.6 Task；研究契约由 Stock Backend/DSH 拥有，不新增存储或发布权限，不提交或 push。

## 正式研究提示词语言与组织（2026-09-15）

用户确认正式研究提示词使用英文，回答仍用自然中文；按问题使用产业研究与核心对象，报告和维护细节按需提供。研究语义决定由 [Stock-Research Human Checklist](../Stock-Research/docs/human-checklist.md) 的“英文研究提示词与问题驱动研究”拥有；产品 persona 与其保持一致，不改变运行时或发布权限。

## M8.6 展示验收修正（2026-09-15）

用户要求修复报告返回与加载残留，保留原扫描动画；图表数据中的价格、均线按两位小数展示，沿用千分位与统一下拉样式。显示精度调整不修改已保存数据与计算输入，CSV 保留原始精度。

## 研究内容加载展示（2026-09-14）

用户确认并授权实施：研究内容加载采用通用 React 展示组件，通过阅读容器的 render slot 接入；Wiki 与后续产业研究可复用。保留扫描与章节轮换动画，不将动画标为真实完成进度；刷新明确显示正在更新财务与估值数据并保留正文。请求与状态归各数据模块，失败可重试，状态绑定当前对象；不建设插件注册中心。

## M8.5 Client 选择与 Backend 研究职责（2026-09-14）

用户确认：产品服务与 Stock-Research Backend 都运行在用户本机。本次按职责分工，不引入云端/用户端或公共/私人知识库划分，也不增加共享发布流程。

- Client 负责自选股、加入研究的公司及界面偏好，由产品本地服务统一读写 SQLite；页面读取选择后，将对象 ID 与筛选条件交给 Backend 查询数据和知识，不直接操作数据库。
- Backend 统一管理沉淀记录、Topic、研究关联、Company/Industry/Theme/Comparison Wiki、文档证据、实体与硬关系。沉淀记录可独立存在；进入研究后由 Agent 提议关联，用户确认后保存，不强制归入 Topic。
- DSH 继续拥有会话与 Agent 执行生命周期，不将会话迁入产品 SQLite；产品只保留必要的会话接续引用。
- “加入研究”是独立于自选和 Wiki 是否存在的 Client 选择。个股研究按该名单查询；取消选择不删除 Backend 知识。
- 本决定覆盖 M7/M8 中“Note 正文归产品 ledger、Backend 只存关联”的旧归属；SQLite 不保存 Topic、沉淀记录或研究关联。Backend 的具体存储与迁移方案在 M8.5 计划中评审，不因本决定自动完成迁移。

用户同时要求申万 Profile 的产品表达统一为“产业研究”，说明保留真实分类/资料来源；41 个标准行业仍称“行业研究”。“我的研究”优化布局、输入与记录阅读，复用现有组件，不展示内部标识。

2026-09-14 补充：用户决定先推进 M8.5，本轮由 Codex 补全 [M8.5 计划](docs/Client选择与研究数据归属_Task_M8.5_2026-09-14.md)，交其他 Agent 实施，Codex 后续审查与验收。本轮只修改文档，不执行代码修改、数据迁移或提交；未决的数据处置语义仍需按 Task 停止条件确认。M8 原有可靠性验收仍由 M8 负责，M9 整体重构仍由用户深度参与设计。

## 行业 Wiki 初始内容与页面（2026-09-14）

行业列表卡片只展示行业名称与基于 Wiki 经营机制的一句话简介，去掉编号、正文状态和统计覆盖口径。

用户接受已交付的 41 篇行业草稿作为行业 Wiki 初始内容，后续在原对象上补充完善；行业研究列表可进入各行业的八块卡片详情，与公司 Wiki 共用阅读展示。由 Backend 校验身份与内容并经既有 Wiki 发布路径保存，不新增前端内容副本；保留具体待核验事项和来源，不将本次内容接受等同于事实或硬关系准入。本次授权接入初始内容及页面，不启动 M9 架构重构。

## M8 收尾与 M9 重构边界（2026-09-12）

用户确认：将当前遗留问题、实际完成状态和验收缺口补齐到现有 M8 Task，并在 M8 内收尾，不把原范围遗留转交 M9。M9 用于后续重构，用户将深度参与架构设计；先依据闭环场景讨论职责、边界与方案，经用户确认后才实施，不提前选定全面插件化、Router 替换、存储迁移或新框架。M7 硬关系正例仍按既有决定后续观测；41 个行业基础正文仍由用户准备。此项不包含新的提交或公开发布授权。

## M7 本轮验收与提交准备（2026-09-11）

用户确认按当前实际完成范围准备分笔提交。符合范围的新硬关系「真实资料→准入入库→新会话读取」成功路径留作后续观测，不阻塞本轮 M7 验收；更大规模的完整业务闭环测试在 M8 完成后开展。这是验收门槛的明确调整，不等于该成功路径已经验证，也不代表尚未实现的功能已经完成。此决定覆盖旧交接中将该真实正例作为本轮关单前置的条款。按仓库与职责整理代码，保留测试局限；当前仅准备提交，不包含 push 或发布。

## 公司 Wiki API 依据卡（2026-09-11）

用户确认 API 数字的「查看依据」不能只写来源名和截至时间。卡片按同一结构展示数据服务说明、实际接口或公开 URL、口径与截至时间。扶摇、腾讯、新浪及其他后续 Provider 共用这套字段；未登记来源标明缺口，不编造接口。密钥与内部身份仍不展示。

## 公司 API 刷新边界（2026-09-10）

用户确认继续按原公司 Wiki schema 实施：财务展示最近年度与最新报告期关键行，估值保持 PE/PB/PS/PCF 及口径。公司页「刷新资料」只调用结构化 API 更新当前公司；不启动 Agent、文档下钻或研究，不刷新行业与其他公司。失败保留旧值及原日期并提示，已有证据内容与研究正文保留。列表刷新仅重新读取公司列表。无法从 API 获取的字段保留缺口，后续显式研究按原证据契约处理。

## M7 后续分工与交接顺序（2026-09-11）

用户确认：Codex 负责调研、方案和验收，其他模型负责实施；默认不由 Codex 直接修改业务代码。先校准 M7 已实现/待验收/未实现状态，核验公司 API 刷新与交付基线，优先补可信硬关系积累闭环，再完成 Topic/Session、记录关联、主动材料发布和新会话复用；M7 通过后再进入 M8。当前授权更新现有交接文档，不新增提交或发布授权。

## M7 实施授权（2026-09-10）

用户审阅 M7 后明确「开始实施」，批准 Task 范围与随稿评审的实施约定：Note 正文仍由产品 ledger 拥有；宿主持久化 Topic 与多次 Session 的绑定；正式 Wiki 草案由产品显式确认后交 Backend 发布；可信宿主在已授权研究范围积累硬关系，模型不持 hook token；旧 Topic 身份、正文、引用与归档原样保留。M8 页面改造、历史批量迁移、提交和发布不在此次授权内。M7 当前执行中，完成以 Task 的真实验证为准。

## M7 审计纳入与公司 Wiki API 内容（2026-09-10）

用户确认前置 tools/skills 审计的主要问题全部属于 M7 设计和调整范围；Stock-Research TODO 已记录的硬关系抽取/可信准入断点必须完善，关联失败不得静默忽略。当前授权完善 Task 待审稿，用户审阅无误后再实施。

创建公司 Wiki 时应使用扶摇 API 补充财务、估值等信息，丰富公司内容。这类 API 内容不要求创建 Document/Block/Evidence 或证据卡片；并非所有 Wiki 内容都必须经过文档证据链。保留来源、报告期、单位及更新时间以表达数据口径，不向用户暴露技术身份。现有文档证据与硬关系准入纪律继续适用于各自的内容，不扩大为所有 Wiki 内容的必经入口。具体字段由真实接口覆盖决定，未支持或失败不得编值。

## M7 前置工具审计与雷达边界（2026-09-10）

用户要求在实施 M7 前全面审计 tools、skills 与权限设计，并明确取消雷达中的雪球能力，不调用用户浏览器或复用其登录态。未来产品内置独立浏览器需另行设计，不能预先恢复。当前授权包括停用该调用路径与验证，不包括开始 M7；研究侧执行边界由 Stock-Research 的 Human Checklist「雷达停用用户浏览器」及 Radar 契约拥有。

## 我的研究、行业议题与主动积累（2026-09-10，当日讨论汇总）

状态：已确认的产品方向，尚未实现。用户最新授权先调研并准备两个连续 Task，不包含代码修改、历史数据迁移或提交。本节替代当日较早的「行业议题与连续研究场景」，并作为旧 M4 研究记录定位及 Stock-Research 快慢循环、Topic 设计后续修订的产品依据；旧文档描述不因此成为新行为。跨仓实施前须同步 Stock-Research 既有权威文档，本文件不成为第二个研究契约 Owner。

### 目的与研究对象

先从实际研究场景定义闭环，再划分业务边界、映射架构并实施。快慢循环服务于更好的分析、研究与复用，不为完成流程或插件化而建设。核心场景是「提出问题 → 读取已有知识 → 下钻原始证据并追问 → 主动积累 → 后续研究复用」。

- 公司与行业是核心研究对象。行业范围为现有统计局表3 的 41 个分行业；代码清单共 43 行，其中「总计」「采矿业合计」是汇总行，不另建行业 Wiki。
- 既有申万行业 Profile 作为 Theme Wiki 的基础材料，保留其上市行业统计口径，不作为 41 个核心行业身份。此为内容用途调整，不意味着将所有 Profile 自动改名或一对一转换为 Theme。
- Topic 承载行业级持续研究议题，不等同于行业 Wiki、单个公司档案或对话 Session。分析一家新公司不必创建 Topic。未来可扩展到「黄金与美债」等跨领域议题，本轮不实施。
- Topic 取消数量限制；同一行业可以有多个不同议题，不采用行业与 Topic 一一绑定。深度对话可由 Agent 判断后自动创建行业议题；「我的研究」允许用户主动手动创建。两个入口使用同一套 Topic，自动创建的议题可在「我的研究」继续推进。

### 产品入口与积累方式

| 入口 | 确认的目标职责 |
| --- | --- |
| 深度对话 | 读取已编译 Wiki 与 Topic 相关内容；按问题下钻原始资料和证据；积累有证据支持的硬关系；可生成公司 Wiki，并可自动创建行业 Topic |
| 我的研究（原「研究记录」） | 展示独立保存的记录与持续研究 Topic；手动创建议题；接入 DSH Agent 深入对话、推进研究和形成材料，不沿用普通页面直连模型作为议题研究引擎 |
| 普通页面问助手 | 围绕当前页面或证据解释和追问；「存入沉淀」保留为保存用户感兴趣问题及内容的记录入口 |
| 个股研究 | 以公司 Wiki 及其配套资料、证据等承接公司研究，替代旧「专题研究」入口；实际替换前核对旧入口独有能力和历史数据 |
| 行业 Wiki | 用户后续提前准备 41 个行业的基础材料，供研究读取；具体准备、校验和接入方式尚未确定 |
| 板块中心 | 后续调整为 41 个标准核心行业的入口，展示各自 Industry Wiki，并可通过研究 Link 关联 Theme Wiki 等内容；替代旧版以申万 Profile 为主体的定位 |

研究积累来自主动沉淀和维护，不从 Session 全文自动推导研究记忆。Session 保留交流过程；Topic、记录和 Wiki 承载可持续使用的内容。新入口复用 DSH Agent 不意味着所有议题共用一个 Session。

### 记录、Wiki 与关联

- 2026-09-11 补充确认：页面助手保存记录时不启动 DSH 研究或自动归入 Topic。在「我的研究」开始研究对话时，沉淀记录作为默认可读取上下文，由 DSH Agent 判断哪些与当前问题有关，主动提出关联；用户确认后才写入 Topic 关联。未确认、拒绝或无相关议题时，记录独立保留。来源公司/资料/引用是保存时的上下文，不等于已确认的研究关联。默认读取需有界并可继续检索，不要求把全部记录正文塞入每轮上下文。

- 「我的研究」包含独立记录和 Topic 两类内容。例如一次大盘行情问答可以只保存记录，没有显著议题也可成立，不强制创建或归入 Topic。
- 用户决定保存感兴趣的内容；Agent 判断记录是否关联公司、行业及相关 Topic。提及公司不自动等于应归入某行业议题。记录保存不以成功关联 Topic 为前提，也不自动变成事实或 Wiki。
- 研究 Link 不按核心/非核心对象预设连接限制：公司、行业、Topic、记录及各类 Wiki 材料均可根据内容相互关联，由 Agent 判断关联；不把 Theme、Comparison 固定为只能罗列或必须连线。Wiki 是可复用的预编译研究材料，也可以成为后续研究的起点。允许关联不意味着默认全连接，实际遍历与相关材料选择算法后续设计。
- 硬关系构成数据库中的事实拓扑，有证据并沿 Backend 校验与正式写入路径积累；核心硬关系不在产品界面暴露，由研究内部使用。用户可见的研究 Link 表达材料相关性，不因建立 Link 就成为事实关系，不混入硬关系拓扑；软链接范围开放不扩张硬关系端点范围。
- 版本、hash 等技术身份不向用户展示；产品展示可理解的材料、出处及关联。Agent 判断关联不等于绕过既有事实准入、Wiki 校验或发布权限。

### 主动研究替代 Dreaming 主导的材料生成

用户认为旧 Dreaming 自动发现并生成研究材料的设想过于理想化，调整为在「我的研究」中由实际问题与 DSH Agent 深入研究驱动材料形成，不再以 Dreaming 为该路径的前置条件。Theme 的生成明确放在「我的研究」。用户同时提到在此生成 Company Wiki；其与深度对话 Company Wiki 维护的具体分工仍有待澄清，不能擅自将 Company 改记为 Comparison。

本决定不等于已删除 Dreaming，也不自动废除现有材料校验、发布和后续复用能力。需要逐项核对旧设计哪些职责转入主动研究、哪些保留、哪些停止。

### 实施边界

- 系统提示词、tools 的业务语义及架构边界需一起调整：议题判断与创建、读取研究材料、记录关联、Wiki Link、公司 Wiki 维护、硬关系准入不能仅靠修改 UI 文案或提示词完成。此为职责划分，不预先要求每项新增一个工具。
- Finance 拥有产品入口、展示和交互组合；Stock-Research 拥有研究业务、工具与持久化契约；DSH 承载 Agent 运行和会话能力。优先沿既有 Owner 改动，不创建第二套 Topic、事实或 Wiki 写入路径。
- Stock-Research TODO 已记录的硬关系抽取、准入与积累断点必须纳入后续核对，不能用前端 Link 或展示图代替事实拓扑闭环。
- 后续以「研究电力 → 下钻公司与证据 → 创建或续接行业议题 → 保存相关发现 → 主动研究并形成材料 → 再次研究时复用」验证业务闭环。已准备连续草案 [M7：研究契约与主动积累](docs/研究契约与主动积累_Task_M7_2026-09-10.md) → [M8：我的研究与行业入口](docs/我的研究与行业入口_Task_M8_2026-09-10.md)；其中拟议交互与待确认决定不等于用户已批准实施。
- 历史 Topic 的解释与处置、Session 绑定、关联纠错及旧入口替换方案仍待确定。本次不授权迁移、删除或静默重解释历史内容。

## M3 双端插件组织与重构（2026-09-09）

用户确认现有 M3 表面功能完成，但内部实现和组织不利于扩展，要求在 M3 同步 DSH server/client 双端插件原理、职责边界与实际版本。2026-09-09 已审阅并授权按 M3 方案实施内部重构；保留现有产品行为与用户数据，不扩展 M4 业务范围。

后续确认：用户自己手动运行并验收重构体验，不要求 Agent 继续整套 UI 验收；Agent 保留必要的构建、类型检查及问题定点修复，不自行启动驻留测试服务。用户已确认启动修复后正常启动，实施状态以 M3 为准。

Node profile/Loader 启用插件，dsh-client-modules 发现并分发已构建的 client bundle，浏览器 Cordis 执行 UI 半侧；产品通过正式插件/slot 组合，避免自建第二套客户端装配。当前运行基线为带补丁的 `0.1.2-rc.1`；本地 deepseek-harness 源码仍为 `0.1.2-alpha.3`，不可混同。方案与验证边界见 [M3](docs/默认Agent与DSH模型配置_M3_2026-09-08.md)。

## M4 演示范围与研究入口（2026-09-09，替代 09-08 调研范围）

用户确认以当前 DSH Harness 为主力，直接复用 Stock-Research 已有能力，优先支持下午演示。首页完整承接当前 Pi workspace 的研究体验，包括既有意图处理、指导、Skills、工具和研究后维护，不另建研究引擎或强制长研究流程。

- 自选股每行增加「加入研究」，点击后主动获取资料并形成公司 Wiki；个股研究以公司 Wiki 为核心，优先自选公司。已加入的公司显示「已加入研究」，不重复启动研究。
- 板块中心先放申万行业 Profile，后续主要展示行业 Wiki；我的持仓不调整。我的研报支持上传并接入 Stock-Research 文档链路。
- 研究记录展示快积累 Topic 与慢积累 Theme / Comparison，不是 session 历史中心；其他对话沿用现有存储机制，不额外建设归档。
- 其他页面「问模型」沿用现有上下文和对话机制，直接调用模型，不走 DSH Agent；Provider 信息统一来自 DSH，服务端使用凭据。

以上替代此前首页和抽屉共用 DSH Agent 会话的 M2/M4 设想。内部路径仍由环境变量与配置决定，知识写入沿 Stock-Research 既有规则。用户已认可 M4 实施方案主体；最终体验由用户手动运行验收。文稿尚未入库。

补充确认：Owner 必须清晰。研究行为对照入口为 `/Users/apple/ts/src/Stock-Research/pi/stock-research-wiki/run.sh`，DSH 工具优先复用 `/Users/apple/ts/src/Stock-Research/dsh` 的既有工作，产品只消费研究插件。首页在「对话」「轨迹」旁增加「历史对话」tab，呈现当前固定工作区的原生 DSH sessions；工作区通过环境变量/配置与 Stock-Research 对齐，不展示目录选择。这不改变「研究记录」展示 Topic / Theme / Comparison 的定位，也不新增会话存储。截图圈出的指令菜单具体改法尚待确认，不据此改变权限或删除功能。

## M3 首版边界与 React 兼容（2026-09-08）

本轮先替换首页会话、侧栏运行状态和模型配置三个区域。Stock-Research 工具、Wiki 与研究环境对接归 M4；不以本轮替换全部旧研究入口。
内部工作区路径按环境变量、配置、隔离数据目录默认值的顺序解析，不展示目录选择。DSH 状态目录与工作区分别配置。用户已明确同意统一 React 18，并要求验证现有页面。

## 默认 Agent 与内部研究工作区（2026-09-08）

产品默认由 Agent 驱动，不要求用户选择 Agent SDK/CLI 或开启 Agent。运行时、模型 Provider 与默认模型由系统配置；研究工作区内部管理，不展示目录选择按钮。模型调用优先复用 DSH LLM Provider 机制，Stock-Research Backend 继续拥有研究业务与证据。凭据来源与可选模型设置界面尚未确定，不自动复制其他宿主凭据。后续实施边界见 [M3](docs/默认Agent与DSH模型配置_M3_2026-09-08.md)，当前仅完成分析与文档。

此决定替代旧 AI 接入设计中要求用户选择执行引擎和切换 agent/direct 的产品目标；M2 暂保旧普通问答仅为接入验证阶段安排，最终入口按 M3 收敛。

同日补充确认：保留 fork 的行情、桌面与开箱即用能力快速迭代；引入 DSH 是为了替换当前薄弱的 Agent 运行和交互体验。Stock-Research 已初步具备研究意图处理、Wiki 与证据迭代能力，后续接通现有能力，不另建意图分类或强制长研究流程。M3 方向不变；已有 Stock-Research 能力与实际迁入 DSH 的能力必须分别核对，当前文档补充不代表代码迁移完成。

## Fork 测试版与 DSH 技术验证（2026-09-08）

以 Vibe-Deep-Research fork 为实施主体。将测试版身份和运行环境隔离，以及「首页和抽屉共用 DSH 会话能力＋一个真实文档工具＋点击引用回读」纳入同一个 docs Task。保留原安装版，Stock-Research 继续提供现有 Backend 服务。本轮范围与验收见 [M2](docs/测试版隔离与DSH研究交互验证_M2_2026-09-08.md)；不代表六阶段研究重设计或完整研究能力迁移已获确认。

## 二开文档与验收形式

后续代码二开沿用 `docs/` 现有实现文档的风格与形式，不另起一套模板或平行台账。

- 文件放 `docs/`。一轮有界改动用 `主题_Mxx_YYYY-MM-DD.md` 或 `主题_YYYY-MM-DD.md`；长期入口用稳定文件名，并在 `docs/README.md` 更新索引。
- 文首写日期、范围、真实状态（例如未提交 / 未推送 / 未发布）。标题用 `Mxx · 主题` 或 `主题 · 本地验收`。
- 正文分开写：用户决定或授权边界、行为与实现变化、验证证据、仍未宣称的缺口。历史决定被替代时标明关系，不覆盖旧文件。
- 证据用表格或可核对路径（测试数、run-id、日志、截图）。局部通过不能写成完整发布或全平台通过。
- 不照搬原作者个人品牌、口头审批流程或过期验收结论；只沿用记录方式。

## 首页与盘面取舍（2026-09-08）

先做功能和界面。本轮范围与边界见 [docs/首页与盘面取舍_M1_2026-09-08.md](docs/首页与盘面取舍_M1_2026-09-08.md)。
## 六阶段模型来源与首页最近对话（2026-09-09）

用户确认：保留旧 `/research` 六阶段编排、受控工具和产物校验，将模型调用统一接入 DSH Provider；不在 DSH 内再启动旧 Agent。DSH 管理凭据，产品编排器负责该历史六阶段流程，Stock-Research 继续拥有自身研究与 Wiki 写入契约。首页空白会话的输入框下展示最近对话，复用原生归档，隐藏空白及已归档会话。（2026-09-21 标注：六阶段编排保留的决定已被 2026-09-15「提交前旧系统退役」替代并于同日退役，代码中已无该路径；最近对话展示部分仍有效。）

## 页面模型入口统一（2026-09-09）

用户要求检查其他需要模型的页面，避免资讯页再次跳转到旧接入流程。资讯翻译、要点提炼、研究记录反思使用 DSH 页面模型；设置入口复用原生设置。旧研报归档的 `/tasks` 编排仍需单独迁移，不能把移除旧设置页视为该流程已完成迁移。

## M4 与 M5 范围拆分（2026-09-09）

用户要求回补 M4 原任务边界与架构，并新增 M5 记录超出 M4 的样式、风格、布局及相关功能调整。M4 继续拥有研究接入、证据回读和业务/模型执行边界；M5 拥有产品呈现与交互收敛，不能替代研究闭环验收。

当前已使用部分 DSH slot，但业务页面仍主要依靠 path 路由、集中式组合与样式适配，不能宣称已形成完整模块与插件 UI slot 体系。后续在 M5 核对实际 runtime 的能力与拆分边界；本次不启动运行时升级或全面重构。

两项均按源码与未完成项回补，不追认整体已验收。研究记录的 Topic / Theme 原目标仍属 M4 待完成项，当前本地 Note 不等同该目标。见 [M4](docs/研究闭环与DSH交互_M4_2026-09-08.md) 与 [M5](docs/产品界面与交互收敛_M5_2026-09-09.md)。

## M8 原生 DSH 与正文引用（2026-09-14）

原生接线补充确认：以既有日历工具为范式，研究工具直接 defineTool 并注册到 DSH Agent 作用域；移除自建工具目录、参数校验和调用分发层。研究协调仅保留问题代次、候选、报告续跑及成功后维护状态，使用 DSH 的取消、空闲维护和作用域释放；不升级 DSH 包。工具按业务能力拆分，研究状态与收尾独立，组合入口不持有业务实现，各工具组使用同一份研究状态。

用户确认并授权先更新 M8 再实施：Pi 扩展只供 Dev 测试实验；DSH 是产品执行入口，须基于自己的 tools 注册、插件生命周期、取消、收尾及释放机制重写产品研究能力，不打包或执行 Pi 扩展、不模拟 Pi 事件、不运行时读取 Pi 文件。两端仅共享 Backend 业务契约；已取消 Dreaming 不恢复。产品拥有最终 profile、预设和通用工具权限，研究包拥有研究能力；启动配置按用途命名，不保留 `vibe-m3.patch.yml` 等阶段名。

对话引用覆盖此前数字上标决定：内部来源显示正文短标题；外部网页按最新截图显示紧凑站点标签，悬停展示已有文章标题与摘要，点击直接打开 HTTP(S) 原文，不进入只有 URL 的空侧栏。文档 Block 与 API 快照沿用统一依据卡和追问。标题取真实元数据或明确链接文字，缺失时只显示站点/来源名称，不猜补身份、不移动历史引用归属。站点名称映射不代表可信背书，不因域名未列入名单而改变引用行为。公司 Wiki 文字「查看依据」不变。行业知识建设沿既有 schema 留到 M9，不扩展本轮实现范围。

## M8.6 研究可视化与图数联动（2026-09-14）

授权确认（2026-09-15）：用户明确确认已取得 Lieflat 商业授权，允许按本轮范围将其模板／Skill 接入产品并适配产品样式。此处记录用户确认，不代表已独立核验授权协议；接入时保留上游许可证及出处。

用户先要求输出 M8.6 Task；随后要求推进并完成 M8.6，再集中进行验收，整体重构留到 M9。允许在已确认边界内实施和验证，未授权提交或 push；M9 的架构设计由用户参与。

已明确需求：深度对话查询华能国际行情时出现 K 线；已有财务分析 Skills 与对话 slot 结合；参考 Lieflat Charts report 模式为 Wiki、Topic 等核心对象提供可视化，扶摇最佳实践作为金融场景参考。图表与精确数字共同存在，通过按钮切换或查看驱动数据。

用户确认：成果数据、计算、来源和受控展示配置统一保存到 Backend 现有 PostgreSQL；DSH 持久消息保存引用，历史结果不变。现有研究关联支持“记录或成果”，仍由用户确认关联。用户授权记录后直接实施；实现和验证见 [M8.6](docs/研究可视化与图数联动_Task_M8.6_2026-09-14.md)，未授权提交或 push。

补充确认（2026-09-15）：产品所有来源展示统一称“同花顺”，不带“扶摇”；真实来源地址与身份保留。对话内 K 线维持严肃金融呈现，采用成熟组件；对话其他金融图表同样评估成熟组件。Wiki／Topic 等对象报告与可视化遵循 Lieflat 语言，调整为产品自己的样式；允许定性报告，不为模板补造数字。金融金额图表优先用亿元，精确数据可用元；全局数字展示采用千分位、明确单位与精度，价格、比例、数量按其业务单位显示。成果图表取消“展开”，改为下载，保留图表／数据切换；不影响对话窗口展开。具体组件选型与下载格式仍须方案及实测，不视为已经验收。

## 聊天窗口尺寸（2026-09-09）

用户确认在 Finance 容器层增加上边框、左右边框拖拽及展开/还原。上边框压缩标题空间，左右边框调整阅读窗口占用宽度；展开占满主工作区，保留原生 conversation slot 和会话生命周期。尺寸限制在可用区域，窄屏自适应，不改变模型、研究流程或会话数据。

## 基础收敛与设计先行（2026-09-09）

用户同意先做有界的基础收敛，再落地证据锚点和交互研究成果，不先启动全面重构。本轮要求先提供架构边界、布局、样式与 UI slot 规划，重点核对产品 UI 组合以及 DSH tools / Agent Server。当前 M3–M4 修复作为基线保留。

[M6 设计草案](docs/架构边界与UI组合方案_M6_2026-09-09.md) 供审阅；具体新增 slot、区域失败行为和接口方案尚未确认，本条不授权重构实施。Web Client 目标与对话 Owner 的后续确认见下条。

## Web Client 组合与对话 Owner（2026-09-10）

用户确认并要求完善到 [M4](docs/研究闭环与DSH交互_M4_2026-09-08.md)：FinanceShell 作为产品外壳插件，拥有布局与展示位置；业务模块贡献页面、导航与上下文，保留 React Router 的 URL 职责及 Slots 的组合职责。可复用对话模块拥有对话交互与会话绑定，放在外壳内不等于归外壳维护全部逻辑。复用组件不默认共享 Session，也不改变页面问助手直连模型、首页走 DSH Agent 的既有执行边界。本次授权为文档完善，不是重构实施；具体贡献接口、关闭时是否继续生成等行为仍需在实施方案中明确。

## 首轮基础收敛 Task（2026-09-10）

用户确认先做有明确边界的基础收敛，再实现证据锚点和一种交互成果，不先全面重构；基础范围由首个证据消费者倒推，全站页面注册等可延后。已要求准备 [M6-A](docs/基础收敛_Task_M6-A_2026-09-10.md) 供审阅，明确用户确认无误后才实施；本次仅授权编写 Task。

用户随后明确「开始实施」，授权按上述 M6-A 有界范围执行；不包含提交、发布或后续 M6-B/M6-C，最终运行体验仍待验收。

验收补充：用户确认切页恢复已测试通过，并授权将资讯页面上下文遗漏纳入当前修复。允许修改 Intel 的公开新闻/A 股公告上下文接入、相邻序列化函数及测试；不改变模型执行路径或自动读取新闻正文。

## 证据与交互成果后续（2026-09-10）

M7 数据能力补充：用户确认并授权实施多 Provider 主备。Finance 页面、Wiki、Agent 消费稳定能力契约；Stock 数据服务统一选择已支持同口径的数据源。失败保留好值及原日期；技术故障不出现在产品正文。对应决定与实现由 Stock Human Checklist 和数据服务拥有。本轮不提交发布。

公司资料导航：用户确认改为可按名称/代码搜索、按全部/自选筛选的公司总览；详情取消常驻公司列表，顶部提供可搜索切换器。返回总览恢复搜索、筛选及位置；公司 URL 与助手按公司隔离的历史保持不变。取消自选不删除公司研究资料。本次授权页面有界调整和验证，不含提交发布。

阅读方案修正：用户确认采用 EmbedPDF 统一替换浏览器 PDF iframe 与片段目录双模式，并授权实施后先自行验证。阅读器拥有唯一页码状态；选中文字后显式点击追问，不自动标注或发送。后端 block 继续用于证据校验，证据卡保持独立，不承诺 block 自动高亮。保持返回来源及助手并排，不扩大到全面重构。

范围更新：用户确认把统一依据卡片、API 补数边界、公司返回恢复、财报/研报阅读与助手追问全部纳入 M6-B，并要求完善实施细节后执行。卡片不做文本高亮；版本、hash 等技术身份不出现在用户界面，包括折叠详情。查看依据不自动跳转或发送问题。阅读应保留公司选择和来源位置，助手与阅读并存。数据优先使用扶摇同花顺 REST API 已支持的能力，遵循 Stock-Research Provider 分责；后续产业信号也参考该能力目录，不把 API 数据伪装成财报证据。不授权提交或发布。

用户明确「可以做1-2我都没问题」，同意按顺序推进证据锚点与一种交互成果，继续遵循有界改动、不全面重构。执行分别见 [M6-B](docs/证据锚点_Task_M6-B_2026-09-10.md) 与 [M6-C](docs/交互成果_Task_M6-C_2026-09-10.md)。该同意不追认尚未验证的能力完成，也不包含提交或发布。

用户最新截图表明页面助手已使用新闻标题上下文；这不等于正文读取或证据锚点验收。DSH 原生消息扩展的接口归属与具体变更范围由后续 Task 核对；不得用替换原生 renderer 冒充无侵入插件扩展。
# DSH 后台知识复核（2026-09-15）

用户授权把 Wiki 维护复核移到 DSH 独立子 Agent；主回答完成即结束计时，无增量不在对话展示复核过程。仅新材料快照触发，失败不拖累下一问题，草案仍需核对后明确授权发布。执行 Owner 见 [Stock Task](../Stock-Research/docs/tasks/dsh-background-review.md)，不新增队列或重启恢复。

## 后台子 Agent 与只读过程（2026-09-18）

用户确认报告生成由真正的 DSH 子 Agent 执行，不进入普通深度对话、最近对话或历史；过程保留轨迹，去掉输入框及聊天操作。实现可复用插件，报告和后台知识整理共用过程查看能力，保留原执行与业务 Owner；既有报告轨迹不删除。查看或关闭面板不改变当前聊天、不取消后台执行。具体边界及验收见 [M9.5 E 节](docs/公司Wiki生成与交互报告_Task_M9.5_2026-09-17.md)。代码已按该节实施；真实 DSH 与浏览器验收仍由用户完成。未授权提交。

## 问助手双模式（2026-09-20）

用户确认问助手拆为 Chat / Agent 两种模式：Chat 沿用页面直连模型；Agent 以 DSH 会话为底层（工具、审批、中止、历史全走原生），UI 走"搬原生 conversation 座位进问助手面板 + 重塑样式"路线，不自绘对话；页面上下文以 `page:` 引用 chip 进入会话，发送时序列化展开；Agent 模式每次打开面板对应新会话，但首次发送时才创建，关闭面板恢复原会话（同日用户确认）。该决定替代 2026-09-10「页面问助手直连模型、不共享 Session、不获得 Agent 工具」中对 Agent 模式的约束，Chat 模式不变。目标是从大盘/资讯/行业/产业链各页都能就地用 Agent 追问并落到公司对象，同时修正议题页与窄窗口下原生对话的视觉。Task 见 [M9.8](docs/问助手双模式与Agent面板_Task_M9.8_2026-09-20.md)，由其他 Agent 实施，原对话 review。未授权提交或发布。
