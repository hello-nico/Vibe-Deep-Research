# Todo

2026-09-24 按三条并行线重写。已完成项移出（DSH 0.1.7、公司研究后台任务 S2 与修复、T5-a、T5-b、存入沉淀、提示词整体调整、右侧面板统一），执行证据归各自 Task。路线权威见[产品对象模型与交互 v1](docs/产品对象模型与交互_v1_2026-09-23.md) §7；本文件只记顺序与未决事项。

2026-09-24 用户确认：T3 改走方案 B；Jev 整体延期（见 §5）；主线先打磨产品核心特色。

## 1. Codex 执行队列（2026-09-24 起实现全部交 Codex；并行规则见 Human Checklist「Codex 并行与 worktree 规则」）

2026-09-25 收尾时的下一步（按顺序）：

0. **2026-09-25 晚 Claude 直接修复并提交**：资料阅读页恢复问助手、“就此追问”把所选原文随本轮上下文送达并在面板显示；依据面板与引用标签不再露出 `legacy:` 编号；财务指标英文名改中文；状态点悬停即时说明；嵌套毛玻璃导致的研究页首卡空白（规范第 29 项）。“查看依据要点几次”未能复现，待用户复验。

1. **整体验收修复第 1、2 轮**：已实施、Claude 审阅、自动检查通过并于 2026-09-25 提交；**待用户真实复验**（Claude 在对话中给出的 25 条验收场景 + 规范第 48–50 项）。重点：第 5 项公司研究后知识整理是否成功（若显示“整理超时”需调整 90 秒时限）。复验通过后进入[客户演示](docs/客户演示计划_2026-09-26.md)的版本冻结
   - **第 5 项最优先**：公司研究完成后知识整理失败。已核实原因是 DeepSeek-V41-Flash 提交结构化结果时 JSON 格式有误（`MALFORMED_RESPONSE`），纠正也同样失败；第 13、19 项同因。要做：此类错误自动重试一次，仍失败时换用更强模型；任务列表的失败原因由 "error" 改为人话。修好可解开第 6、22–25、42 项。
   - 第 4 项：过程视图中英文标签（"Called tools""Think"）与研究模型英文开头。
   - 第 8 项：公司页“所属行业”不可点、“相关研究材料”开抽屉、行业 Wiki 页无“成员公司”。
   - 第 14 项：「新会话」打开即自动开始研究，未先显示议题开场。
   - 第 34 项：大盘 Ask 的页面快照缺后加载的情绪 / 板块资金 / 成交额榜。
   - 第 35 项：Agent 模式未先读取页面上下文。
   - 前一轮修复 Task（A 公司研究入口、B1、B2、C、开发入口转发）已实施未提交，复验通过后与本轮一起提交。
2. **Jev 第 3、4 轮评估**（2026-09-25 进度：第 3 轮已裁决（Task §9.2：采用带上一条回答的版本，直接生效阈值 0.9，先影子模式攒真实正例）；第 4 轮第二次抽样复核见 Task §9.2，待修脚本后重跑 prepare）：
   - [第 3 轮](docs/Jev记忆触发评估_第3轮_Task_2026-09-25.md)：过滤漏掉产品模板与测试语句（议题自动提示、公司研究模板、旧页面快照、引用序列化、后台“继续核对 / 整理本轮研究”、验收用“请只回复…”）；修后真实正例预计不足 20 条，真实样本主要测误触发率，分类准确率以合成样本为主。
   - [第 4 轮](docs/Jev引用核对评估_第4轮_Task_2026-09-25.md)：结论只取了引用元素文字（1,064 条因过短被丢），应取所在整句 / 表格行并带报告名与期间；表格数字无单位导致数字比对失败，缺单位时按数值比对或读表头单位；改写词表补 FY2025 / 2026H1、直接 / 间接持股；原文摘录只留表头与相关行，≤1,500 字。
3. **演示后并发三个 Task**（2026-09-25 写定，演示前不派）：[报告引用数字核对](docs/报告引用数字核对_Task_2026-09-26.md)（先做，直接修 35% 错块引用）、[记忆触发影子模式](docs/记忆触发影子模式_Task_2026-09-26.md)、[报告引用语义核对（Jev）](docs/报告引用语义核对_Jev_Task_2026-09-26.md)（依赖前者；阈值依据第 4 轮 §9.4）。Jev 场景 Benchmark 报告已发布：https://claude.ai/artifact/HnM1tZsZtgXiSqY5mF5TPR

4. 待观察：关闭依据面板后首卡空白（未复现，再遇到时记录浏览器、滚动与窗口宽度）；知识整理摘要中文待下次成功整理核对；事实白名单待真实暂存触发。

**研究记忆机制 v2**：2026-09-25 调整为“记忆只服务偏好 / 纠正 / 跟踪 / 回忆四个时刻，触发由 Jev 判断用户原话”，M10-a / M10-b / M3 暂停，待 [Jev 第 3 轮评估](docs/Jev记忆触发评估_第3轮_Task_2026-09-25.md) 结论后改写设计稿再派。

## 2. Claude（设计、视觉、审阅）

1. 写整体验收修复第 2 轮 Task；把 Jev 第 3、4 轮抽样问题写入各 Task §9；审阅 Codex 回填与复验结果。
2. 界面打磨按用户截图逐项做（Claude 负责视觉与样式，不交 Codex）。

## 3. 已完成（证据归各自 Task）

T6-b 证据墙界面、T6-c 议题图文报告、产品体验调整 A–C / A-5 / D、外部内容注入隔离、2026-09-25 我的研究界面重设计与深度对话去掉「轨迹」、T1-a 对象登记层、T1-b 悬停预览卡、T2、T3-0（结论：方案 B）、T3-b 议题与问助手面板、T3-c 任务过程原生视图、T4-a、T4-b 状态徽标与待处理、T5-a / T5-b / T5-c1、T6-a 证据墙接口、D 旧研究材料阅读器退役、深度对话工具行中文名、事实白名单与会话标题、单位别名、计算期间解析。

## 4. 已定待实施决策

- （空）

## 5. Jev 接入（2026-09-24 延期后当晚恢复，与 T6-b 并行）

第一轮验证与裁决见 [Jev 中文效果验证](docs/Jev中文效果验证_Task_2026-09-24.md) §10。
- **外部内容注入隔离**：[Task](docs/外部内容注入隔离_Task_2026-09-24.md)，已审阅并叠加到主目录（未提交），待用户重建 Backend 后验收。
- **Jev 第二轮验证**：已完成并终裁（[§11.2–11.3](docs/Jev中文效果验证_Task_2026-09-24.md)）：引用核验有效，待定产品形态；相关性筛选不产品化，改做外部结果确定性去重与空页过滤；整理闸门、判断连续性待数据积累后第三轮。
- **M9.7 Jev 判断连续性**：[Task](docs/Jev判断连续性_Task_2026-09-24.md)，待第二轮结果裁决。
- **Jev 第 3 轮（记忆触发）/ 第 4 轮（引用核对）评估**：[第 3 轮](docs/Jev记忆触发评估_第3轮_Task_2026-09-25.md)、[第 4 轮](docs/Jev引用核对评估_第4轮_Task_2026-09-25.md)，可并行；先 prepare，Claude 复核样本后再跑。
- **Jev 场景 Benchmark（2026-09-25 用户提出，第 3、4 轮之后）**：把“Jev 适合做什么”做成一张可复跑的对比图，形式参照 CursorBench：**每条线是一个场景**（注入筛查、引用核对、记忆触发、相关度、判断连续性，以及后续的新闻去重 / 新事件、投资动作建议检查等），**线上各点是同一场景的不同配置**（上下文多少、阈值、题面变体），**横轴为每千次判断费用（或 p95 延迟），纵轴为该场景的主指标**（精确率 / 召回按场景事先定义）。沿用各轮评估脚本与样本，固定样本集与版本以便后续复跑对比；可加一个通用小模型做同题基线点。产出为产品内或文档内图表，待第 3、4 轮结论后写 Task。

## 6. 小遗留

- （空；旧申万行业链接已由 D 项处理）

## 7. 治理遗留（不阻塞主线）

Owner：[治理对齐修复 Task §9](docs/治理对齐修复_Task_2026-09-21.md)、[真实阅读与数据引用优化 Task](docs/真实阅读与数据引用优化_Task_2026-09-21.md) §4c–4d。
- 原始 source 块不透明身份与固定版本入口；ReportReader 引用切换失败场景真实验收。
- 设置页写入凭据后的模型接入实测。
- 核实消费者后清理 core/ai 旧三件套。
- 读取稳定性、计算纪律与 source / Wiki 身份缺口。

## 8. 远期路线：客户端打包、账号与用户管理、移动端（2026-09-24 记录，未决方向，非当前范围）

前提：产品打磨到用户认为合适、Web 工作台稳定后才启动。启动前须先改 AGENTS.md 与 Human Checklist 中“当前只交付 Web 工作台、不恢复原生外壳或 Electron”（2026-09 决定），本节不构成授权。

**已定方向（2026-09-24 用户）**：Stock Backend 走云端实现。桌面与移动端都是连接云端 Backend 的客户端，不在用户机器上跑 Backend 与 PostgreSQL，也不要求 Docker。

**部署形态与云端配置规划**：[文档](docs/部署形态与云端配置规划_2026-09-24.md)（纯 SaaS vs 客户端 + 云端，分阶段计划与分规模配置；建议默认纯 SaaS）。

**R0 Backend 上云（其余各段的前提）**
- 多用户：按用户隔离个人研究数据（记录、议题、研究关联与假设、研究记忆、草案、上传资料、报告、任务记录）；公共资料（公告 / 定期报告原件与解析、公司 / 行业 Wiki、硬关系）是否全员共享需定（见待决 ⑥）。
- 鉴权：每个请求带用户令牌，替代现在本机的 hook token 与本机代理放行；供应商与取数凭据只在服务端。
- Client SQLite（自选、加入研究的公司、界面偏好）迁为按用户存云端，或保留本机偏好 + 云端名单（待定）。
- 部署、备份、监控与费用；本机开发仍可用 Docker 起一套。

**R1 桌面客户端（macOS / Windows）**
- 外壳参考 DSH 自己的桌面版：Electron 从 `file://` 加载前端、经 IPC 桥转发请求（`dsh-host-webserver` README），桌面平台头 `desktop-mac` / `desktop-win`（`dsh-deepseek-account-platform`）。优先复用 DSH 桌面组合方式，不另起外壳。
- Backend 在云端后，桌面包只含前端 + DSH Host（Agent 运行在本机、调用云端 Backend），或连云端 DSH Host（与 R4 共用）；两者选一（见待决 ①）。
- 自动更新；签名与公证（Apple notarization、Windows 代码签名）。
- 沙箱：DSH 已有三平台本机沙箱（`dsh-sandbox` / `dsh-sandbox-local`：macOS、Linux 各自 runner，Windows 为 `dsh-sandbox-windows-acl` 受限令牌 + ACL；只读 / 工作区可写 / 完全访问三档；无法强制时直接失败不裸跑；同机限制，非容器隔离）。**当前产品预设未挂载 bash / 文件写工具**，沙箱只在 DSH Host 跑在本机且开放命令或文件执行时需要；Host 在云端时改用容器 / microVM 隔离。用户此前的三平台沙箱调研作为输入。

**R2 登录（账号）**
- 方式：第三方授权 + 系统浏览器 + PKCE，参考 `dsh-deepseek-account-platform`（系统浏览器登录、凭据存本机凭据库、令牌只在 Host、界面只拿登录状态）与 `dsh-authorization`（可注册自有授权流程）、`dsh-api-account-controller`（登录状态 / 资料 / 登出，不回传令牌）。
- 需另建账号服务（自建或托管认证服务，待选）。
- **待决注意**：Google 登录在中国大陆不可用，若主要用户在大陆需另备手机号 / 微信等；上架 iOS App Store 时提供第三方登录通常须同时提供“通过 Apple 登录”。

**R3 用户与用量管理**
- 先定计费模式：用户自带模型 Key，还是由产品提供模型额度。后者需服务端模型网关持有供应商 Key、按用户计量与限额；本机 `dsh-token-meter` 只做估算与展示，不能作为计费依据。
- 登入 / 登出、设备管理、额度与账单；沿用 DSH 账号与凭据分层（Client 不接触令牌）。

**R4 移动端**
- Backend 已在云端；手机跑不了 DSH Host，移动端需要云端 DSH Host（服务端隔离执行——DSH 文档建议用容器 / microVM / 远程执行器而非同机沙箱）。先 Web 响应式 / PWA，再考虑原生。

**关键待决（启动时逐项与用户确认）**：① DSH Host 放本机（桌面）还是云端（移动端必须云端；两种并存则会话需跨端同步）；② 本机选择数据（自选等）上云方式；③ 登录方式与认证服务自建 / 托管；④ 模型计费模式；⑤ 大陆用户与应用商店的登录要求；⑥ 公共资料与 Wiki 是否全员共享、个人研究如何隔离。
