# Todo

2026-09-24 按三条并行线重写。已完成项移出（DSH 0.1.7、公司研究后台任务 S2 与修复、T5-a、T5-b、存入沉淀、提示词整体调整、右侧面板统一），执行证据归各自 Task。路线权威见[产品对象模型与交互 v1](docs/产品对象模型与交互_v1_2026-09-23.md) §7；本文件只记顺序与未决事项。

2026-09-24 用户确认：T3 改走方案 B；Jev 整体延期（见 §5）；主线先打磨产品核心特色。

## 1. Codex 执行队列（2026-09-24 起实现全部交 Codex；并行规则见 Human Checklist「Codex 并行与 worktree 规则」）

并行线：T6-a（Stock 主目录）｜T3-b（worktree `codex/t3-b`）｜D → T4-b（worktree `codex/d-t4b`）；T6-b、T6-c 待 T6-a 与 T3-b 合回。

1. **验收问题修复（2026-09-24 批）**：B 已验收提交；A（关闭依据后首卡空白）未复现，用户再遇到时记录浏览器、滚动位置与窗口宽度；C 待下一次成功的知识整理核对中文。
2. **T6-a 证据墙关系读取与用户假设**：[Task](docs/证据墙关系读取_T6-a_Task_2026-09-24.md)，已实施、审阅补修，Backend 已重建；待用户统一验收。
3. **T3-b 议题与问助手面板**：[Task](docs/议题与问助手面板_T3-b_Task_2026-09-24.md)，已实施审阅，已叠加到主目录（未提交），待用户验收。
4. **T4-b 状态徽标与待处理**：[Task](docs/状态徽标与待处理_T4-b_Task_2026-09-24.md)，已实施审阅，已叠加到主目录（未提交），待用户验收。
5. **D 项 旧研究材料阅读器退役**：[Task](docs/旧研究材料阅读器退役_D_Task_2026-09-24.md)，已实施审阅，已叠加到主目录（未提交），待用户验收。
6. **T6-b 证据墙界面（含 T5-c2 依据变化展示）**：[Task](docs/证据墙界面_T6-b_Task_2026-09-24.md)（设计稿 2026-09-24 用户确认）→ **T6-c 议题图文报告**：[Task](docs/议题图文报告_T6-c_Task_2026-09-24.md)（已预写，T6-b 合回后派）。

## 2. Claude（设计、视觉、审阅）

1. 审阅每个 Codex 回填，做视觉收尾（T3-b 面板、T4-b 徽标）。
2. 各线合并与冲突处理；T3-b 面板、T4-b 徽标、T6-b 证据墙视觉收尾。

## 3. 已完成（证据归各自 Task）

T1-a 对象登记层、T1-b 悬停预览卡、T2、T3-0（结论：方案 B）、T3-c 任务过程原生视图、T4-a、T5-a / T5-b / T5-c1、深度对话工具行中文名、事实白名单与会话标题、单位别名、计算期间解析。

## 4. 已定待实施决策

- **草案失效处理**（T4-a / T4-b）：知识整理草案 24 小时过期或研究页底稿变化即作废；任务记录与提醒卡显示“草案已失效”并提供「重新整理」；不自动重做、不延长保存。

## 5. 延期：Jev 接入（2026-09-24 用户决定，主线先打磨产品核心特色）

Task 已写好，恢复时直接派发；第一轮验证与裁决见 [Jev 中文效果验证](docs/Jev中文效果验证_Task_2026-09-24.md) §10。
- **外部内容注入隔离**：[Task](docs/外部内容注入隔离_Task_2026-09-24.md)（第一轮注入识别 11/11，用户已认可隔离方式）。
- **Jev 第二轮验证**：[Task §11](docs/Jev中文效果验证_Task_2026-09-24.md)（引用核验、整理闸门、判断连续性）。
- **M9.7 Jev 判断连续性**：[Task](docs/Jev判断连续性_Task_2026-09-24.md)，待第二轮结果。
- **外部检索相关性筛选（设想）**：用 Jev 判断外部内容与问题的相关性以减少模型上下文。第一轮相关性 4/8 不可用；若恢复，先在第二轮验证加入：真实检索结果配原问题 ≥10 条，保守阈值（只略去 <0.2 或只排序、低分只给标题摘要），统计省下字数、误丢与耗时；略去条目在过程里可见。

## 6. 小遗留

- 旧申万行业目标（如 `industries/801161-si`）生成的 `/sectors/<代码>` 行业页不识别；历史个例，未处理。

## 7. 治理遗留（不阻塞主线）

Owner：[治理对齐修复 Task §9](docs/治理对齐修复_Task_2026-09-21.md)、[真实阅读与数据引用优化 Task](docs/真实阅读与数据引用优化_Task_2026-09-21.md) §4c–4d。
- 原始 source 块不透明身份与固定版本入口；ReportReader 引用切换失败场景真实验收。
- 设置页写入凭据后的模型接入实测。
- 核实消费者后清理 core/ai 旧三件套。
- 读取稳定性、计算纪律与 source / Wiki 身份缺口。

## 8. 远期路线：客户端打包、账号与用户管理、移动端（2026-09-24 记录，未决方向，非当前范围）

前提：产品打磨到用户认为合适、Web 工作台稳定后才启动。启动前须先改 AGENTS.md 与 Human Checklist 中“当前只交付 Web 工作台、不恢复原生外壳或 Electron”（2026-09 决定），本节不构成授权。

**已定方向（2026-09-24 用户）**：Stock Backend 走云端实现。桌面与移动端都是连接云端 Backend 的客户端，不在用户机器上跑 Backend 与 PostgreSQL，也不要求 Docker。

**R0 Backend 上云（其余各段的前提）**
- 多用户：按用户隔离个人研究数据（记录、议题、研究关联与假设、研究记忆、草案、上传资料、报告、任务记录）；公共资料（公告 / 定期报告原件与解析、公司 / 行业 Wiki、硬关系）是否全员共享需定（见待决 ⑥）。
- 鉴权：每个请求带用户令牌，替代现在本机的 hook token 与本机代理放行；供应商与取数凭据只在服务端。
- Client SQLite（自选、加入研究的公司、界面偏好）迁为按用户存云端，或保留本机偏好 + 云端名单（待定）。
- 部署、备份、监控与费用；本机开发仍可用 Docker 起一套。

**R1 桌面客户端（macOS / Windows）**
- 外壳参考 DSH 自己的桌面版：Electron 从 `file://` 加载前端、经 IPC 桥转发请求（`dsh-host-webserver` README），桌面平台头 `desktop-mac` / `desktop-win`（`dsh-deepseek-account-platform`）。优先复用 DSH 桌面组合方式，不另起外壳。
- Backend 在云端后，桌面包只含前端 + DSH Host（Agent 运行在本机、调用云端 Backend），或连云端 DSH Host（与 R4 共用）；两者选一（见待决 ①）。
- 自动更新；签名与公证（Apple notarization、Windows 代码签名）。
- 沙箱：DSH 已有三平台本机沙箱（`dsh-sandbox` / `dsh-sandbox-local`：macOS、Linux 各自 runner，Windows 为 `dsh-sandbox-windows-acl` 受限令牌 + ACL；只读 / 工作区可写 / 完全访问三档；无法强制时直接失败不裸跑；同机限制，非容器隔离）。**当前产品预设未挂载 bash / 文件写工具**，沙箱只在 DSH Host 跑在本机且开放命令或文件执行时需要；Host 在云端时改用容器 / microVM 隔离。用户此前的三平台沙箱调研作为输入。

**R2 登录（账号）**
- 方式：第三方授权 + 系统浏览器 + PKCE，参考 `dsh-deepseek-account-platform`（系统浏览器登录、凭据存本机凭据库、令牌只在 Host、界面只拿登录状态）与 `dsh-authorization`（可注册自有授权流程）、`dsh-api-account-controller`（登录状态 / 资料 / 登出，不回传令牌）。
- 需另建账号服务（自建或托管认证服务，待选）。
- **待决注意**：Google 登录在中国大陆不可用，若主要用户在大陆需另备手机号 / 微信等；上架 iOS App Store 时提供第三方登录通常须同时提供“通过 Apple 登录”。

**R3 用户与用量管理**
- 先定计费模式：用户自带模型 Key，还是由产品提供模型额度。后者需服务端模型网关持有供应商 Key、按用户计量与限额；本机 `dsh-token-meter` 只做估算与展示，不能作为计费依据。
- 登入 / 登出、设备管理、额度与账单；沿用 DSH 账号与凭据分层（Client 不接触令牌）。

**R4 移动端**
- Backend 已在云端；手机跑不了 DSH Host，移动端需要云端 DSH Host（服务端隔离执行——DSH 文档建议用容器 / microVM / 远程执行器而非同机沙箱）。先 Web 响应式 / PWA，再考虑原生。

**关键待决（启动时逐项与用户确认）**：① DSH Host 放本机（桌面）还是云端（移动端必须云端；两种并存则会话需跨端同步）；② 本机选择数据（自选等）上云方式；③ 登录方式与认证服务自建 / 托管；④ 模型计费模式；⑤ 大陆用户与应用商店的登录要求；⑥ 公共资料与 Wiki 是否全员共享、个人研究如何隔离。
