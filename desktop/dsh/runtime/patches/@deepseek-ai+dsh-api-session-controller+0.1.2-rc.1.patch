diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/client.js b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/client.js
index 1ed2102..d436fd8 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/client.js
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/client.js
@@ -1499,6 +1499,23 @@ window.__ModuleLoader__.load({
 				this.refreshSubagents(address.childSessionId);
 				this.notifier.notifyNow();
 			}
+			/**
+			* Retain a healthy child address without changing navigation state.
+			* The caller must refresh the direct-parent catalog before retaining.
+			* @param address - catalog-derived parent and child ids.
+			*/
+			retainSubagent(address) {
+				const catalog = this.catalogs.get(address.parentSessionId);
+				const entry = catalog?.state === "ready" ? catalog.entries.find((candidate) => candidate.id === address.childSessionId) : void 0;
+				if (entry === void 0 || entry.kind !== "child" || entry.mode !== address.mode) throw new Error(`sessions.retainSubagent: ${address.childSessionId} is not a healthy catalog child`);
+				this.addresses.set(address.childSessionId, address);
+				this.sessions.get(address.childSessionId)?.configureSubagent(address, catalog.parentAvailable);
+			}
+			/** Remove an address introduced only for a read hold, unless it became the current navigation target. */
+			releaseRetainedSubagent(address) {
+				const retained = this.addresses.get(address.childSessionId);
+				if (this.selected !== address.childSessionId && retained?.parentSessionId === address.parentSessionId && retained.mode === address.mode) this.addresses.delete(address.childSessionId);
+			}
 			/** Clear the selection (the layout falls to the no-session view state). */
 			clearSelection() {
 				this.selected = void 0;
@@ -2258,6 +2275,8 @@ window.__ModuleLoader__.load({
 			*/
 			selection;
 			scopes = /* @__PURE__ */ new Map();
+			/** Explicit read-only consumers keeping catalog-addressed child bindings alive. */
+			retainedSubagents = /* @__PURE__ */ new Map();
 			/** In-flight scope drops remain here after records leave `scopes`, so root disposal can await quiescence. */
 			scopeDrops = /* @__PURE__ */ new Set();
 			/**
@@ -2298,6 +2317,7 @@ window.__ModuleLoader__.load({
 					disposeManagerProjection();
 					const scopes = [...this.scopes];
 					this.scopes.clear();
+					this.retainedSubagents.clear();
 					this.deferredRemovals.clear();
 					this.watched = void 0;
 					for (const [id, record] of scopes) this.startScopeDrop(id, record);
@@ -2321,6 +2341,46 @@ window.__ModuleLoader__.load({
 				this.manager.selectSubagent(address);
 			}
 			/**
+			* Keep a catalog-addressed child binding readable without selecting or opening it.
+			* @param address - exact address from a successfully refreshed direct-parent catalog.
+			* @returns the stable binding and an idempotent release callback.
+			*/
+			retainSubagent(address) {
+				const existing = this.retainedSubagents.get(address.childSessionId);
+				if (existing !== void 0 && (existing.address.parentSessionId !== address.parentSessionId || existing.address.mode !== address.mode)) throw new Error(`sessions.retainSubagent: ${address.childSessionId} is already retained through another address`);
+				const priorAddress = this.manager.subagentAddress(address.childSessionId);
+				if (priorAddress !== void 0 && (priorAddress.parentSessionId !== address.parentSessionId || priorAddress.mode !== address.mode)) throw new Error(`sessions.retainSubagent: ${address.childSessionId} is already retained through another address`);
+				this.manager.retainSubagent(address);
+				this.retainedSubagents.set(address.childSessionId, {
+					address,
+					count: (existing?.count ?? 0) + 1,
+					ownsAddress: existing?.ownsAddress ?? priorAddress === void 0
+				});
+				const binding = this.resolve(address.childSessionId)?.binding;
+				if (binding === void 0) {
+					this.retainedSubagents.delete(address.childSessionId);
+					if (existing === void 0 && priorAddress === void 0) this.manager.releaseRetainedSubagent(address);
+					throw new Error(`sessions.retainSubagent: ${address.childSessionId} could not be materialized`);
+				}
+				let disposed = false;
+				return {
+					binding,
+					dispose: () => {
+						if (disposed) return;
+						disposed = true;
+						const retained = this.retainedSubagents.get(address.childSessionId);
+						if (retained === void 0) return;
+						if (retained.count > 1) {
+							this.retainedSubagents.set(address.childSessionId, { ...retained, count: retained.count - 1 });
+							return;
+						}
+						this.retainedSubagents.delete(address.childSessionId);
+						if (retained.ownsAddress) this.manager.releaseRetainedSubagent(address);
+						this.pruneScopes();
+					}
+				};
+			}
+			/**
 			* Resolve an already discovered direct-parent address without opening it.
 			* Feature plugins use this to avoid Agent-bound RPCs in persisted child views.
 			* @param id - possible addressed child id.
@@ -2575,7 +2635,7 @@ window.__ModuleLoader__.load({
 			/** The one aliveness predicate shared by scope mint and prune: host-listed or currently addressed. */
 			eligible(id) {
 				const { ids, current } = this.list.getSnapshot();
-				return current === id || ids.includes(id);
+				return current === id || ids.includes(id) || this.retainedSubagents.has(id);
 			}
 			/** Project the manager's list snapshot into the store (title derivation is display-only). */
 			projectList() {
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/index.js b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/index.js
index 9f8e63c..3e2716d 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/index.js
@@ -1337,7 +1337,7 @@ var SessionHistoryController = class {
 				signal,
 				projectionMode: withProjections || address.kind === "subagent" ? "all" : "none"
 			});
-			if (observation.header.cwd === void 0) {
+			if (address.kind === "session" && observation.header.cwd === void 0) {
 				observation[Symbol.dispose]();
 				rejectNotFound(address);
 			}
@@ -2612,7 +2612,10 @@ let SessionController = (() => {
 			ctx.plugin(SessionFileReferences);
 			ctx.plugin(SessionSkillCatalog);
 			ctx.on("session/created", (session) => {
-				ctx.emit("api-session/added", this.listState.summaryFor(session));
+				// Projection providers may expose non-plain runtime values. The control stream
+				// carries their encoded form; keep this immediate event strictly JSON-safe.
+				const { projections: _projections, ...remoteSummary } = this.listState.summaryFor(session);
+				ctx.emit("api-session/added", remoteSummary);
 			});
 			ctx.on("session/disposed", (session) => {
 				ctx.emit("api-session/removed", session.id);
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/contract/sessions.d.ts b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/contract/sessions.d.ts
index 037b920..dafaa1c 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/contract/sessions.d.ts
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/contract/sessions.d.ts
@@ -11,7 +11,7 @@ import type { WorkspaceId } from '@deepseek-ai/dsh-workspace/types';
 import type { RemoteResult } from '@deepseek-ai/dsh-typert-protocol';
 import type { AgentContext } from '../scope.ts';
 import type { SessionSearchResultItem } from '../sessions/manager.ts';
-import type { SessionBinding, SessionListState } from '../sessions/service.ts';
+import type { RetainedSubagentBinding, SessionBinding, SessionListState } from '../sessions/service.ts';
 import type { SessionFace } from './session.ts';
 import type { ObservableSnapshot } from '@deepseek-ai/dsh-client-store';
 export type { AgentContext } from '../scope.ts';
@@ -45,6 +45,11 @@ export interface ISessions {
      * @param address - catalog-derived parent and child ids.
      */
     openSubagent(address: SubagentAddress): void;
+    /**
+     * Keep a catalog-addressed child binding readable without changing the current session.
+     * The direct-parent catalog must have been refreshed successfully first.
+     */
+    retainSubagent(address: SubagentAddress): RetainedSubagentBinding;
     /**
      * Resolve an already discovered direct-parent address without opening it.
      * @param id - possible addressed child id.
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/index.d.ts b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/index.d.ts
index d3fe21c..cdf814c 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/index.d.ts
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/index.d.ts
@@ -5,7 +5,7 @@ export type { ClientSessionPageRequest, SessionControlStream, SessionControlStre
 export { createScope, scopeOf } from './scope.ts';
 export type { AgentContext, AgentScopeHandle } from './scope.ts';
 export { SessionCreateError, SessionForkError } from './sessions/service.ts';
-export type { SessionBinding, SessionListState, SessionSummary } from './sessions/service.ts';
+export type { RetainedSubagentBinding, SessionBinding, SessionListState, SessionSummary } from './sessions/service.ts';
 export type { SessionListPhase, SessionListSnapshot, SessionSearchResultItem, SubagentCatalogSnapshot, } from './sessions/manager.ts';
 export type { Session } from './sessions/session.ts';
 export type { ProjectionsBaseline, ProjectionValueStore, SessionProjectionMap, UseProjection, } from './sessions/projection-store.ts';
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.d.ts b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.d.ts
index f6f616a..76092e7 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.d.ts
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.d.ts
@@ -106,6 +106,14 @@ export declare class SessionManager {
      * @param address - catalog-derived parent and child ids.
      */
     selectSubagent(address: SubagentAddress): void;
+    /**
+     * Retain a healthy child address without changing navigation state.
+     * The caller must refresh the direct-parent catalog before retaining.
+     * @param address - catalog-derived parent and child ids.
+     */
+    retainSubagent(address: SubagentAddress): void;
+    /** Remove an address introduced only for a read hold, unless it became the current navigation target. */
+    releaseRetainedSubagent(address: SubagentAddress): void;
     /** Clear the selection (the layout falls to the no-session view state). */
     clearSelection(): void;
     /**
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.js b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.js
index 73d85a5..860e8be 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.js
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/manager.js
@@ -115,6 +115,31 @@ export class SessionManager {
         void this.refreshSubagents(address.childSessionId);
         this.notifier.notifyNow();
     }
+    /**
+     * Retain a healthy child address without changing navigation state.
+     * The caller must refresh the direct-parent catalog before retaining.
+     * @param address - catalog-derived parent and child ids.
+     */
+    retainSubagent(address) {
+        const catalog = this.catalogs.get(address.parentSessionId);
+        const entry = catalog?.state === 'ready'
+            ? catalog.entries.find(candidate => candidate.id === address.childSessionId)
+            : undefined;
+        if (entry === undefined || entry.kind !== 'child' || entry.mode !== address.mode) {
+            throw new Error(`sessions.retainSubagent: ${address.childSessionId} is not a healthy catalog child`);
+        }
+        this.addresses.set(address.childSessionId, address);
+        this.sessions.get(address.childSessionId)?.configureSubagent(address, catalog.parentAvailable);
+    }
+    /** Remove an address introduced only for a read hold, unless it became the current navigation target. */
+    releaseRetainedSubagent(address) {
+        const retained = this.addresses.get(address.childSessionId);
+        if (this.selected !== address.childSessionId
+            && retained?.parentSessionId === address.parentSessionId
+            && retained.mode === address.mode) {
+            this.addresses.delete(address.childSessionId);
+        }
+    }
     /** Clear the selection (the layout falls to the no-session view state). */
     clearSelection() {
         this.selected = undefined;
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.d.ts b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.d.ts
index e6fb2c6..49e6581 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.d.ts
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.d.ts
@@ -108,6 +108,12 @@ export interface SessionBinding {
     readonly eventSource: SessionEventSource;
     readonly ctx: AgentContext;
 }
+/** A reference-counted read-only hold on a catalog-addressed child binding. */
+export interface RetainedSubagentBinding {
+    readonly binding: SessionBinding;
+    /** Release this hold. Repeated calls are no-ops. */
+    dispose(): void;
+}
 export { scopeOf } from '../scope.ts';
 /** Root sessions service: list store, current selection, object-layer manager, scope tree, bindings, and breadcrumb routes. */
 export declare class ClientSessions implements ISessions {
@@ -133,6 +139,8 @@ export declare class ClientSessions implements ISessions {
      */
     private readonly selection;
     private readonly scopes;
+    /** Explicit read-only consumers keeping catalog-addressed child bindings alive. */
+    private readonly retainedSubagents;
     /** In-flight scope drops remain here after records leave `scopes`, so root disposal can await quiescence. */
     private readonly scopeDrops;
     /**
@@ -159,6 +167,12 @@ export declare class ClientSessions implements ISessions {
      * @param address - catalog-derived parent and child ids.
      */
     openSubagent(address: SubagentAddress): void;
+    /**
+     * Keep a catalog-addressed child binding readable without selecting or opening it.
+     * @param address - exact address from a successfully refreshed direct-parent catalog.
+     * @returns the stable binding and an idempotent release callback.
+     */
+    retainSubagent(address: SubagentAddress): RetainedSubagentBinding;
     /**
      * Resolve an already discovered direct-parent address without opening it.
      * Feature plugins use this to avoid Agent-bound RPCs in persisted child views.
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.js b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.js
index 6786a43..113bfc0 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.js
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/client/sessions/service.js
@@ -93,6 +93,8 @@ export class ClientSessions {
      */
     selection;
     scopes = new Map();
+    /** Explicit read-only consumers keeping catalog-addressed child bindings alive. */
+    retainedSubagents = new Map();
     /** In-flight scope drops remain here after records leave `scopes`, so root disposal can await quiescence. */
     scopeDrops = new Set();
     /**
@@ -136,6 +138,7 @@ export class ClientSessions {
             disposeManagerProjection();
             const scopes = [...this.scopes];
             this.scopes.clear();
+            this.retainedSubagents.clear();
             this.deferredRemovals.clear();
             this.watched = undefined;
             for (const [id, record] of scopes)
@@ -159,6 +162,57 @@ export class ClientSessions {
     openSubagent(address) {
         this.manager.selectSubagent(address);
     }
+    /**
+     * Keep a catalog-addressed child binding readable without selecting or opening it.
+     * @param address - exact address from a successfully refreshed direct-parent catalog.
+     * @returns the stable binding and an idempotent release callback.
+     */
+    retainSubagent(address) {
+        const existing = this.retainedSubagents.get(address.childSessionId);
+        if (existing !== undefined
+            && (existing.address.parentSessionId !== address.parentSessionId
+                || existing.address.mode !== address.mode)) {
+            throw new Error(`sessions.retainSubagent: ${address.childSessionId} is already retained through another address`);
+        }
+        const priorAddress = this.manager.subagentAddress(address.childSessionId);
+        if (priorAddress !== undefined
+            && (priorAddress.parentSessionId !== address.parentSessionId || priorAddress.mode !== address.mode)) {
+            throw new Error(`sessions.retainSubagent: ${address.childSessionId} is already retained through another address`);
+        }
+        this.manager.retainSubagent(address);
+        this.retainedSubagents.set(address.childSessionId, {
+            address,
+            count: (existing?.count ?? 0) + 1,
+            ownsAddress: existing?.ownsAddress ?? priorAddress === undefined,
+        });
+        const binding = this.resolve(address.childSessionId)?.binding;
+        if (binding === undefined) {
+            this.retainedSubagents.delete(address.childSessionId);
+            if (existing === undefined && priorAddress === undefined)
+                this.manager.releaseRetainedSubagent(address);
+            throw new Error(`sessions.retainSubagent: ${address.childSessionId} could not be materialized`);
+        }
+        let disposed = false;
+        return {
+            binding,
+            dispose: () => {
+                if (disposed)
+                    return;
+                disposed = true;
+                const retained = this.retainedSubagents.get(address.childSessionId);
+                if (retained === undefined)
+                    return;
+                if (retained.count > 1) {
+                    this.retainedSubagents.set(address.childSessionId, { ...retained, count: retained.count - 1 });
+                    return;
+                }
+                this.retainedSubagents.delete(address.childSessionId);
+                if (retained.ownsAddress)
+                    this.manager.releaseRetainedSubagent(address);
+                this.pruneScopes();
+            },
+        };
+    }
     /**
      * Resolve an already discovered direct-parent address without opening it.
      * Feature plugins use this to avoid Agent-bound RPCs in persisted child views.
@@ -428,7 +482,7 @@ export class ClientSessions {
     /** The one aliveness predicate shared by scope mint and prune: host-listed or currently addressed. */
     eligible(id) {
         const { ids, current } = this.list.getSnapshot();
-        return current === id || ids.includes(id);
+        return current === id || ids.includes(id) || this.retainedSubagents.has(id);
     }
     /** Project the manager's list snapshot into the store (title derivation is display-only). */
     projectList() {
diff --git a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/history.js b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/history.js
index 3292a32..2a001eb 100644
--- a/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/history.js
+++ b/node_modules/@deepseek-ai/dsh-api-session-controller/lib/types/history.js
@@ -232,7 +232,7 @@ export class SessionHistoryController {
                 signal,
                 projectionMode: withProjections || address.kind === 'subagent' ? 'all' : 'none',
             });
-            if (observation.header.cwd === undefined) {
+            if (address.kind === 'session' && observation.header.cwd === undefined) {
                 observation[Symbol.dispose]();
                 rejectNotFound(address);
             }
